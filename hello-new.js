!function(gin1, gin2, gin3, gin4, gin5, gin6, gin7, gin8, gin9, gin10, gin11, gin12, gin13, gin14, gin15, gin16, gin17, gin18, gin19, gin20, gin21, gin22, gin23, gin24, gin25, gin26, gin27, gin28, gin29, gin30, gin31, gin32, gin33, gin34, gin35, gin36, gin37, gin38, gin39, gin40, gin41, gin42, gin43, gin44, gin45, gin46, gin47, gin48, gin49, gin50, gin51, gin52, gin53, gin54, gin55, gin56, gin57, gin58, gin59, gin60, gin61, gin62, gin63, gin64, gin65, gin66, gin67, gin68, gin69, gin70, gin71, gin72, gin73, gin74, gin75, gin76, gin77, gin78, gin79, gin80, gin81, gin82, gin83, gin84, gin85, gin86, gin87, gin88, gin89, gin90, gin91, gin92, gin93, gin94, gin95, gin96, gin97, gin98, gin99, gin100, gin101, gin102, gin103, gin104, gin105, gin106, gin107, gin108, gin109, gin110, gin111, gin112, gin113, gin114, gin115, gin116, gin117, gin118, gin119, gin120, gin121, gin122, gin123, gin124, gin125, gin126, gin127, gin128, gin129, gin130, gin131, gin132, gin133, gin134, gin135, gin136, gin137, gin138, gin139, gin140, gin141, gin142, gin143, gin144, gin145, gin146, gin147, gin148, gin149, gin150, gin151, gin152, gin153, gin154, gin155, gin156, gin157, gin158, gin159, gin160, gin161, gin162, gin163, gin164, gin165, gin166, gin167, gin168, gin169, gin170, gin171, gin172, gin173, gin174, gin175, gin176, gin177, gin178, gin179, gin180, gin181, gin182, gin183, gin184, gin185, gin186, gin187, gin188, gin189, gin190, gin191, gin192, gin193, gin194, gin195, gin196, gin197, gin198, gin199, gin200, gin201, gin202, gin203, gin204, gin205, gin206, gin207, gin208, gin209, gin210, gin211, gin212, gin213, gin214, gin215, gin216, gin217, gin218, gin219, gin220, gin221, gin222, gin223, gin224, gin225, gin226, gin227, gin228, gin229, gin230, gin231, gin232, gin233, gin234, gin235, gin236, gin237, gin238, gin239, gin240, gin241, gin242, gin243, gin244, gin245, gin246, gin247, gin248, gin249, gin250, gin251, gin252, gin253, gin254, gin255, gin256, gin257, gin258, gin259, gin260, gin261, gin262, gin263, gin264, gin265, gin266, gin267, gin268, gin269, gin270, gin271, gin272, gin273, gin274, gin275, gin276, gin277, gin278, gin279, gin280, gin281, gin282, gin283, gin284, gin285, gin286, gin287, gin288, gin289, gin290, gin291, gin292, gin293, gin294, gin295, gin296, gin297, gin298, gin299, gin300, gin301, gin302, gin303, gin304, gin305, gin306, gin307, gin308, gin309, gin310, gin311, gin312, gin313, gin314, gin315, gin316, gin317, gin318, gin319, gin320, gin321, gin322, gin323, gin324, gin325, gin326, gin327, gin328, gin329, gin330, gin331, gin332, gin333, gin334, gin335, gin336, gin337, gin338, gin339, gin340, gin341, gin342, gin343, gin344, gin345, gin346, gin347, gin348, gin349, gin350, gin351, gin352, gin353, gin354, gin355, gin356, gin357, gin358, gin359, gin360, gin361, gin362, gin363, gin364, gin365, gin366, gin367, gin368, gin369, gin370, gin371, gin372, gin373, gin374, gin375, gin376, gin377, gin378, gin379, gin380, gin381, gin382, gin383, gin384, gin385, gin386, gin387, gin388, gin389, gin390, gin391, gin392, gin393, gin394, gin395, gin396, gin397, gin398, gin399, gin400, gin401, gin402, gin403, gin404, gin405, gin406, gin407, gin408, gin409, gin410, gin411, gin412, gin413, gin414, gin415, gin416, gin417, gin418, gin419, gin420, gin421, gin422, gin423, gin424, gin425, gin426, gin427, gin428, gin429, gin430, gin431, gin432, gin433, gin434, gin435, gin436, gin437, gin438, gin439, gin440, gin441, gin442, gin443, gin444, gin445, gin446, gin447, gin448, gin449, gin450, gin451, gin452, gin453, gin454, gin455, gin456, gin457, gin458, gin459, gin460, gin461, gin462, gin463, gin464, gin465, gin466, gin467, gin468, gin469, gin470, gin471, gin472, gin473, gin474, gin475, gin476, gin477, gin478, gin479, gin480, gin481, gin482, gin483, gin484, gin485, gin486, gin487, gin488, gin489, gin490, gin491, gin492, gin493, gin494, gin495, gin496, gin497, gin498, gin499, gin500, gin501, gin502, gin503, gin504, gin505, gin506, gin507, gin508, gin509, gin510, gin511, gin512, gin513, gin514, gin515, gin516, gin517, gin518, gin519, gin520, gin521, gin522, gin523, gin524, gin525, gin526, gin527, gin528, gin529, gin530, gin531, gin532, gin533, gin534, gin535, gin536, gin537, gin538, gin539, gin540, gin541, gin542, gin543, gin544, gin545, gin546, gin547, gin548, gin549, gin550, gin551, gin552, gin553, gin554, gin555, gin556, gin557, gin558, gin559, gin560, gin561, gin562, gin563, gin564, gin565, gin566, gin567, gin568, gin569, gin570, gin571, gin572, gin573, gin574, gin575, gin576, gin577, gin578, gin579, gin580, gin581, gin582, gin583, gin584, gin585, gin586, gin587, gin588, gin589, gin590, gin591, gin592, gin593, gin594, gin595, gin596, gin597, gin598, gin599, gin600, gin601, gin602, gin603, gin604, gin605, gin606, gin607, gin608, gin609, gin610, gin611, gin612, gin613, gin614, gin615, gin616, gin617, gin618, gin619, gin620, gin621, gin622, gin623, gin624, gin625, gin626, gin627, gin628, gin629, gin630, gin631, gin632, gin633, gin634, gin635, gin636, gin637, gin638, gin639, gin640, gin641, gin642, gin643, gin644, gin645, gin646, gin647, gin648, gin649, gin650, gin651, gin652, gin653, gin654, gin655, gin656, gin657, gin658, gin659, gin660, gin661, gin662, gin663, gin664, gin665, gin666, gin667, gin668, gin669, gin670, gin671, gin672, gin673, gin674, gin675, gin676, gin677, gin678, gin679, gin680, gin681, gin682, gin683, gin684, gin685, gin686, gin687, gin688, gin689, gin690, gin691, gin692, gin693, gin694, gin695, gin696, gin697, gin698, gin699, gin700, gin701, gin702, gin703, gin704, gin705, gin706, gin707, gin708, gin709, gin710, gin711, gin712, gin713, gin714, gin715, gin716, gin717, gin718, gin719, gin720, gin721, gin722, gin723, gin724, gin725, gin726, gin727, gin728, gin729, gin730, gin731, gin732, gin733, gin734, gin735, gin736, gin737, gin738, gin739, gin740, gin741, gin742, gin743, gin744, gin745, gin746, gin747, gin748, gin749, gin750, gin751, gin752, gin753, gin754, gin755, gin756, gin757, gin758, gin759, gin760, gin761, gin762, gin763, gin764, gin765, gin766, gin767, gin768, gin769, gin770, gin771, gin772, gin773, gin774, gin775, gin776, gin777, gin778, gin779, gin780, gin781, gin782, gin783, gin784, gin785, gin786, gin787, gin788, gin789, gin790, gin791, gin792, gin793, gin794, gin795, gin796, gin797, gin798, gin799, gin800, gin801, gin802, gin803, gin804, gin805, gin806, gin807, gin808, gin809, gin810, gin811, gin812, gin813, gin814, gin815, gin816, gin817, gin818, gin819, gin820, gin821, gin822, gin823, gin824, gin825, gin826) {
  (function(global, factory) {
  "use strict";
  if (typeof module === gin2 && typeof module.exports === gin2) {
    module[gin3] = global.document ? factory(global, true) : function(w) {
  if (!w.document) {
    throw new Error("jQuery requires a window with a document");
  }
  return factory(w);
};
  } else {
    factory(global);
  }
})(typeof window !== gin6 ? window : this, function(window, noGlobal) {
  "use strict";
  var arr = [];
  var document = gin5.document;
  var getProto = Object.getPrototypeOf;
  var slice = arr.slice;
  var concat = arr.concat;
  var push = arr.push;
  var indexOf = arr.indexOf;
  var class2type = {};
  var toString = class2type.toString;
  var hasOwn = class2type.hasOwnProperty;
  var fnToString = hasOwn.toString;
  var ObjectFunctionString = fnToString[gin7](Object);
  var support = {};
  function DOMEval(code, doc) {
    doc = doc || document;
    var script = doc[gin8](gin9);
    script[gin10] = code;
    doc[gin11][gin12](script)[gin13][gin14](script);
  }
  var version = "3.2.1", jQuery = function(selector, context) {
  return new jQuery[gin16].init(selector, context);
}, rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, rmsPrefix = /^-ms-/, rdashAlpha = /-([a-z])/g, fcamelCase = function(all, letter) {
  return letter[gin17]();
};
  jQuery[gin16] = jQuery[gin18] = {
  jquery: version, 
  constructor: jQuery, 
  length: 0, 
  toArray: function() {
  return slice[gin7](this);
}, 
  get: function(num) {
  if (num == null) {
    return slice[gin7](this);
  }
  return num < 0 ? this[num + this[gin19]] : this[num];
}, 
  pushStack: function(elems) {
  var ret = jQuery[gin20](this[gin21](), elems);
  ret[gin22] = this;
  return ret;
}, 
  each: function(callback) {
  return jQuery[gin23](this, callback);
}, 
  map: function(callback) {
  return this[gin24](jQuery[gin25](this, function(elem, i) {
  return callback[gin7](elem, i, elem);
}));
}, 
  slice: function() {
  return this[gin24](slice[gin26](this, arguments));
}, 
  first: function() {
  return this[gin27](0);
}, 
  last: function() {
  return this[gin27](-1);
}, 
  eq: function(i) {
  var len = this.length, j = +i + (i < 0 ? len : 0);
  return this[gin24](j >= 0 && j < len ? [this[j]] : []);
}, 
  end: function() {
  return this[gin22] || this[gin21]();
}, 
  push: push, 
  sort: arr[gin28], 
  splice: arr[gin29]};
  jQuery[gin30] = jQuery[gin16][gin30] = function() {
  var options, name, src, copy, copyIsArray, clone, target = arguments[0] || {}, i = 1, length = arguments.length, deep = false;
  if (typeof target === gin31) {
    deep = target;
    target = arguments[i] || {};
    i++;
  }
  if (typeof target !== gin2 && !jQuery[gin32](target)) {
    target = {};
  }
  if (i === length) {
    target = this;
    i--;
  }
  for (; i < length; i++) {
    if ((options = arguments[i]) != null) {
      for (name in options) {
        src = target[name];
        copy = options[name];
        if (target === copy) {
          continue;
        }
        if (deep && copy && (jQuery[gin33](copy) || (copyIsArray = Array[gin34](copy)))) {
          if (copyIsArray) {
            copyIsArray = false;
            clone = src && Array[gin34](src) ? src : [];
          } else {
            clone = src && jQuery[gin33](src) ? src : {};
          }
          target[name] = jQuery[gin30](deep, clone, copy);
        } else if (copy !== undefined) {
          target[name] = copy;
        }
      }
    }
  }
  return target;
};
  jQuery[gin30]({
  expando: gin35 + (version + Math[gin36]())[gin37](/\D/g, gin38), 
  isReady: true, 
  error: function(msg) {
  throw new Error(msg);
}, 
  noop: function() {
}, 
  isFunction: function(obj) {
  return jQuery[gin39](obj) === gin40;
}, 
  isWindow: function(obj) {
  return obj != null && obj === obj[gin41];
}, 
  isNumeric: function(obj) {
  var type = jQuery[gin39](obj);
  return (type === gin42 || type === gin43) && !isNaN(obj - parseFloat(obj));
}, 
  isPlainObject: function(obj) {
  var proto, Ctor;
  if (!obj || toString[gin7](obj) !== gin44) {
    return false;
  }
  proto = getProto(obj);
  if (!proto) {
    return true;
  }
  Ctor = hasOwn[gin7](proto, gin21) && proto[gin21];
  return typeof Ctor === gin40 && fnToString[gin7](Ctor) === ObjectFunctionString;
}, 
  isEmptyObject: function(obj) {
  var name;
  for (name in obj) {
    return false;
  }
  return true;
}, 
  type: function(obj) {
  if (obj == null) {
    return obj + gin38;
  }
  return typeof obj === gin2 || typeof obj === gin40 ? class2type[toString[gin7](obj)] || gin2 : typeof obj;
}, 
  globalEval: function(code) {
  DOMEval(code);
}, 
  camelCase: function(string) {
  return string[gin37](rmsPrefix, gin45)[gin37](rdashAlpha, fcamelCase);
}, 
  each: function(obj, callback) {
  var length, i = 0;
  if (isArrayLike(obj)) {
    length = obj[gin19];
    for (; i < length; i++) {
      if (callback[gin7](obj[i], i, obj[i]) === false) {
        break;
      }
    }
  } else {
    for (i in obj) {
      if (callback[gin7](obj[i], i, obj[i]) === false) {
        break;
      }
    }
  }
  return obj;
}, 
  trim: function(text) {
  return text == null ? "" : (text + gin38)[gin37](rtrim, gin38);
}, 
  makeArray: function(arr, results) {
  var ret = results || [];
  if (arr != null) {
    if (isArrayLike(Object(arr))) {
      jQuery[gin20](ret, typeof arr === gin43 ? [arr] : arr);
    } else {
      push[gin7](ret, arr);
    }
  }
  return ret;
}, 
  inArray: function(elem, arr, i) {
  return arr == null ? -1 : indexOf[gin7](arr, elem, i);
}, 
  merge: function(first, second) {
  var len = +second.length, j = 0, i = first.length;
  for (; j < len; j++) {
    first[i++] = second[j];
  }
  first[gin19] = i;
  return first;
}, 
  grep: function(elems, callback, invert) {
  var callbackInverse, matches = [], i = 0, length = elems.length, callbackExpect = !invert;
  for (; i < length; i++) {
    callbackInverse = !callback(elems[i], i);
    if (callbackInverse !== callbackExpect) {
      matches[gin46](elems[i]);
    }
  }
  return matches;
}, 
  map: function(elems, callback, arg) {
  var length, value, i = 0, ret = [];
  if (isArrayLike(elems)) {
    length = elems[gin19];
    for (; i < length; i++) {
      value = callback(elems[i], i, arg);
      if (value != null) {
        ret[gin46](value);
      }
    }
  } else {
    for (i in elems) {
      value = callback(elems[i], i, arg);
      if (value != null) {
        ret[gin46](value);
      }
    }
  }
  return concat[gin26]([], ret);
}, 
  guid: 1, 
  proxy: function(fn, context) {
  var tmp, args, proxy;
  if (typeof context === gin43) {
    tmp = fn[context];
    context = fn;
    fn = tmp;
  }
  if (!jQuery[gin32](fn)) {
    return undefined;
  }
  args = slice[gin7](arguments, 2);
  proxy = function() {
  return fn[gin26](context || this, args[gin47](slice[gin7](arguments)));
};
  proxy[gin48] = fn[gin48] = fn[gin48] || jQuery.guid++;
  return proxy;
}, 
  now: Date[gin49], 
  support: support});
  if (typeof Symbol === gin40) {
    jQuery[gin16][Symbol[gin50]] = arr[Symbol[gin50]];
  }
  jQuery[gin23](gin51[gin52](gin53), function(i, name) {
  class2type[gin54 + name + gin55] = name[gin56]();
});
  function isArrayLike(obj) {
    var length = !!obj && gin19 in obj && obj[gin19], type = jQuery[gin39](obj);
    if (type === gin40 || jQuery[gin57](obj)) {
      return false;
    }
    return type === gin58 || length === 0 || typeof length === gin42 && length > 0 && (length - 1) in obj;
  }
  var Sizzle = (function(window) {
  var i, support, Expr, getText, isXML, tokenize, compile, select, outermostContext, sortInput, hasDuplicate, setDocument, document, docElem, documentIsHTML, rbuggyQSA, rbuggyMatches, matches, contains, expando = gin59 + 1 * new Date(), preferredDoc = gin5.document, dirruns = 0, done = 0, classCache = createCache(), tokenCache = createCache(), compilerCache = createCache(), sortOrder = function(a, b) {
  if (a === b) {
    hasDuplicate = true;
  }
  return 0;
}, hasOwn = ({}).hasOwnProperty, arr = [], pop = arr.pop, push_native = arr.push, push = arr.push, slice = arr.slice, indexOf = function(list, elem) {
  var i = 0, len = list.length;
  for (; i < len; i++) {
    if (list[i] === elem) {
      return i;
    }
  }
  return -1;
}, booleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", whitespace = "[\\x20\\t\\r\\n\\f]", identifier = "(?:\\\\.|[\\w-]|[^\x00-\\xa0])+", attributes = gin63 + whitespace + gin64 + identifier + gin65 + whitespace + gin66 + whitespace + gin67 + identifier + gin68 + whitespace + gin69, pseudos = gin70 + identifier + gin71 + gin72 + gin73 + attributes + gin74 + gin75 + gin76, rwhitespace = new RegExp(whitespace + gin77, "g"), rtrim = new RegExp(gin79 + whitespace + gin80 + whitespace + gin81, "g"), rcomma = new RegExp(gin79 + whitespace + gin82 + whitespace + gin83), rcombinators = new RegExp(gin79 + whitespace + gin84 + whitespace + gin85 + whitespace + gin83), rattributeQuotes = new RegExp(gin86 + whitespace + gin87 + whitespace + gin69, "g"), rpseudo = new RegExp(pseudos), ridentifier = new RegExp(gin79 + identifier + gin88), matchExpr = {
  gin89: new RegExp(gin90 + identifier + gin85), 
  gin91: new RegExp(gin92 + identifier + gin85), 
  gin93: new RegExp(gin94 + identifier + gin95), 
  gin96: new RegExp(gin79 + attributes), 
  gin97: new RegExp(gin79 + pseudos), 
  gin98: new RegExp(gin99 + whitespace + gin100 + whitespace + gin101 + whitespace + gin102 + whitespace + gin103, "i"), 
  gin105: new RegExp(gin106 + booleans + gin107, "i"), 
  gin108: new RegExp(gin79 + whitespace + gin109 + whitespace + gin110 + whitespace + gin111, "i")}, rinputs = /^(?:input|select|textarea|button)$/i, rheader = /^h\d$/i, rnative = /^[^{]+\{\s*\[native \w/, rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/, rsibling = /[+~]/, runescape = new RegExp(gin112 + whitespace + gin113 + whitespace + gin114, "ig"), funescape = function(_, escaped, escapedWhitespace) {
  var high = gin116 + escaped - 0x10000;
  return high !== high || escapedWhitespace ? escaped : high < 0 ? String[gin117](high + 0x10000) : String[gin117](high >> 10 | 0xD800, high & 0x3FF | 0xDC00);
}, rcssescape = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\0-\x1f\x7f-\uFFFF\w-]/g, fcssescape = function(ch, asCodePoint) {
  if (asCodePoint) {
    if (ch === gin118) {
      return "\ufffd";
    }
    return ch[gin120](0, -1) + gin121 + ch[gin122](ch[gin19] - 1)[gin123](16) + gin53;
  }
  return gin121 + ch;
}, unloadHandler = function() {
  setDocument();
}, disabledAncestor = addCombinator(function(elem) {
  return elem[gin124] === true && (gin125 in elem || gin126 in elem);
}, {
  dir: gin13, 
  next: gin127});
  try {
    push[gin26]((arr = slice[gin7](preferredDoc[gin128])), preferredDoc[gin128]);
    arr[preferredDoc[gin128][gin19]].nodeType;
  }  catch (e) {
  push = {
  apply: arr.length ? function(target, els) {
  push_native[gin26](target, slice[gin7](els));
} : function(target, els) {
  var j = target.length, i = 0;
  while ((target[j++] = els[i++])) {
  }
  target[gin19] = j - 1;
}};
}
  function Sizzle(selector, context, results, seed) {
    var m, i, elem, nid, match, groups, newSelector, newContext = context && context[gin129], nodeType = context ? context.nodeType : 9;
    results = results || [];
    if (typeof selector !== gin43 || !selector || nodeType !== 1 && nodeType !== 9 && nodeType !== 11) {
      return results;
    }
    if (!seed) {
      if ((context ? context[gin129] || context : preferredDoc) !== document) {
        setDocument(context);
      }
      context = context || document;
      if (documentIsHTML) {
        if (nodeType !== 11 && (match = rquickExpr[gin130](selector))) {
          if ((m = match[1])) {
            if (nodeType === 9) {
              if ((elem = context[gin131](m))) {
                if (elem[gin132] === m) {
                  results[gin46](elem);
                  return results;
                }
              } else {
                return results;
              }
            } else {
              if (newContext && (elem = newContext[gin131](m)) && contains(context, elem) && elem[gin132] === m) {
                results[gin46](elem);
                return results;
              }
            }
          } else if (match[2]) {
            push[gin26](results, context[gin133](selector));
            return results;
          } else if ((m = match[3]) && support[gin134] && context[gin134]) {
            push[gin26](results, context[gin134](m));
            return results;
          }
        }
        if (support[gin135] && !compilerCache[selector + gin53] && (!rbuggyQSA || !rbuggyQSA[gin136](selector))) {
          if (nodeType !== 1) {
            newContext = context;
            newSelector = selector;
          } else if (context[gin137][gin56]() !== gin2) {
            if ((nid = context[gin138](gin132))) {
              nid = nid[gin37](rcssescape, fcssescape);
            } else {
              context[gin139](gin132, (nid = expando));
            }
            groups = tokenize(selector);
            i = groups[gin19];
            while (i--) {
              groups[i] = gin140 + nid + gin53 + toSelector(groups[i]);
            }
            newSelector = groups[gin141](gin142);
            newContext = rsibling[gin136](selector) && testContext(context[gin13]) || context;
          }
          if (newSelector) {
            try {
              push[gin26](results, newContext[gin143](newSelector));
              return results;
            }            catch (qsaError) {
}
 finally             {
              if (nid === expando) {
                context[gin144](gin132);
              }
            }
          }
        }
      }
    }
    return select(selector[gin37](rtrim, gin145), context, results, seed);
  }
  function createCache() {
    var keys = [];
    function cache(key, value) {
      if (keys[gin46](key + gin53) > Expr[gin146]) {
        delete cache[keys[gin147]()];
      }
      return (cache[key + gin53] = value);
    }
    return cache;
  }
  function markFunction(fn) {
    fn[expando] = true;
    return fn;
  }
  function assert(fn) {
    var el = document[gin8](gin148);
    try {
      return !!fn(el);
    }    catch (e) {
  return false;
}
 finally     {
      if (el.parentNode) {
        el[gin13][gin14](el);
      }
      el = null;
    }
  }
  function addHandle(attrs, handler) {
    var arr = attrs[gin52](gin149), i = arr.length;
    while (i--) {
      Expr[gin150][arr[i]] = handler;
    }
  }
  function siblingCheck(a, b) {
    var cur = b && a, diff = cur && a[gin151] === 1 && b[gin151] === 1 && a[gin152] - b[gin152];
    if (diff) {
      return diff;
    }
    if (cur) {
      while ((cur = cur[gin153])) {
        if (cur === b) {
          return -1;
        }
      }
    }
    return a ? 1 : -1;
  }
  function createInputPseudo(type) {
    return function(elem) {
  var name = elem[gin137][gin56]();
  return name === gin154 && elem[gin39] === type;
};
  }
  function createButtonPseudo(type) {
    return function(elem) {
  var name = elem[gin137][gin56]();
  return (name === gin154 || name === gin155) && elem[gin39] === type;
};
  }
  function createDisabledPseudo(disabled) {
    return function(elem) {
  if (gin125 in elem) {
    if (elem[gin13] && elem[gin124] === false) {
      if (gin126 in elem) {
        if (gin126 in elem[gin13]) {
          return elem[gin13][gin124] === disabled;
        } else {
          return elem[gin124] === disabled;
        }
      }
      return elem[gin156] === disabled || elem[gin156] !== !disabled && disabledAncestor(elem) === disabled;
    }
    return elem[gin124] === disabled;
  } else if (gin126 in elem) {
    return elem[gin124] === disabled;
  }
  return false;
};
  }
  function createPositionalPseudo(fn) {
    return markFunction(function(argument) {
  argument = +argument;
  return markFunction(function(seed, matches) {
  var j, matchIndexes = fn([], seed[gin19], argument), i = matchIndexes.length;
  while (i--) {
    if (seed[(j = matchIndexes[i])]) {
      seed[j] = !(matches[j] = seed[j]);
    }
  }
});
});
  }
  function testContext(context) {
    return context && typeof context.getElementsByTagName !== gin6 && context;
  }
  support = Sizzle[gin157] = {};
  isXML = Sizzle[gin158] = function(elem) {
  var documentElement = elem && (elem[gin129] || elem)[gin159];
  return documentElement ? documentElement[gin137] !== gin160 : false;
};
  setDocument = Sizzle[gin161] = function(node) {
  var hasCompare, subWindow, doc = node ? node[gin129] || node : preferredDoc;
  if (doc === document || doc[gin151] !== 9 || !doc.documentElement) {
    return document;
  }
  document = doc;
  docElem = document[gin159];
  documentIsHTML = !isXML(document);
  if (preferredDoc !== document && (subWindow = document[gin162]) && subWindow[gin163] !== subWindow) {
    if (subWindow.addEventListener) {
      subWindow[gin164](gin165, unloadHandler, false);
    } else if (subWindow.attachEvent) {
      subWindow[gin166](gin167, unloadHandler);
    }
  }
  support[gin168] = assert(function(el) {
  el[gin169] = gin104;
  return !el[gin138](gin169);
});
  support[gin133] = assert(function(el) {
  el[gin12](document[gin170](gin38));
  return !el[gin133](gin83).length;
});
  support[gin134] = rnative[gin136](document[gin134]);
  support[gin171] = assert(function(el) {
  docElem[gin12](el)[gin132] = expando;
  return !document.getElementsByName || !document[gin172](expando).length;
});
  if (support.getById) {
    Expr[gin173][gin89] = function(id) {
  var attrId = id[gin37](runescape, funescape);
  return function(elem) {
  return elem[gin138](gin132) === attrId;
};
};
    Expr[gin174][gin89] = function(id, context) {
  if (typeof context.getElementById !== gin6 && documentIsHTML) {
    var elem = context[gin131](id);
    return elem ? [elem] : [];
  }
};
  } else {
    Expr[gin173][gin89] = function(id) {
  var attrId = id[gin37](runescape, funescape);
  return function(elem) {
  var node = typeof elem.getAttributeNode !== gin6 && elem[gin175](gin132);
  return node && node[gin176] === attrId;
};
};
    Expr[gin174][gin89] = function(id, context) {
  if (typeof context.getElementById !== gin6 && documentIsHTML) {
    var node, i, elems, elem = context[gin131](id);
    if (elem) {
      node = elem[gin175](gin132);
      if (node && node[gin176] === id) {
        return [elem];
      }
      elems = context[gin172](id);
      i = 0;
      while ((elem = elems[i++])) {
        node = elem[gin175](gin132);
        if (node && node[gin176] === id) {
          return [elem];
        }
      }
    }
    return [];
  }
};
  }
  Expr[gin174][gin93] = support.getElementsByTagName ? function(tag, context) {
  if (typeof context.getElementsByTagName !== gin6) {
    return context[gin133](tag);
  } else if (support.qsa) {
    return context[gin143](tag);
  }
} : function(tag, context) {
  var elem, tmp = [], i = 0, results = context[gin133](tag);
  if (tag === gin83) {
    while ((elem = results[i++])) {
      if (elem[gin151] === 1) {
        tmp[gin46](elem);
      }
    }
    return tmp;
  }
  return results;
};
  Expr[gin174][gin91] = support[gin134] && function(className, context) {
  if (typeof context.getElementsByClassName !== gin6 && documentIsHTML) {
    return context[gin134](className);
  }
};
  rbuggyMatches = [];
  rbuggyQSA = [];
  if ((support[gin135] = rnative[gin136](document[gin143]))) {
    assert(function(el) {
  docElem[gin12](el)[gin177] = gin178 + expando + gin179 + gin180 + expando + gin181 + gin182;
  if (el[gin143](gin183).length) {
    rbuggyQSA[gin46](gin184 + whitespace + gin185);
  }
  if (!el[gin143](gin186).length) {
    rbuggyQSA[gin46](gin63 + whitespace + gin187 + booleans + gin85);
  }
  if (!el[gin143](gin188 + expando + gin189).length) {
    rbuggyQSA[gin46](gin190);
  }
  if (!el[gin143](gin191).length) {
    rbuggyQSA[gin46](gin191);
  }
  if (!el[gin143](gin192 + expando + gin193).length) {
    rbuggyQSA[gin46](gin194);
  }
});
    assert(function(el) {
  el[gin177] = gin195 + gin196;
  var input = document[gin8](gin154);
  input[gin139](gin39, gin197);
  el[gin12](input)[gin139](gin198, gin199);
  if (el[gin143](gin200).length) {
    rbuggyQSA[gin46](gin198 + whitespace + gin201);
  }
  if (el[gin143](gin202)[gin19] !== 2) {
    rbuggyQSA[gin46](gin202, gin203);
  }
  docElem[gin12](el)[gin124] = true;
  if (el[gin143](gin203)[gin19] !== 2) {
    rbuggyQSA[gin46](gin202, gin203);
  }
  el[gin143](gin204);
  rbuggyQSA[gin46](gin205);
});
  }
  if ((support[gin206] = rnative[gin136]((matches = docElem[gin207] || docElem[gin208] || docElem[gin209] || docElem[gin210] || docElem[gin211])))) {
    assert(function(el) {
  support[gin212] = matches[gin7](el, gin83);
  matches[gin7](el, gin213);
  rbuggyMatches[gin46](gin214, pseudos);
});
  }
  rbuggyQSA = rbuggyQSA[gin19] && new RegExp(rbuggyQSA[gin141](gin149));
  rbuggyMatches = rbuggyMatches[gin19] && new RegExp(rbuggyMatches[gin141](gin149));
  hasCompare = rnative[gin136](docElem[gin215]);
  contains = hasCompare || rnative[gin136](docElem[gin216]) ? function(a, b) {
  var adown = a[gin151] === 9 ? a.documentElement : a, bup = b && b[gin13];
  return a === bup || !!(bup && bup[gin151] === 1 && (adown.contains ? adown[gin216](bup) : a[gin215] && a[gin215](bup) & 16));
} : function(a, b) {
  if (b) {
    while ((b = b[gin13])) {
      if (b === a) {
        return true;
      }
    }
  }
  return false;
};
  sortOrder = hasCompare ? function(a, b) {
  if (a === b) {
    hasDuplicate = true;
    return 0;
  }
  var compare = !a.compareDocumentPosition - !b.compareDocumentPosition;
  if (compare) {
    return compare;
  }
  compare = (a[gin129] || a) === (b[gin129] || b) ? a[gin215](b) : 1;
  if (compare & 1 || (!support.sortDetached && b[gin215](a) === compare)) {
    if (a === document || a[gin129] === preferredDoc && contains(preferredDoc, a)) {
      return -1;
    }
    if (b === document || b[gin129] === preferredDoc && contains(preferredDoc, b)) {
      return 1;
    }
    return sortInput ? (indexOf(sortInput, a) - indexOf(sortInput, b)) : 0;
  }
  return compare & 4 ? -1 : 1;
} : function(a, b) {
  if (a === b) {
    hasDuplicate = true;
    return 0;
  }
  var cur, i = 0, aup = a.parentNode, bup = b.parentNode, ap = [a], bp = [b];
  if (!aup || !bup) {
    return a === document ? -1 : b === document ? 1 : aup ? -1 : bup ? 1 : sortInput ? (indexOf(sortInput, a) - indexOf(sortInput, b)) : 0;
  } else if (aup === bup) {
    return siblingCheck(a, b);
  }
  cur = a;
  while ((cur = cur[gin13])) {
    ap[gin217](cur);
  }
  cur = b;
  while ((cur = cur[gin13])) {
    bp[gin217](cur);
  }
  while (ap[i] === bp[i]) {
    i++;
  }
  return i ? siblingCheck(ap[i], bp[i]) : ap[i] === preferredDoc ? -1 : bp[i] === preferredDoc ? 1 : 0;
};
  return document;
};
  Sizzle[gin207] = function(expr, elements) {
  return Sizzle(expr, null, null, elements);
};
  Sizzle[gin206] = function(elem, expr) {
  if ((elem[gin129] || elem) !== document) {
    setDocument(elem);
  }
  expr = expr[gin37](rattributeQuotes, gin218);
  if (support[gin206] && documentIsHTML && !compilerCache[expr + gin53] && (!rbuggyMatches || !rbuggyMatches[gin136](expr)) && (!rbuggyQSA || !rbuggyQSA[gin136](expr))) {
    try {
      var ret = matches[gin7](elem, expr);
      if (ret || support[gin212] || elem[gin219] && elem[gin219][gin151] !== 11) {
        return ret;
      }
    }    catch (e) {
}
  }
  return Sizzle(expr, document, null, [elem])[gin19] > 0;
};
  Sizzle[gin216] = function(context, elem) {
  if ((context[gin129] || context) !== document) {
    setDocument(context);
  }
  return contains(context, elem);
};
  Sizzle[gin220] = function(elem, name) {
  if ((elem[gin129] || elem) !== document) {
    setDocument(elem);
  }
  var fn = Expr[gin150][name[gin56]()], val = fn && hasOwn[gin7](Expr[gin150], name[gin56]()) ? fn(elem, name, !documentIsHTML) : undefined;
  return val !== undefined ? val : support[gin168] || !documentIsHTML ? elem[gin138](name) : (val = elem[gin175](name)) && val[gin221] ? val.value : null;
};
  Sizzle[gin222] = function(sel) {
  return (sel + gin38)[gin37](rcssescape, fcssescape);
};
  Sizzle[gin223] = function(msg) {
  throw new Error(gin224 + msg);
};
  Sizzle[gin225] = function(results) {
  var elem, duplicates = [], j = 0, i = 0;
  hasDuplicate = !support.detectDuplicates;
  sortInput = !support.sortStable && results[gin120](0);
  results[gin28](sortOrder);
  if (hasDuplicate) {
    while ((elem = results[i++])) {
      if (elem === results[i]) {
        j = duplicates[gin46](i);
      }
    }
    while (j--) {
      results[gin29](duplicates[j], 1);
    }
  }
  sortInput = null;
  return results;
};
  getText = Sizzle[gin226] = function(elem) {
  var node, ret = "", i = 0, nodeType = elem.nodeType;
  if (!nodeType) {
    while ((node = elem[i++])) {
      ret += getText(node);
    }
  } else if (nodeType === 1 || nodeType === 9 || nodeType === 11) {
    if (typeof elem.textContent === gin43) {
      return elem.textContent;
    } else {
      for (elem = elem[gin227]; elem; elem = elem[gin153]) {
        ret += getText(elem);
      }
    }
  } else if (nodeType === 3 || nodeType === 4) {
    return elem.nodeValue;
  }
  return ret;
};
  Expr = Sizzle[gin228] = {
  cacheLength: 50, 
  createPseudo: markFunction, 
  match: matchExpr, 
  attrHandle: {}, 
  find: {}, 
  relative: {
  gin229: {
  dir: gin13, 
  first: true}, 
  gin53: {
  dir: gin13}, 
  gin77: {
  dir: gin230, 
  first: true}, 
  gin231: {
  dir: gin230}}, 
  preFilter: {
  gin96: function(match) {
  match[1] = match[1][gin37](runescape, funescape);
  match[3] = (match[3] || match[4] || match[5] || gin38)[gin37](runescape, funescape);
  if (match[2] === gin190) {
    match[3] = gin53 + match[3] + gin53;
  }
  return match[gin120](0, 4);
}, 
  gin98: function(match) {
  match[1] = match[1][gin56]();
  if (match[1][gin120](0, 3) === gin232) {
    if (!match[3]) {
      Sizzle[gin223](match[0]);
    }
    match[4] = +(match[4] ? match[5] + (match[6] || 1) : 2 * (match[3] === gin233 || match[3] === gin234));
    match[5] = +((match[7] + match[8]) || match[3] === gin234);
  } else if (match[3]) {
    Sizzle[gin223](match[0]);
  }
  return match;
}, 
  gin97: function(match) {
  var excess, unquoted = !match[6] && match[2];
  if (matchExpr[gin98][gin136](match[0])) {
    return null;
  }
  if (match[3]) {
    match[2] = match[4] || match[5] || gin38;
  } else if (unquoted && rpseudo[gin136](unquoted) && (excess = tokenize(unquoted, true)) && (excess = unquoted[gin235](gin85, unquoted[gin19] - excess) - unquoted[gin19])) {
    match[0] = match[0][gin120](0, excess);
    match[2] = unquoted[gin120](0, excess);
  }
  return match[gin120](0, 3);
}}, 
  filter: {
  gin93: function(nodeNameSelector) {
  var nodeName = nodeNameSelector[gin37](runescape, funescape)[gin56]();
  return nodeNameSelector === gin83 ? function() {
  return true;
} : function(elem) {
  return elem[gin137] && elem[gin137][gin56]() === nodeName;
};
}, 
  gin91: function(className) {
  var pattern = classCache[className + gin53];
  return pattern || (pattern = new RegExp(gin236 + whitespace + gin85 + className + gin237 + whitespace + gin238)) && classCache(className, function(elem) {
  return pattern[gin136](typeof elem.className === gin43 && elem[gin169] || typeof elem.getAttribute !== gin6 && elem[gin138](gin239) || gin38);
});
}, 
  gin96: function(name, operator, check) {
  return function(elem) {
  var result = Sizzle[gin220](elem, name);
  if (result == null) {
    return operator === gin214;
  }
  if (!operator) {
    return true;
  }
  result += gin38;
  return operator === gin86 ? result === check : operator === gin214 ? result !== check : operator === gin240 ? check && result[gin235](check) === 0 : operator === gin241 ? check && result[gin235](check) > -1 : operator === gin242 ? check && result[gin120](-check.length) === check : operator === gin190 ? (gin53 + result[gin37](rwhitespace, gin53) + gin53)[gin235](check) > -1 : operator === gin243 ? result === check || result[gin120](0, check[gin19] + 1) === check + gin244 : false;
};
}, 
  gin98: function(type, what, argument, first, last) {
  var simple = type[gin120](0, 3) !== gin232, forward = type[gin120](-4) !== gin245, ofType = what === gin246;
  return first === 1 && last === 0 ? function(elem) {
  return !!elem.parentNode;
} : function(elem, context, xml) {
  var cache, uniqueCache, outerCache, node, nodeIndex, start, dir = simple !== forward ? "nextSibling" : "previousSibling", parent = elem.parentNode, name = ofType && elem[gin137][gin56](), useCache = !xml && !ofType, diff = false;
  if (parent) {
    if (simple) {
      while (dir) {
        node = elem;
        while ((node = node[dir])) {
          if (ofType ? node[gin137][gin56]() === name : node[gin151] === 1) {
            return false;
          }
        }
        start = dir = type === gin247 && !start && gin153;
      }
      return true;
    }
    start = [forward ? parent.firstChild : parent.lastChild];
    if (forward && useCache) {
      node = parent;
      outerCache = node[expando] || (node[expando] = {});
      uniqueCache = outerCache[node[gin248]] || (outerCache[node[gin248]] = {});
      cache = uniqueCache[type] || [];
      nodeIndex = cache[0] === dirruns && cache[1];
      diff = nodeIndex && cache[2];
      node = nodeIndex && parent[gin128][nodeIndex];
      while ((node = ++nodeIndex && node && node[dir] || (diff = nodeIndex = 0) || start[gin249]())) {
        if (node[gin151] === 1 && ++diff && node === elem) {
          uniqueCache[type] = [dirruns, nodeIndex, diff];
          break;
        }
      }
    } else {
      if (useCache) {
        node = elem;
        outerCache = node[expando] || (node[expando] = {});
        uniqueCache = outerCache[node[gin248]] || (outerCache[node[gin248]] = {});
        cache = uniqueCache[type] || [];
        nodeIndex = cache[0] === dirruns && cache[1];
        diff = nodeIndex;
      }
      if (diff === false) {
        while ((node = ++nodeIndex && node && node[dir] || (diff = nodeIndex = 0) || start[gin249]())) {
          if ((ofType ? node[gin137][gin56]() === name : node[gin151] === 1) && ++diff) {
            if (useCache) {
              outerCache = node[expando] || (node[expando] = {});
              uniqueCache = outerCache[node[gin248]] || (outerCache[node[gin248]] = {});
              uniqueCache[type] = [dirruns, diff];
            }
            if (node === elem) {
              break;
            }
          }
        }
      }
    }
    diff -= last;
    return diff === first || (diff % first === 0 && diff / first >= 0);
  }
};
}, 
  gin97: function(pseudo, argument) {
  var args, fn = Expr[gin250][pseudo] || Expr[gin251][pseudo[gin56]()] || Sizzle[gin223](gin252 + pseudo);
  if (fn[expando]) {
    return fn(argument);
  }
  if (fn[gin19] > 1) {
    args = [pseudo, pseudo, "", argument];
    return Expr[gin251][gin253](pseudo[gin56]()) ? markFunction(function(seed, matches) {
  var idx, matched = fn(seed, argument), i = matched.length;
  while (i--) {
    idx = indexOf(seed, matched[i]);
    seed[idx] = !(matches[idx] = matched[i]);
  }
}) : function(elem) {
  return fn(elem, 0, args);
};
  }
  return fn;
}}, 
  pseudos: {
  gin254: markFunction(function(selector) {
  var input = [], results = [], matcher = compile(selector[gin37](rtrim, gin145));
  return matcher[expando] ? markFunction(function(seed, matches, context, xml) {
  var elem, unmatched = matcher(seed, null, xml, []), i = seed.length;
  while (i--) {
    if ((elem = unmatched[i])) {
      seed[i] = !(matches[i] = elem);
    }
  }
}) : function(elem, context, xml) {
  input[0] = elem;
  matcher(input, null, xml, results);
  input[0] = null;
  return !results[gin249]();
};
}), 
  gin255: markFunction(function(selector) {
  return function(elem) {
  return Sizzle(selector, elem)[gin19] > 0;
};
}), 
  gin216: markFunction(function(text) {
  text = text[gin37](runescape, funescape);
  return function(elem) {
  return (elem[gin256] || elem[gin257] || getText(elem))[gin235](text) > -1;
};
}), 
  gin258: markFunction(function(lang) {
  if (!ridentifier[gin136](lang || gin38)) {
    Sizzle[gin223](gin259 + lang);
  }
  lang = lang[gin37](runescape, funescape)[gin56]();
  return function(elem) {
  var elemLang;
  do {
    if ((elemLang = documentIsHTML ? elem.lang : elem[gin138](gin260) || elem[gin138](gin258))) {
      elemLang = elemLang[gin56]();
      return elemLang === lang || elemLang[gin235](lang + gin244) === 0;
    }
  } while ((elem = elem[gin13]) && elem[gin151] === 1);
  return false;
};
}), 
  gin261: function(elem) {
  var hash = gin5[gin262] && gin5[gin262][gin263];
  return hash && hash[gin120](1) === elem[gin132];
}, 
  gin264: function(elem) {
  return elem === docElem;
}, 
  gin265: function(elem) {
  return elem === document[gin266] && (!document.hasFocus || document[gin267]()) && !!(elem[gin39] || elem[gin268] || ~elem.tabIndex);
}, 
  gin269: createDisabledPseudo(false), 
  gin124: createDisabledPseudo(true), 
  gin270: function(elem) {
  var nodeName = elem[gin137][gin56]();
  return (nodeName === gin154 && !!elem.checked) || (nodeName === gin271 && !!elem.selected);
}, 
  gin272: function(elem) {
  if (elem.parentNode) {
    elem[gin13].selectedIndex;
  }
  return elem[gin272] === true;
}, 
  gin273: function(elem) {
  for (elem = elem[gin227]; elem; elem = elem[gin153]) {
    if (elem[gin151] < 6) {
      return false;
    }
  }
  return true;
}, 
  gin274: function(elem) {
  return !Expr[gin250][gin273](elem);
}, 
  gin275: function(elem) {
  return rheader[gin136](elem[gin137]);
}, 
  gin154: function(elem) {
  return rinputs[gin136](elem[gin137]);
}, 
  gin155: function(elem) {
  var name = elem[gin137][gin56]();
  return name === gin154 && elem[gin39] === gin155 || name === gin155;
}, 
  gin10: function(elem) {
  var attr;
  return elem[gin137][gin56]() === gin154 && elem[gin39] === gin10 && ((attr = elem[gin138](gin39)) == null || attr[gin56]() === gin10);
}, 
  gin276: createPositionalPseudo(function() {
  return [0];
}), 
  gin245: createPositionalPseudo(function(matchIndexes, length) {
  return [length - 1];
}), 
  gin27: createPositionalPseudo(function(matchIndexes, length, argument) {
  return [argument < 0 ? argument + length : argument];
}), 
  gin233: createPositionalPseudo(function(matchIndexes, length) {
  var i = 0;
  for (; i < length; i += 2) {
    matchIndexes[gin46](i);
  }
  return matchIndexes;
}), 
  gin234: createPositionalPseudo(function(matchIndexes, length) {
  var i = 1;
  for (; i < length; i += 2) {
    matchIndexes[gin46](i);
  }
  return matchIndexes;
}), 
  gin277: createPositionalPseudo(function(matchIndexes, length, argument) {
  var i = argument < 0 ? argument + length : argument;
  for (; --i >= 0; ) {
    matchIndexes[gin46](i);
  }
  return matchIndexes;
}), 
  gin278: createPositionalPseudo(function(matchIndexes, length, argument) {
  var i = argument < 0 ? argument + length : argument;
  for (; ++i < length; ) {
    matchIndexes[gin46](i);
  }
  return matchIndexes;
})}};
  Expr[gin250][gin232] = Expr[gin250][gin27];
  for (i in {
  radio: true, 
  checkbox: true, 
  file: true, 
  password: true, 
  image: true}) {
    Expr[gin250][i] = createInputPseudo(i);
  }
  for (i in {
  submit: true, 
  reset: true}) {
    Expr[gin250][i] = createButtonPseudo(i);
  }
  function setFilters() {
  }
  setFilters[gin18] = Expr[gin279] = Expr[gin250];
  Expr[gin251] = new setFilters();
  tokenize = Sizzle[gin280] = function(selector, parseOnly) {
  var matched, match, tokens, type, soFar, groups, preFilters, cached = tokenCache[selector + gin53];
  if (cached) {
    return parseOnly ? 0 : cached[gin120](0);
  }
  soFar = selector;
  groups = [];
  preFilters = Expr[gin281];
  while (soFar) {
    if (!matched || (match = rcomma[gin130](soFar))) {
      if (match) {
        soFar = soFar[gin120](match[0][gin19]) || soFar;
      }
      groups[gin46]((tokens = []));
    }
    matched = false;
    if ((match = rcombinators[gin130](soFar))) {
      matched = match[gin147]();
      tokens[gin46]({
  value: matched, 
  type: match[0][gin37](rtrim, gin53)});
      soFar = soFar[gin120](matched[gin19]);
    }
    for (type in Expr.filter) {
      if ((match = matchExpr[type][gin130](soFar)) && (!preFilters[type] || (match = preFilters[type](match)))) {
        matched = match[gin147]();
        tokens[gin46]({
  value: matched, 
  type: type, 
  matches: match});
        soFar = soFar[gin120](matched[gin19]);
      }
    }
    if (!matched) {
      break;
    }
  }
  return parseOnly ? soFar.length : soFar ? Sizzle[gin223](selector) : tokenCache(selector, groups)[gin120](0);
};
  function toSelector(tokens) {
    var i = 0, len = tokens.length, selector = "";
    for (; i < len; i++) {
      selector += tokens[i][gin176];
    }
    return selector;
  }
  function addCombinator(matcher, combinator, base) {
    var dir = combinator.dir, skip = combinator.next, key = skip || dir, checkNonElements = base && key === gin13, doneName = done++;
    return combinator.first ? function(elem, context, xml) {
  while ((elem = elem[dir])) {
    if (elem[gin151] === 1 || checkNonElements) {
      return matcher(elem, context, xml);
    }
  }
  return false;
} : function(elem, context, xml) {
  var oldCache, uniqueCache, outerCache, newCache = [dirruns, doneName];
  if (xml) {
    while ((elem = elem[dir])) {
      if (elem[gin151] === 1 || checkNonElements) {
        if (matcher(elem, context, xml)) {
          return true;
        }
      }
    }
  } else {
    while ((elem = elem[dir])) {
      if (elem[gin151] === 1 || checkNonElements) {
        outerCache = elem[expando] || (elem[expando] = {});
        uniqueCache = outerCache[elem[gin248]] || (outerCache[elem[gin248]] = {});
        if (skip && skip === elem[gin137][gin56]()) {
          elem = elem[dir] || elem;
        } else if ((oldCache = uniqueCache[key]) && oldCache[0] === dirruns && oldCache[1] === doneName) {
          return (newCache[2] = oldCache[2]);
        } else {
          uniqueCache[key] = newCache;
          if ((newCache[2] = matcher(elem, context, xml))) {
            return true;
          }
        }
      }
    }
  }
  return false;
};
  }
  function elementMatcher(matchers) {
    return matchers[gin19] > 1 ? function(elem, context, xml) {
  var i = matchers.length;
  while (i--) {
    if (!matchers[i](elem, context, xml)) {
      return false;
    }
  }
  return true;
} : matchers[0];
  }
  function multipleContexts(selector, contexts, results) {
    var i = 0, len = contexts.length;
    for (; i < len; i++) {
      Sizzle(selector, contexts[i], results);
    }
    return results;
  }
  function condense(unmatched, map, filter, context, xml) {
    var elem, newUnmatched = [], i = 0, len = unmatched.length, mapped = map != null;
    for (; i < len; i++) {
      if ((elem = unmatched[i])) {
        if (!filter || filter(elem, context, xml)) {
          newUnmatched[gin46](elem);
          if (mapped) {
            map[gin46](i);
          }
        }
      }
    }
    return newUnmatched;
  }
  function setMatcher(preFilter, selector, matcher, postFilter, postFinder, postSelector) {
    if (postFilter && !postFilter[expando]) {
      postFilter = setMatcher(postFilter);
    }
    if (postFinder && !postFinder[expando]) {
      postFinder = setMatcher(postFinder, postSelector);
    }
    return markFunction(function(seed, results, context, xml) {
  var temp, i, elem, preMap = [], postMap = [], preexisting = results.length, elems = seed || multipleContexts(selector || gin83, context.nodeType ? [context] : context, []), matcherIn = preFilter && (seed || !selector) ? condense(elems, preMap, preFilter, context, xml) : elems, matcherOut = matcher ? postFinder || (seed ? preFilter : preexisting || postFilter) ? [] : results : matcherIn;
  if (matcher) {
    matcher(matcherIn, matcherOut, context, xml);
  }
  if (postFilter) {
    temp = condense(matcherOut, postMap);
    postFilter(temp, [], context, xml);
    i = temp[gin19];
    while (i--) {
      if ((elem = temp[i])) {
        matcherOut[postMap[i]] = !(matcherIn[postMap[i]] = elem);
      }
    }
  }
  if (seed) {
    if (postFinder || preFilter) {
      if (postFinder) {
        temp = [];
        i = matcherOut[gin19];
        while (i--) {
          if ((elem = matcherOut[i])) {
            temp[gin46]((matcherIn[i] = elem));
          }
        }
        postFinder(null, (matcherOut = []), temp, xml);
      }
      i = matcherOut[gin19];
      while (i--) {
        if ((elem = matcherOut[i]) && (temp = postFinder ? indexOf(seed, elem) : preMap[i]) > -1) {
          seed[temp] = !(results[temp] = elem);
        }
      }
    }
  } else {
    matcherOut = condense(matcherOut === results ? matcherOut[gin29](preexisting, matcherOut[gin19]) : matcherOut);
    if (postFinder) {
      postFinder(null, results, matcherOut, xml);
    } else {
      push[gin26](results, matcherOut);
    }
  }
});
  }
  function matcherFromTokens(tokens) {
    var checkContext, matcher, j, len = tokens.length, leadingRelative = Expr[gin282][tokens[0][gin39]], implicitRelative = leadingRelative || Expr[gin282][gin53], i = leadingRelative ? 1 : 0, matchContext = addCombinator(function(elem) {
  return elem === checkContext;
}, implicitRelative, true), matchAnyContext = addCombinator(function(elem) {
  return indexOf(checkContext, elem) > -1;
}, implicitRelative, true), matchers = [function(elem, context, xml) {
  var ret = (!leadingRelative && (xml || context !== outermostContext)) || ((checkContext = context).nodeType ? matchContext(elem, context, xml) : matchAnyContext(elem, context, xml));
  checkContext = null;
  return ret;
}];
    for (; i < len; i++) {
      if ((matcher = Expr[gin282][tokens[i][gin39]])) {
        matchers = [addCombinator(elementMatcher(matchers), matcher)];
      } else {
        matcher = Expr[gin173][tokens[i][gin39]][gin26](null, tokens[i][gin207]);
        if (matcher[expando]) {
          j = ++i;
          for (; j < len; j++) {
            if (Expr[gin282][tokens[j][gin39]]) {
              break;
            }
          }
          return setMatcher(i > 1 && elementMatcher(matchers), i > 1 && toSelector(tokens[gin120](0, i - 1)[gin47]({
  value: tokens[i - 2][gin39] === gin53 ? "*" : ""}))[gin37](rtrim, gin145), matcher, i < j && matcherFromTokens(tokens[gin120](i, j)), j < len && matcherFromTokens((tokens = tokens[gin120](j))), j < len && toSelector(tokens));
        }
        matchers[gin46](matcher);
      }
    }
    return elementMatcher(matchers);
  }
  function matcherFromGroupMatchers(elementMatchers, setMatchers) {
    var bySet = setMatchers[gin19] > 0, byElement = elementMatchers[gin19] > 0, superMatcher = function(seed, context, xml, results, outermost) {
  var elem, j, matcher, matchedCount = 0, i = "0", unmatched = seed && [], setMatched = [], contextBackup = outermostContext, elems = seed || byElement && Expr[gin174][gin93](gin83, outermost), dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math[gin36]() || 0.1), len = elems.length;
  if (outermost) {
    outermostContext = context === document || context || outermost;
  }
  for (; i !== len && (elem = elems[i]) != null; i++) {
    if (byElement && elem) {
      j = 0;
      if (!context && elem[gin129] !== document) {
        setDocument(elem);
        xml = !documentIsHTML;
      }
      while ((matcher = elementMatchers[j++])) {
        if (matcher(elem, context || document, xml)) {
          results[gin46](elem);
          break;
        }
      }
      if (outermost) {
        dirruns = dirrunsUnique;
      }
    }
    if (bySet) {
      if ((elem = !matcher && elem)) {
        matchedCount--;
      }
      if (seed) {
        unmatched[gin46](elem);
      }
    }
  }
  matchedCount += i;
  if (bySet && i !== matchedCount) {
    j = 0;
    while ((matcher = setMatchers[j++])) {
      matcher(unmatched, setMatched, context, xml);
    }
    if (seed) {
      if (matchedCount > 0) {
        while (i--) {
          if (!(unmatched[i] || setMatched[i])) {
            setMatched[i] = pop[gin7](results);
          }
        }
      }
      setMatched = condense(setMatched);
    }
    push[gin26](results, setMatched);
    if (outermost && !seed && setMatched[gin19] > 0 && (matchedCount + setMatchers[gin19]) > 1) {
      Sizzle[gin225](results);
    }
  }
  if (outermost) {
    dirruns = dirrunsUnique;
    outermostContext = contextBackup;
  }
  return unmatched;
};
    return bySet ? markFunction(superMatcher) : superMatcher;
  }
  compile = Sizzle[gin284] = function(selector, match) {
  var i, setMatchers = [], elementMatchers = [], cached = compilerCache[selector + gin53];
  if (!cached) {
    if (!match) {
      match = tokenize(selector);
    }
    i = match[gin19];
    while (i--) {
      cached = matcherFromTokens(match[i]);
      if (cached[expando]) {
        setMatchers[gin46](cached);
      } else {
        elementMatchers[gin46](cached);
      }
    }
    cached = compilerCache(selector, matcherFromGroupMatchers(elementMatchers, setMatchers));
    cached[gin285] = selector;
  }
  return cached;
};
  select = Sizzle[gin286] = function(selector, context, results, seed) {
  var i, tokens, token, type, find, compiled = typeof selector === gin40 && selector, match = !seed && tokenize((selector = compiled[gin285] || selector));
  results = results || [];
  if (match[gin19] === 1) {
    tokens = match[0] = match[0][gin120](0);
    if (tokens[gin19] > 2 && (token = tokens[0])[gin39] === gin89 && context[gin151] === 9 && documentIsHTML && Expr[gin282][tokens[1][gin39]]) {
      context = (Expr[gin174][gin89](token[gin207][0][gin37](runescape, funescape), context) || [])[0];
      if (!context) {
        return results;
      } else if (compiled) {
        context = context[gin13];
      }
      selector = selector[gin120](tokens[gin147]()[gin176][gin19]);
    }
    i = matchExpr[gin108][gin136](selector) ? 0 : tokens.length;
    while (i--) {
      token = tokens[i];
      if (Expr[gin282][(type = token[gin39])]) {
        break;
      }
      if ((find = Expr[gin174][type])) {
        if ((seed = find(token[gin207][0][gin37](runescape, funescape), rsibling[gin136](tokens[0][gin39]) && testContext(context[gin13]) || context))) {
          tokens[gin29](i, 1);
          selector = seed[gin19] && toSelector(tokens);
          if (!selector) {
            push[gin26](results, seed);
            return results;
          }
          break;
        }
      }
    }
  }
  (compiled || compile(selector, match))(seed, context, !documentIsHTML, results, !context || rsibling[gin136](selector) && testContext(context[gin13]) || context);
  return results;
};
  support[gin287] = expando[gin52](gin38)[gin28](sortOrder)[gin141](gin38) === expando;
  support[gin288] = !!hasDuplicate;
  setDocument();
  support[gin289] = assert(function(el) {
  return el[gin215](document[gin8](gin148)) & 1;
});
  if (!assert(function(el) {
  el[gin177] = gin290;
  return el[gin227][gin138](gin268) === gin140;
})) {
    addHandle(gin291, function(elem, name, isXML) {
  if (!isXML) {
    return elem[gin138](name, name[gin56]() === gin39 ? 1 : 2);
  }
});
  }
  if (!support.attributes || !assert(function(el) {
  el[gin177] = gin292;
  el[gin227][gin139](gin176, gin38);
  return el[gin227][gin138](gin176) === gin38;
})) {
    addHandle(gin176, function(elem, name, isXML) {
  if (!isXML && elem[gin137][gin56]() === gin154) {
    return elem.defaultValue;
  }
});
  }
  if (!assert(function(el) {
  return el[gin138](gin124) == null;
})) {
    addHandle(booleans, function(elem, name, isXML) {
  var val;
  if (!isXML) {
    return elem[name] === true ? name[gin56]() : (val = elem[gin175](name)) && val[gin221] ? val.value : null;
  }
});
  }
  return Sizzle;
})(gin5);
  jQuery[gin174] = Sizzle;
  jQuery[gin293] = Sizzle[gin228];
  jQuery[gin293][gin294] = jQuery[gin293][gin250];
  jQuery[gin225] = jQuery[gin295] = Sizzle[gin225];
  jQuery[gin10] = Sizzle[gin226];
  jQuery[gin296] = Sizzle[gin158];
  jQuery[gin216] = Sizzle[gin216];
  jQuery[gin297] = Sizzle[gin222];
  var dir = function(elem, dir, until) {
  var matched = [], truncate = until !== undefined;
  while ((elem = elem[dir]) && elem[gin151] !== 9) {
    if (elem[gin151] === 1) {
      if (truncate && jQuery(elem)[gin298](until)) {
        break;
      }
      matched[gin46](elem);
    }
  }
  return matched;
};
  var siblings = function(n, elem) {
  var matched = [];
  for (; n; n = n[gin153]) {
    if (n[gin151] === 1 && n !== elem) {
      matched[gin46](n);
    }
  }
  return matched;
};
  var rneedsContext = jQuery[gin293][gin299].needsContext;
  function nodeName(elem, name) {
    return elem[gin137] && elem[gin137][gin56]() === name[gin56]();
  }
  ;
  var rsingleTag = (/^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i);
  var risSimple = /^.[^:#\[\.,]*$/;
  function winnow(elements, qualifier, not) {
    if (jQuery[gin32](qualifier)) {
      return jQuery[gin300](elements, function(elem, i) {
  return !!qualifier[gin7](elem, i, elem) !== not;
});
    }
    if (qualifier.nodeType) {
      return jQuery[gin300](elements, function(elem) {
  return (elem === qualifier) !== not;
});
    }
    if (typeof qualifier !== gin43) {
      return jQuery[gin300](elements, function(elem) {
  return (indexOf[gin7](qualifier, elem) > -1) !== not;
});
    }
    if (risSimple[gin136](qualifier)) {
      return jQuery[gin173](qualifier, elements, not);
    }
    qualifier = jQuery[gin173](qualifier, elements);
    return jQuery[gin300](elements, function(elem) {
  return (indexOf[gin7](qualifier, elem) > -1) !== not && elem[gin151] === 1;
});
  }
  jQuery[gin173] = function(expr, elems, not) {
  var elem = elems[0];
  if (not) {
    expr = gin301 + expr + gin85;
  }
  if (elems[gin19] === 1 && elem[gin151] === 1) {
    return jQuery[gin174][gin206](elem, expr) ? [elem] : [];
  }
  return jQuery[gin174][gin207](expr, jQuery[gin300](elems, function(elem) {
  return elem[gin151] === 1;
}));
};
  jQuery[gin16][gin30]({
  find: function(selector) {
  var i, ret, len = this.length, self = this;
  if (typeof selector !== gin43) {
    return this[gin24](jQuery(selector)[gin173](function() {
  for (i = 0; i < len; i++) {
    if (jQuery[gin216](self[i], this)) {
      return true;
    }
  }
}));
  }
  ret = this[gin24]([]);
  for (i = 0; i < len; i++) {
    jQuery[gin174](selector, self[i], ret);
  }
  return len > 1 ? jQuery[gin225](ret) : ret;
}, 
  filter: function(selector) {
  return this[gin24](winnow(this, selector || [], false));
}, 
  not: function(selector) {
  return this[gin24](winnow(this, selector || [], true));
}, 
  is: function(selector) {
  return !!winnow(this, typeof selector === gin43 && rneedsContext[gin136](selector) ? jQuery(selector) : selector || [], false).length;
}});
  var rootjQuery, rquickExpr = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/, init = jQuery[gin16][gin302] = function(selector, context, root) {
  var match, elem;
  if (!selector) {
    return this;
  }
  root = root || rootjQuery;
  if (typeof selector === gin43) {
    if (selector[0] === gin303 && selector[selector[gin19] - 1] === gin229 && selector[gin19] >= 3) {
      match = [null, selector, null];
    } else {
      match = rquickExpr[gin130](selector);
    }
    if (match && (match[1] || !context)) {
      if (match[1]) {
        context = context instanceof jQuery ? context[0] : context;
        jQuery[gin20](this, jQuery[gin304](match[1], context && context[gin151] ? context[gin129] || context : document, true));
        if (rsingleTag[gin136](match[1]) && jQuery[gin33](context)) {
          for (match in context) {
            if (jQuery[gin32](this[match])) {
              this[match](context[match]);
            } else {
              this[gin220](match, context[match]);
            }
          }
        }
        return this;
      } else {
        elem = document[gin131](match[2]);
        if (elem) {
          this[0] = elem;
          this[gin19] = 1;
        }
        return this;
      }
    } else if (!context || context[gin305]) {
      return (context || root)[gin174](selector);
    } else {
      return this[gin21](context)[gin174](selector);
    }
  } else if (selector.nodeType) {
    this[0] = selector;
    this[gin19] = 1;
    return this;
  } else if (jQuery[gin32](selector)) {
    return root[gin306] !== undefined ? root[gin306](selector) : selector(jQuery);
  }
  return jQuery[gin307](selector, this);
};
  init[gin18] = jQuery[gin16];
  rootjQuery = jQuery(document);
  var rparentsprev = /^(?:parents|prev(?:Until|All))/, guaranteedUnique = {
  children: true, 
  contents: true, 
  next: true, 
  prev: true};
  jQuery[gin16][gin30]({
  has: function(target) {
  var targets = jQuery(target, this), l = targets.length;
  return this[gin173](function() {
  var i = 0;
  for (; i < l; i++) {
    if (jQuery[gin216](this, targets[i])) {
      return true;
    }
  }
});
}, 
  closest: function(selectors, context) {
  var cur, i = 0, l = this.length, matched = [], targets = typeof selectors !== gin43 && jQuery(selectors);
  if (!rneedsContext[gin136](selectors)) {
    for (; i < l; i++) {
      for (cur = this[i]; cur && cur !== context; cur = cur[gin13]) {
        if (cur[gin151] < 11 && (targets ? targets[gin308](cur) > -1 : cur[gin151] === 1 && jQuery[gin174][gin206](cur, selectors))) {
          matched[gin46](cur);
          break;
        }
      }
    }
  }
  return this[gin24](matched[gin19] > 1 ? jQuery[gin225](matched) : matched);
}, 
  index: function(elem) {
  if (!elem) {
    return (this[0] && this[0][gin13]) ? this[gin276]()[gin309]().length : -1;
  }
  if (typeof elem === gin43) {
    return indexOf[gin7](jQuery(elem), this[0]);
  }
  return indexOf[gin7](this, elem.jquery ? elem[0] : elem);
}, 
  add: function(selector, context) {
  return this[gin24](jQuery[gin225](jQuery[gin20](this[gin310](), jQuery(selector, context))));
}, 
  addBack: function(selector) {
  return this[gin311](selector == null ? this.prevObject : this[gin22][gin173](selector));
}});
  function sibling(cur, dir) {
    while ((cur = cur[dir]) && cur[gin151] !== 1) {
    }
    return cur;
  }
  jQuery[gin23]({
  parent: function(elem) {
  var parent = elem.parentNode;
  return parent && parent[gin151] !== 11 ? parent : null;
}, 
  parents: function(elem) {
  return dir(elem, gin13);
}, 
  parentsUntil: function(elem, i, until) {
  return dir(elem, gin13, until);
}, 
  next: function(elem) {
  return sibling(elem, gin153);
}, 
  prev: function(elem) {
  return sibling(elem, gin230);
}, 
  nextAll: function(elem) {
  return dir(elem, gin153);
}, 
  prevAll: function(elem) {
  return dir(elem, gin230);
}, 
  nextUntil: function(elem, i, until) {
  return dir(elem, gin153, until);
}, 
  prevUntil: function(elem, i, until) {
  return dir(elem, gin230, until);
}, 
  siblings: function(elem) {
  return siblings((elem[gin13] || {})[gin227], elem);
}, 
  children: function(elem) {
  return siblings(elem[gin227]);
}, 
  contents: function(elem) {
  if (nodeName(elem, gin312)) {
    return elem.contentDocument;
  }
  if (nodeName(elem, gin313)) {
    elem = elem[gin314] || elem;
  }
  return jQuery[gin20]([], elem[gin128]);
}}, function(name, fn) {
  jQuery[gin16][name] = function(until, selector) {
  var matched = jQuery[gin25](this, fn, until);
  if (name[gin120](-5) !== gin315) {
    selector = until;
  }
  if (selector && typeof selector === gin43) {
    matched = jQuery[gin173](selector, matched);
  }
  if (this[gin19] > 1) {
    if (!guaranteedUnique[name]) {
      jQuery[gin225](matched);
    }
    if (rparentsprev[gin136](name)) {
      matched[gin316]();
    }
  }
  return this[gin24](matched);
};
});
  var rnothtmlwhite = (/[^\x20\t\r\n\f]+/g);
  function createOptions(options) {
    var object = {};
    jQuery[gin23](options[gin299](rnothtmlwhite) || [], function(_, flag) {
  object[flag] = true;
});
    return object;
  }
  jQuery[gin317] = function(options) {
  options = typeof options === gin43 ? createOptions(options) : jQuery[gin30]({}, options);
  var firing, memory, fired, locked, list = [], queue = [], firingIndex = -1, fire = function() {
  locked = locked || options[gin318];
  fired = firing = true;
  for (; queue.length; firingIndex = -1) {
    memory = queue[gin147]();
    while (++firingIndex < list[gin19]) {
      if (list[firingIndex][gin26](memory[0], memory[1]) === false && options[gin319]) {
        firingIndex = list[gin19];
        memory = false;
      }
    }
  }
  if (!options.memory) {
    memory = false;
  }
  firing = false;
  if (locked) {
    if (memory) {
      list = [];
    } else {
      list = gin38;
    }
  }
}, self = {
  add: function() {
  if (list) {
    if (memory && !firing) {
      firingIndex = list[gin19] - 1;
      queue[gin46](memory);
    }
    (function add(args) {
  jQuery[gin23](args, function(_, arg) {
  if (jQuery[gin32](arg)) {
    if (!options.unique || !self[gin255](arg)) {
      list[gin46](arg);
    }
  } else if (arg && arg[gin19] && jQuery[gin39](arg) !== gin43) {
    add(arg);
  }
});
})(arguments);
    if (memory && !firing) {
      fire();
    }
  }
  return this;
}, 
  remove: function() {
  jQuery[gin23](arguments, function(_, arg) {
  var index;
  while ((index = jQuery[gin320](arg, list, index)) > -1) {
    list[gin29](index, 1);
    if (index <= firingIndex) {
      firingIndex--;
    }
  }
});
  return this;
}, 
  has: function(fn) {
  return fn ? jQuery[gin320](fn, list) > -1 : list[gin19] > 0;
}, 
  empty: function() {
  if (list) {
    list = [];
  }
  return this;
}, 
  disable: function() {
  locked = queue = [];
  list = memory = gin38;
  return this;
}, 
  disabled: function() {
  return !list;
}, 
  lock: function() {
  locked = queue = [];
  if (!memory && !firing) {
    list = memory = gin38;
  }
  return this;
}, 
  locked: function() {
  return !!locked;
}, 
  fireWith: function(context, args) {
  if (!locked) {
    args = args || [];
    args = [context, args.slice ? args[gin120]() : args];
    queue[gin46](args);
    if (!firing) {
      fire();
    }
  }
  return this;
}, 
  fire: function() {
  self[gin321](this, arguments);
  return this;
}, 
  fired: function() {
  return !!fired;
}};
  return self;
};
  function Identity(v) {
    return v;
  }
  function Thrower(ex) {
    throw ex;
  }
  function adoptValue(value, resolve, reject, noValue) {
    var method;
    try {
      if (value && jQuery[gin32]((method = value[gin322]))) {
        method[gin7](value)[gin323](resolve)[gin324](reject);
      } else if (value && jQuery[gin32]((method = value[gin325]))) {
        method[gin7](value, resolve, reject);
      } else {
        resolve[gin26](undefined, [value][gin120](noValue));
      }
    }    catch (value) {
  reject[gin26](undefined, [value]);
}
  }
  jQuery[gin30]({
  Deferred: function(func) {
  var tuples = [["notify", "progress", jQuery[gin317](gin328), jQuery[gin317](gin328), 2], ["resolve", "done", jQuery[gin317](gin330), jQuery[gin317](gin330), 0, "resolved"], ["reject", "fail", jQuery[gin317](gin330), jQuery[gin317](gin330), 1, "rejected"]], state = "pending", promise = {
  state: function() {
  return state;
}, 
  always: function() {
  deferred[gin323](arguments)[gin324](arguments);
  return this;
}, 
  gin335: function(fn) {
  return promise[gin325](null, fn);
}, 
  pipe: function() {
  var fns = arguments;
  return jQuery[gin336](function(newDefer) {
  jQuery[gin23](tuples, function(i, tuple) {
  var fn = jQuery[gin32](fns[tuple[4]]) && fns[tuple[4]];
  deferred[tuple[1]](function() {
  var returned = fn && fn[gin26](this, arguments);
  if (returned && jQuery[gin32](returned[gin322])) {
    returned[gin322]()[gin327](newDefer[gin326])[gin323](newDefer[gin329])[gin324](newDefer[gin332]);
  } else {
    newDefer[tuple[0] + gin337](this, fn ? [returned] : arguments);
  }
});
});
  fns = null;
})[gin322]();
}, 
  then: function(onFulfilled, onRejected, onProgress) {
  var maxDepth = 0;
  function resolve(depth, deferred, handler, special) {
    return function() {
  var that = this, args = arguments, mightThrow = function() {
  var returned, then;
  if (depth < maxDepth) {
    return;
  }
  returned = handler[gin26](that, args);
  if (returned === deferred[gin322]()) {
    throw new TypeError("Thenable self-resolution");
  }
  then = returned && (typeof returned === gin2 || typeof returned === gin40) && returned[gin325];
  if (jQuery[gin32](then)) {
    if (special) {
      then[gin7](returned, resolve(maxDepth, deferred, Identity, special), resolve(maxDepth, deferred, Thrower, special));
    } else {
      maxDepth++;
      then[gin7](returned, resolve(maxDepth, deferred, Identity, special), resolve(maxDepth, deferred, Thrower, special), resolve(maxDepth, deferred, Identity, deferred[gin339]));
    }
  } else {
    if (handler !== Identity) {
      that = undefined;
      args = [returned];
    }
    (special || deferred[gin340])(that, args);
  }
}, process = special ? mightThrow : function() {
  try {
    mightThrow();
  }  catch (e) {
  if (jQuery[gin336].exceptionHook) {
    jQuery[gin336][gin341](e, process[gin342]);
  }
  if (depth + 1 >= maxDepth) {
    if (handler !== Thrower) {
      that = undefined;
      args = [e];
    }
    deferred[gin343](that, args);
  }
}
};
  if (depth) {
    process();
  } else {
    if (jQuery[gin336].getStackHook) {
      process[gin342] = jQuery[gin336][gin344]();
    }
    gin5[gin345](process);
  }
};
  }
  return jQuery[gin336](function(newDefer) {
  tuples[0][3][gin311](resolve(0, newDefer, jQuery[gin32](onProgress) ? onProgress : Identity, newDefer[gin339]));
  tuples[1][3][gin311](resolve(0, newDefer, jQuery[gin32](onFulfilled) ? onFulfilled : Identity));
  tuples[2][3][gin311](resolve(0, newDefer, jQuery[gin32](onRejected) ? onRejected : Thrower));
})[gin322]();
}, 
  promise: function(obj) {
  return obj != null ? jQuery[gin30](obj, promise) : promise;
}}, deferred = {};
  jQuery[gin23](tuples, function(i, tuple) {
  var list = tuple[2], stateString = tuple[5];
  promise[tuple[1]] = list[gin311];
  if (stateString) {
    list[gin311](function() {
  state = stateString;
}, tuples[3 - i][2][gin346], tuples[0][2][gin347]);
  }
  list[gin311](tuple[3][gin348]);
  deferred[tuple[0]] = function() {
  deferred[tuple[0] + gin337](this === deferred ? undefined : this, arguments);
  return this;
};
  deferred[tuple[0] + gin337] = list[gin321];
});
  promise[gin322](deferred);
  if (func) {
    func[gin7](deferred, deferred);
  }
  return deferred;
}, 
  when: function(singleValue) {
  var remaining = arguments.length, i = remaining, resolveContexts = Array(i), resolveValues = slice[gin7](arguments), master = jQuery[gin336](), updateFunc = function(i) {
  return function(value) {
  resolveContexts[i] = this;
  resolveValues[i] = arguments[gin19] > 1 ? slice[gin7](arguments) : value;
  if (!(--remaining)) {
    master[gin340](resolveContexts, resolveValues);
  }
};
};
  if (remaining <= 1) {
    adoptValue(singleValue, master[gin323](updateFunc(i))[gin329], master[gin332], !remaining);
    if (master[gin349]() === gin334 || jQuery[gin32](resolveValues[i] && resolveValues[i][gin325])) {
      return master[gin325]();
    }
  }
  while (i--) {
    adoptValue(resolveValues[i], updateFunc(i), master[gin332]);
  }
  return master[gin322]();
}});
  var rerrorNames = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
  jQuery[gin336][gin341] = function(error, stack) {
  if (gin5[gin350] && gin5[gin350][gin351] && error && rerrorNames[gin136](error[gin198])) {
    gin5[gin350][gin351](gin352 + error[gin353], error[gin354], stack);
  }
};
  jQuery[gin355] = function(error) {
  gin5[gin345](function() {
  throw error;
});
};
  var readyList = jQuery[gin336]();
  jQuery[gin16][gin306] = function(fn) {
  readyList[gin325](fn)[gin335](function(error) {
  jQuery[gin355](error);
});
  return this;
};
  jQuery[gin30]({
  isReady: false, 
  readyWait: 1, 
  ready: function(wait) {
  if (wait === true ? --jQuery.readyWait : jQuery.isReady) {
    return;
  }
  jQuery[gin356] = true;
  if (wait !== true && --jQuery.readyWait > 0) {
    return;
  }
  readyList[gin340](document, [jQuery]);
}});
  jQuery[gin306][gin325] = readyList[gin325];
  function completed() {
    document[gin357](gin358, completed);
    gin5[gin357](gin359, completed);
    jQuery[gin306]();
  }
  if (document[gin360] === gin361 || (document[gin360] !== gin362 && !document[gin159].doScroll)) {
    gin5[gin345](jQuery[gin306]);
  } else {
    document[gin164](gin358, completed);
    gin5[gin164](gin359, completed);
  }
  var access = function(elems, fn, key, value, chainable, emptyGet, raw) {
  var i = 0, len = elems.length, bulk = key == null;
  if (jQuery[gin39](key) === gin2) {
    chainable = true;
    for (i in key) {
      access(elems, fn, i, key[i], true, emptyGet, raw);
    }
  } else if (value !== undefined) {
    chainable = true;
    if (!jQuery[gin32](value)) {
      raw = true;
    }
    if (bulk) {
      if (raw) {
        fn[gin7](elems, value);
        fn = null;
      } else {
        bulk = fn;
        fn = function(elem, key, value) {
  return bulk[gin7](jQuery(elem), value);
};
      }
    }
    if (fn) {
      for (; i < len; i++) {
        fn(elems[i], key, raw ? value : value[gin7](elems[i], i, fn(elems[i], key)));
      }
    }
  }
  if (chainable) {
    return elems;
  }
  if (bulk) {
    return fn[gin7](elems);
  }
  return len ? fn(elems[0], key) : emptyGet;
};
  var acceptData = function(owner) {
  return owner[gin151] === 1 || owner[gin151] === 9 || !(+owner.nodeType);
};
  function Data() {
    this[gin363] = jQuery[gin363] + Data.uid++;
  }
  Data[gin364] = 1;
  Data[gin18] = {
  cache: function(owner) {
  var value = owner[this[gin363]];
  if (!value) {
    value = {};
    if (acceptData(owner)) {
      if (owner.nodeType) {
        owner[this[gin363]] = value;
      } else {
        Object[gin365](owner, this[gin363], {
  value: value, 
  configurable: true});
      }
    }
  }
  return value;
}, 
  set: function(owner, data, value) {
  var prop, cache = this[gin366](owner);
  if (typeof data === gin43) {
    cache[jQuery[gin367](data)] = value;
  } else {
    for (prop in data) {
      cache[jQuery[gin367](prop)] = data[prop];
    }
  }
  return cache;
}, 
  get: function(owner, key) {
  return key === undefined ? this[gin366](owner) : owner[this[gin363]] && owner[this[gin363]][jQuery[gin367](key)];
}, 
  access: function(owner, key, value) {
  if (key === undefined || ((key && typeof key === gin43) && value === undefined)) {
    return this[gin310](owner, key);
  }
  this[gin368](owner, key, value);
  return value !== undefined ? value : key;
}, 
  remove: function(owner, key) {
  var i, cache = owner[this[gin363]];
  if (cache === undefined) {
    return;
  }
  if (key !== undefined) {
    if (Array[gin34](key)) {
      key = key[gin25](jQuery[gin367]);
    } else {
      key = jQuery[gin367](key);
      key = key in cache ? [key] : (key[gin299](rnothtmlwhite) || []);
    }
    i = key[gin19];
    while (i--) {
      delete cache[key[i]];
    }
  }
  if (key === undefined || jQuery[gin369](cache)) {
    if (owner.nodeType) {
      owner[this[gin363]] = undefined;
    } else {
      delete owner[this[gin363]];
    }
  }
}, 
  hasData: function(owner) {
  var cache = owner[this[gin363]];
  return cache !== undefined && !jQuery[gin369](cache);
}};
  var dataPriv = new Data();
  var dataUser = new Data();
  var rbrace = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/, rmultiDash = /[A-Z]/g;
  function getData(data) {
    if (data === gin370) {
      return true;
    }
    if (data === gin371) {
      return false;
    }
    if (data === gin372) {
      return null;
    }
    if (data === +data + gin38) {
      return +data;
    }
    if (rbrace[gin136](data)) {
      return JSON[gin373](data);
    }
    return data;
  }
  function dataAttr(elem, key, data) {
    var name;
    if (data === undefined && elem[gin151] === 1) {
      name = gin374 + key[gin37](rmultiDash, gin375)[gin56]();
      data = elem[gin138](name);
      if (typeof data === gin43) {
        try {
          data = getData(data);
        }        catch (e) {
}
        dataUser[gin368](elem, key, data);
      } else {
        data = undefined;
      }
    }
    return data;
  }
  jQuery[gin30]({
  hasData: function(elem) {
  return dataUser[gin376](elem) || dataPriv[gin376](elem);
}, 
  data: function(elem, name, data) {
  return dataUser[gin377](elem, name, data);
}, 
  removeData: function(elem, name) {
  dataUser[gin378](elem, name);
}, 
  _data: function(elem, name, data) {
  return dataPriv[gin377](elem, name, data);
}, 
  _removeData: function(elem, name) {
  dataPriv[gin378](elem, name);
}});
  jQuery[gin16][gin30]({
  data: function(key, value) {
  var i, name, data, elem = this[0], attrs = elem && elem[gin168];
  if (key === undefined) {
    if (this.length) {
      data = dataUser[gin310](elem);
      if (elem[gin151] === 1 && !dataPriv[gin310](elem, gin379)) {
        i = attrs[gin19];
        while (i--) {
          if (attrs[i]) {
            name = attrs[i][gin198];
            if (name[gin235](gin374) === 0) {
              name = jQuery[gin367](name[gin120](5));
              dataAttr(elem, name, data[name]);
            }
          }
        }
        dataPriv[gin368](elem, gin379, true);
      }
    }
    return data;
  }
  if (typeof key === gin2) {
    return this[gin23](function() {
  dataUser[gin368](this, key);
});
  }
  return access(this, function(value) {
  var data;
  if (elem && value === undefined) {
    data = dataUser[gin310](elem, key);
    if (data !== undefined) {
      return data;
    }
    data = dataAttr(elem, key);
    if (data !== undefined) {
      return data;
    }
    return;
  }
  this[gin23](function() {
  dataUser[gin368](this, key, value);
});
}, null, value, arguments[gin19] > 1, null, true);
}, 
  removeData: function(key) {
  return this[gin23](function() {
  dataUser[gin378](this, key);
});
}});
  jQuery[gin30]({
  queue: function(elem, type, data) {
  var queue;
  if (elem) {
    type = (type || gin380) + gin381;
    queue = dataPriv[gin310](elem, type);
    if (data) {
      if (!queue || Array[gin34](data)) {
        queue = dataPriv[gin377](elem, type, jQuery[gin307](data));
      } else {
        queue[gin46](data);
      }
    }
    return queue || [];
  }
}, 
  dequeue: function(elem, type) {
  type = type || gin380;
  var queue = jQuery[gin381](elem, type), startLength = queue.length, fn = queue[gin147](), hooks = jQuery[gin382](elem, type), next = function() {
  jQuery[gin383](elem, type);
};
  if (fn === gin384) {
    fn = queue[gin147]();
    startLength--;
  }
  if (fn) {
    if (type === gin380) {
      queue[gin217](gin384);
    }
    delete hooks.stop;
    fn[gin7](elem, next, hooks);
  }
  if (!startLength && hooks) {
    hooks[gin273][gin348]();
  }
}, 
  _queueHooks: function(elem, type) {
  var key = type + gin385;
  return dataPriv[gin310](elem, key) || dataPriv[gin377](elem, key, {
  empty: jQuery[gin317](gin330)[gin311](function() {
  dataPriv[gin378](elem, [type + gin381, key]);
})});
}});
  jQuery[gin16][gin30]({
  queue: function(type, data) {
  var setter = 2;
  if (typeof type !== gin43) {
    data = type;
    type = gin380;
    setter--;
  }
  if (arguments[gin19] < setter) {
    return jQuery[gin381](this[0], type);
  }
  return data === undefined ? this : this[gin23](function() {
  var queue = jQuery[gin381](this, type, data);
  jQuery[gin382](this, type);
  if (type === gin380 && queue[0] !== gin384) {
    jQuery[gin383](this, type);
  }
});
}, 
  dequeue: function(type) {
  return this[gin23](function() {
  jQuery[gin383](this, type);
});
}, 
  clearQueue: function(type) {
  return this[gin381](type || gin380, []);
}, 
  promise: function(type, obj) {
  var tmp, count = 1, defer = jQuery[gin336](), elements = this, i = this.length, resolve = function() {
  if (!(--count)) {
    defer[gin340](elements, [elements]);
  }
};
  if (typeof type !== gin43) {
    obj = type;
    type = undefined;
  }
  type = type || gin380;
  while (i--) {
    tmp = dataPriv[gin310](elements[i], type + gin385);
    if (tmp && tmp[gin273]) {
      count++;
      tmp[gin273][gin311](resolve);
    }
  }
  resolve();
  return defer[gin322](obj);
}});
  var pnum = (/[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/).source;
  var rcssNum = new RegExp(gin386 + pnum + gin387, "i");
  var cssExpand = ["Top", "Right", "Bottom", "Left"];
  var isHiddenWithinTree = function(elem, el) {
  elem = el || elem;
  return elem[gin392][gin393] === gin394 || elem[gin392][gin393] === gin38 && jQuery[gin216](elem[gin129], elem) && jQuery[gin395](elem, gin393) === gin394;
};
  var swap = function(elem, options, callback, args) {
  var ret, name, old = {};
  for (name in options) {
    old[name] = elem[gin392][name];
    elem[gin392][name] = options[name];
  }
  ret = callback[gin26](elem, args || []);
  for (name in options) {
    elem[gin392][name] = old[name];
  }
  return ret;
};
  function adjustCSS(elem, prop, valueParts, tween) {
    var adjusted, scale = 1, maxIterations = 20, currentValue = tween ? function() {
  return tween[gin396]();
} : function() {
  return jQuery[gin395](elem, prop, gin38);
}, initial = currentValue(), unit = valueParts && valueParts[3] || (jQuery[gin397][prop] ? "" : "px"), initialInUnit = (jQuery[gin397][prop] || unit !== gin398 && +initial) && rcssNum[gin130](jQuery[gin395](elem, prop));
    if (initialInUnit && initialInUnit[3] !== unit) {
      unit = unit || initialInUnit[3];
      valueParts = valueParts || [];
      initialInUnit = +initial || 1;
      do {
        scale = scale || gin399;
        initialInUnit = initialInUnit / scale;
        jQuery[gin392](elem, prop, initialInUnit + unit);
      } while (scale !== (scale = currentValue() / initial) && scale !== 1 && --maxIterations);
    }
    if (valueParts) {
      initialInUnit = +initialInUnit || +initial || 0;
      adjusted = valueParts[1] ? initialInUnit + (valueParts[1] + 1) * valueParts[2] : +valueParts[2];
      if (tween) {
        tween[gin400] = unit;
        tween[gin401] = initialInUnit;
        tween[gin402] = adjusted;
      }
    }
    return adjusted;
  }
  var defaultDisplayMap = {};
  function getDefaultDisplay(elem) {
    var temp, doc = elem.ownerDocument, nodeName = elem.nodeName, display = defaultDisplayMap[nodeName];
    if (display) {
      return display;
    }
    temp = doc[gin403][gin12](doc[gin8](nodeName));
    display = jQuery[gin395](temp, gin393);
    temp[gin13][gin14](temp);
    if (display === gin394) {
      display = gin404;
    }
    defaultDisplayMap[nodeName] = display;
    return display;
  }
  function showHide(elements, show) {
    var display, elem, values = [], index = 0, length = elements.length;
    for (; index < length; index++) {
      elem = elements[index];
      if (!elem.style) {
        continue;
      }
      display = elem[gin392][gin393];
      if (show) {
        if (display === gin394) {
          values[index] = dataPriv[gin310](elem, gin393) || null;
          if (!values[index]) {
            elem[gin392][gin393] = gin38;
          }
        }
        if (elem[gin392][gin393] === gin38 && isHiddenWithinTree(elem)) {
          values[index] = getDefaultDisplay(elem);
        }
      } else {
        if (display !== gin394) {
          values[index] = gin394;
          dataPriv[gin368](elem, gin393, display);
        }
      }
    }
    for (index = 0; index < length; index++) {
      if (values[index] != null) {
        elements[index][gin392][gin393] = values[index];
      }
    }
    return elements;
  }
  jQuery[gin16][gin30]({
  show: function() {
  return showHide(this, true);
}, 
  hide: function() {
  return showHide(this);
}, 
  toggle: function(state) {
  if (typeof state === gin31) {
    return state ? this[gin405]() : this[gin406]();
  }
  return this[gin23](function() {
  if (isHiddenWithinTree(this)) {
    jQuery(this)[gin405]();
  } else {
    jQuery(this)[gin406]();
  }
});
}});
  var rcheckableType = (/^(?:checkbox|radio)$/i);
  var rtagName = (/<([a-z][^\/\0>\x20\t\r\n\f]+)/i);
  var rscriptType = (/^$|\/(?:java|ecma)script/i);
  var wrapMap = {
  option: [1, "<select multiple='multiple'>", "</select>"], 
  thead: [1, "<table>", "</table>"], 
  col: [2, "<table><colgroup>", "</colgroup></table>"], 
  tr: [2, "<table><tbody>", "</tbody></table>"], 
  td: [3, "<table><tbody><tr>", "</tr></tbody></table>"], 
  _default: [0, "", ""]};
  wrapMap[gin417] = wrapMap[gin271];
  wrapMap[gin418] = wrapMap[gin419] = wrapMap[gin420] = wrapMap[gin421] = wrapMap[gin422];
  wrapMap[gin423] = wrapMap[gin424];
  function getAll(context, tag) {
    var ret;
    if (typeof context.getElementsByTagName !== gin6) {
      ret = context[gin133](tag || gin83);
    } else if (typeof context.querySelectorAll !== gin6) {
      ret = context[gin143](tag || gin83);
    } else {
      ret = [];
    }
    if (tag === undefined || tag && nodeName(context, tag)) {
      return jQuery[gin20]([context], ret);
    }
    return ret;
  }
  function setGlobalEval(elems, refElements) {
    var i = 0, l = elems.length;
    for (; i < l; i++) {
      dataPriv[gin368](elems[i], gin425, !refElements || dataPriv[gin310](refElements[i], gin425));
    }
  }
  var rhtml = /<|&#?\w+;/;
  function buildFragment(elems, context, scripts, selection, ignored) {
    var elem, tmp, tag, wrap, contains, j, fragment = context[gin426](), nodes = [], i = 0, l = elems.length;
    for (; i < l; i++) {
      elem = elems[i];
      if (elem || elem === 0) {
        if (jQuery[gin39](elem) === gin2) {
          jQuery[gin20](nodes, elem.nodeType ? [elem] : elem);
        } else if (!rhtml[gin136](elem)) {
          nodes[gin46](context[gin427](elem));
        } else {
          tmp = tmp || fragment[gin12](context[gin8](gin428));
          tag = (rtagName[gin130](elem) || ["", ""])[1][gin56]();
          wrap = wrapMap[tag] || wrapMap[gin429];
          tmp[gin177] = wrap[1] + jQuery[gin430](elem) + wrap[2];
          j = wrap[0];
          while (j--) {
            tmp = tmp[gin431];
          }
          jQuery[gin20](nodes, tmp[gin128]);
          tmp = fragment[gin227];
          tmp[gin256] = gin38;
        }
      }
    }
    fragment[gin256] = gin38;
    i = 0;
    while ((elem = nodes[i++])) {
      if (selection && jQuery[gin320](elem, selection) > -1) {
        if (ignored) {
          ignored[gin46](elem);
        }
        continue;
      }
      contains = jQuery[gin216](elem[gin129], elem);
      tmp = getAll(fragment[gin12](elem), gin9);
      if (contains) {
        setGlobalEval(tmp);
      }
      if (scripts) {
        j = 0;
        while ((elem = tmp[j++])) {
          if (rscriptType[gin136](elem[gin39] || gin38)) {
            scripts[gin46](elem);
          }
        }
      }
    }
    return fragment;
  }
  (function() {
  var fragment = document[gin426](), div = fragment[gin12](document[gin8](gin428)), input = document[gin8](gin154);
  input[gin139](gin39, gin432);
  input[gin139](gin270, gin270);
  input[gin139](gin198, gin433);
  div[gin12](input);
  support[gin434] = div[gin435](true)[gin435](true)[gin431][gin270];
  div[gin177] = gin436;
  support[gin437] = !!div[gin435](true)[gin431].defaultValue;
})();
  var documentElement = document.documentElement;
  var rkeyEvent = /^key/, rmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/, rtypenamespace = /^([^.]*)(?:\.(.+)|)/;
  function returnTrue() {
    return true;
  }
  function returnFalse() {
    return false;
  }
  function safeActiveElement() {
    try {
      return document.activeElement;
    }    catch (err) {
}
  }
  function on(elem, types, selector, data, fn, one) {
    var origFn, type;
    if (typeof types === gin2) {
      if (typeof selector !== gin43) {
        data = data || selector;
        selector = undefined;
      }
      for (type in types) {
        on(elem, type, selector, data, types[type], one);
      }
      return elem;
    }
    if (data == null && fn == null) {
      fn = selector;
      data = selector = undefined;
    } else if (fn == null) {
      if (typeof selector === gin43) {
        fn = data;
        data = undefined;
      } else {
        fn = data;
        data = selector;
        selector = undefined;
      }
    }
    if (fn === false) {
      fn = returnFalse;
    } else if (!fn) {
      return elem;
    }
    if (one === 1) {
      origFn = fn;
      fn = function(event) {
  jQuery()[gin438](event);
  return origFn[gin26](this, arguments);
};
      fn[gin48] = origFn[gin48] || (origFn[gin48] = jQuery.guid++);
    }
    return elem[gin23](function() {
  jQuery[gin439][gin311](this, types, fn, data, selector);
});
  }
  jQuery[gin439] = {
  global: {}, 
  add: function(elem, types, handler, data, selector) {
  var handleObjIn, eventHandle, tmp, events, t, handleObj, special, handlers, type, namespaces, origType, elemData = dataPriv[gin310](elem);
  if (!elemData) {
    return;
  }
  if (handler.handler) {
    handleObjIn = handler;
    handler = handleObjIn[gin440];
    selector = handleObjIn[gin285];
  }
  if (selector) {
    jQuery[gin174][gin206](documentElement, selector);
  }
  if (!handler.guid) {
    handler[gin48] = jQuery.guid++;
  }
  if (!(events = elemData[gin441])) {
    events = elemData[gin441] = {};
  }
  if (!(eventHandle = elemData[gin442])) {
    eventHandle = elemData[gin442] = function(e) {
  return typeof jQuery !== gin6 && jQuery[gin439][gin443] !== e[gin39] ? jQuery[gin439][gin444][gin26](elem, arguments) : undefined;
};
  }
  types = (types || gin38)[gin299](rnothtmlwhite) || [""];
  t = types[gin19];
  while (t--) {
    tmp = rtypenamespace[gin130](types[t]) || [];
    type = origType = tmp[1];
    namespaces = (tmp[2] || gin38)[gin52](gin445)[gin28]();
    if (!type) {
      continue;
    }
    special = jQuery[gin439][gin446][type] || {};
    type = (selector ? special.delegateType : special.bindType) || type;
    special = jQuery[gin439][gin446][type] || {};
    handleObj = jQuery[gin30]({
  type: type, 
  origType: origType, 
  data: data, 
  handler: handler, 
  guid: handler[gin48], 
  selector: selector, 
  needsContext: selector && jQuery[gin293][gin299][gin108][gin136](selector), 
  namespace: namespaces[gin141](gin445)}, handleObjIn);
    if (!(handlers = events[type])) {
      handlers = events[type] = [];
      handlers[gin447] = 0;
      if (!special.setup || special[gin448][gin7](elem, data, namespaces, eventHandle) === false) {
        if (elem.addEventListener) {
          elem[gin164](type, eventHandle);
        }
      }
    }
    if (special.add) {
      special[gin311][gin7](elem, handleObj);
      if (!handleObj[gin440].guid) {
        handleObj[gin440][gin48] = handler[gin48];
      }
    }
    if (selector) {
      handlers[gin29](handlers.delegateCount++, 0, handleObj);
    } else {
      handlers[gin46](handleObj);
    }
    jQuery[gin439][gin449][type] = true;
  }
}, 
  remove: function(elem, types, handler, selector, mappedTypes) {
  var j, origCount, tmp, events, t, handleObj, special, handlers, type, namespaces, origType, elemData = dataPriv[gin376](elem) && dataPriv[gin310](elem);
  if (!elemData || !(events = elemData[gin441])) {
    return;
  }
  types = (types || gin38)[gin299](rnothtmlwhite) || [""];
  t = types[gin19];
  while (t--) {
    tmp = rtypenamespace[gin130](types[t]) || [];
    type = origType = tmp[1];
    namespaces = (tmp[2] || gin38)[gin52](gin445)[gin28]();
    if (!type) {
      for (type in events) {
        jQuery[gin439][gin378](elem, type + types[t], handler, selector, true);
      }
      continue;
    }
    special = jQuery[gin439][gin446][type] || {};
    type = (selector ? special.delegateType : special.bindType) || type;
    handlers = events[type] || [];
    tmp = tmp[2] && new RegExp(gin450 + namespaces[gin141](gin451) + gin452);
    origCount = j = handlers[gin19];
    while (j--) {
      handleObj = handlers[j];
      if ((mappedTypes || origType === handleObj[gin453]) && (!handler || handler[gin48] === handleObj[gin48]) && (!tmp || tmp[gin136](handleObj[gin454])) && (!selector || selector === handleObj[gin285] || selector === gin455 && handleObj[gin285])) {
        handlers[gin29](j, 1);
        if (handleObj.selector) {
          handlers.delegateCount--;
        }
        if (special.remove) {
          special[gin378][gin7](elem, handleObj);
        }
      }
    }
    if (origCount && !handlers.length) {
      if (!special.teardown || special[gin456][gin7](elem, namespaces, elemData[gin442]) === false) {
        jQuery[gin457](elem, type, elemData[gin442]);
      }
      delete events[type];
    }
  }
  if (jQuery[gin369](events)) {
    dataPriv[gin378](elem, gin458);
  }
}, 
  dispatch: function(nativeEvent) {
  var event = jQuery[gin439][gin459](nativeEvent);
  var i, j, ret, matched, handleObj, handlerQueue, args = new Array(arguments.length), handlers = (dataPriv[gin310](this, gin441) || {})[event[gin39]] || [], special = jQuery[gin439][gin446][event[gin39]] || {};
  args[0] = event;
  for (i = 1; i < arguments[gin19]; i++) {
    args[i] = arguments[i];
  }
  event[gin460] = this;
  if (special[gin461] && special[gin461][gin7](this, event) === false) {
    return;
  }
  handlerQueue = jQuery[gin439][gin462][gin7](this, event, handlers);
  i = 0;
  while ((matched = handlerQueue[i++]) && !event[gin463]()) {
    event[gin464] = matched[gin465];
    j = 0;
    while ((handleObj = matched[gin462][j++]) && !event[gin466]()) {
      if (!event.rnamespace || event[gin467][gin136](handleObj[gin454])) {
        event[gin468] = handleObj;
        event[gin469] = handleObj[gin469];
        ret = ((jQuery[gin439][gin446][handleObj[gin453]] || {})[gin442] || handleObj[gin440])[gin26](matched[gin465], args);
        if (ret !== undefined) {
          if ((event[gin470] = ret) === false) {
            event[gin471]();
            event[gin472]();
          }
        }
      }
    }
  }
  if (special.postDispatch) {
    special[gin473][gin7](this, event);
  }
  return event.result;
}, 
  handlers: function(event, handlers) {
  var i, handleObj, sel, matchedHandlers, matchedSelectors, handlerQueue = [], delegateCount = handlers.delegateCount, cur = event.target;
  if (delegateCount && cur[gin151] && !(event[gin39] === gin474 && event[gin155] >= 1)) {
    for (; cur !== this; cur = cur[gin13] || this) {
      if (cur[gin151] === 1 && !(event[gin39] === gin474 && cur[gin124] === true)) {
        matchedHandlers = [];
        matchedSelectors = {};
        for (i = 0; i < delegateCount; i++) {
          handleObj = handlers[i];
          sel = handleObj[gin285] + gin53;
          if (matchedSelectors[sel] === undefined) {
            matchedSelectors[sel] = handleObj.needsContext ? jQuery(sel, this)[gin308](cur) > -1 : jQuery[gin174](sel, this, null, [cur]).length;
          }
          if (matchedSelectors[sel]) {
            matchedHandlers[gin46](handleObj);
          }
        }
        if (matchedHandlers.length) {
          handlerQueue[gin46]({
  elem: cur, 
  handlers: matchedHandlers});
        }
      }
    }
  }
  cur = this;
  if (delegateCount < handlers[gin19]) {
    handlerQueue[gin46]({
  elem: cur, 
  handlers: handlers[gin120](delegateCount)});
  }
  return handlerQueue;
}, 
  addProp: function(name, hook) {
  Object[gin365](jQuery[gin475][gin18], name, {
  enumerable: true, 
  configurable: true, 
  get: jQuery[gin32](hook) ? function() {
  if (this.originalEvent) {
    return hook(this[gin476]);
  }
} : function() {
  if (this.originalEvent) {
    return this[gin476][name];
  }
}, 
  set: function(value) {
  Object[gin365](this, name, {
  enumerable: true, 
  configurable: true, 
  writable: true, 
  value: value});
}});
}, 
  fix: function(originalEvent) {
  return originalEvent[jQuery[gin363]] ? originalEvent : new jQuery.Event(originalEvent);
}, 
  special: {
  load: {
  noBubble: true}, 
  focus: {
  trigger: function() {
  if (this !== safeActiveElement() && this[gin265]) {
    this[gin265]();
    return false;
  }
}, 
  delegateType: gin477}, 
  blur: {
  trigger: function() {
  if (this === safeActiveElement() && this[gin478]) {
    this[gin478]();
    return false;
  }
}, 
  delegateType: gin479}, 
  click: {
  trigger: function() {
  if (this[gin39] === gin480 && this[gin474] && nodeName(this, gin154)) {
    this[gin474]();
    return false;
  }
}, 
  _default: function(event) {
  return nodeName(event[gin261], gin481);
}}, 
  beforeunload: {
  postDispatch: function(event) {
  if (event[gin470] !== undefined && event[gin476]) {
    event[gin476][gin482] = event[gin470];
  }
}}}};
  jQuery[gin457] = function(elem, type, handle) {
  if (elem.removeEventListener) {
    elem[gin357](type, handle);
  }
};
  jQuery[gin475] = function(src, props) {
  if (!(this instanceof jQuery[gin475])) {
    return new jQuery.Event(src, props);
  }
  if (src && src[gin39]) {
    this[gin476] = src;
    this[gin39] = src[gin39];
    this[gin483] = src[gin484] || src[gin484] === undefined && src[gin482] === false ? returnTrue : returnFalse;
    this[gin261] = (src[gin261] && src[gin261][gin151] === 3) ? src[gin261].parentNode : src.target;
    this[gin464] = src[gin464];
    this[gin485] = src[gin485];
  } else {
    this[gin39] = src;
  }
  if (props) {
    jQuery[gin30](this, props);
  }
  this[gin486] = src && src[gin486] || jQuery[gin49]();
  this[jQuery[gin363]] = true;
};
  jQuery[gin475][gin18] = {
  constructor: jQuery[gin475], 
  isDefaultPrevented: returnFalse, 
  isPropagationStopped: returnFalse, 
  isImmediatePropagationStopped: returnFalse, 
  isSimulated: false, 
  preventDefault: function() {
  var e = this.originalEvent;
  this[gin483] = returnTrue;
  if (e && !this.isSimulated) {
    e[gin471]();
  }
}, 
  stopPropagation: function() {
  var e = this.originalEvent;
  this[gin463] = returnTrue;
  if (e && !this.isSimulated) {
    e[gin472]();
  }
}, 
  stopImmediatePropagation: function() {
  var e = this.originalEvent;
  this[gin466] = returnTrue;
  if (e && !this.isSimulated) {
    e[gin487]();
  }
  this[gin472]();
}};
  jQuery[gin23]({
  altKey: true, 
  bubbles: true, 
  cancelable: true, 
  changedTouches: true, 
  ctrlKey: true, 
  detail: true, 
  eventPhase: true, 
  metaKey: true, 
  pageX: true, 
  pageY: true, 
  shiftKey: true, 
  view: true, 
  gin488: true, 
  charCode: true, 
  key: true, 
  keyCode: true, 
  button: true, 
  buttons: true, 
  clientX: true, 
  clientY: true, 
  offsetX: true, 
  offsetY: true, 
  pointerId: true, 
  pointerType: true, 
  screenX: true, 
  screenY: true, 
  targetTouches: true, 
  toElement: true, 
  touches: true, 
  which: function(event) {
  var button = event.button;
  if (event[gin489] == null && rkeyEvent[gin136](event[gin39])) {
    return event[gin490] != null ? event.charCode : event.keyCode;
  }
  if (!event.which && button !== undefined && rmouseEvent[gin136](event[gin39])) {
    if (button & 1) {
      return 1;
    }
    if (button & 2) {
      return 3;
    }
    if (button & 4) {
      return 2;
    }
    return 0;
  }
  return event.which;
}}, jQuery[gin439][gin491]);
  jQuery[gin23]({
  mouseenter: gin492, 
  mouseleave: gin493, 
  pointerenter: gin494, 
  pointerleave: gin495}, function(orig, fix) {
  jQuery[gin439][gin446][orig] = {
  delegateType: fix, 
  bindType: fix, 
  handle: function(event) {
  var ret, target = this, related = event.relatedTarget, handleObj = event.handleObj;
  if (!related || (related !== target && !jQuery[gin216](target, related))) {
    event[gin39] = handleObj[gin453];
    ret = handleObj[gin440][gin26](this, arguments);
    event[gin39] = fix;
  }
  return ret;
}};
});
  jQuery[gin16][gin30]({
  on: function(types, selector, data, fn) {
  return on(this, types, selector, data, fn);
}, 
  one: function(types, selector, data, fn) {
  return on(this, types, selector, data, fn, 1);
}, 
  off: function(types, selector, fn) {
  var handleObj, type;
  if (types && types[gin471] && types[gin468]) {
    handleObj = types[gin468];
    jQuery(types[gin460])[gin438](handleObj.namespace ? handleObj[gin453] + gin445 + handleObj[gin454] : handleObj.origType, handleObj[gin285], handleObj[gin440]);
    return this;
  }
  if (typeof types === gin2) {
    for (type in types) {
      this[gin438](type, selector, types[type]);
    }
    return this;
  }
  if (selector === false || typeof selector === gin40) {
    fn = selector;
    selector = undefined;
  }
  if (fn === false) {
    fn = returnFalse;
  }
  return this[gin23](function() {
  jQuery[gin439][gin378](this, types, fn, selector);
});
}});
  var rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([a-z][^\/\0>\x20\t\r\n\f]*)[^>]*)\/>/gi, rnoInnerhtml = /<script|<style|<link/i, rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i, rscriptTypeMasked = /^true\/(.*)/, rcleanScript = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g;
  function manipulationTarget(elem, content) {
    if (nodeName(elem, gin496) && nodeName(content[gin151] !== 11 ? content : content.firstChild, gin497)) {
      return jQuery(gin498, elem)[0] || elem;
    }
    return elem;
  }
  function disableScript(elem) {
    elem[gin39] = (elem[gin138](gin39) !== null) + gin499 + elem[gin39];
    return elem;
  }
  function restoreScript(elem) {
    var match = rscriptTypeMasked[gin130](elem[gin39]);
    if (match) {
      elem[gin39] = match[1];
    } else {
      elem[gin144](gin39);
    }
    return elem;
  }
  function cloneCopyEvent(src, dest) {
    var i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;
    if (dest[gin151] !== 1) {
      return;
    }
    if (dataPriv[gin376](src)) {
      pdataOld = dataPriv[gin377](src);
      pdataCur = dataPriv[gin368](dest, pdataOld);
      events = pdataOld[gin441];
      if (events) {
        delete pdataCur.handle;
        pdataCur[gin441] = {};
        for (type in events) {
          for (i = 0 , l = events[type][gin19]; i < l; i++) {
            jQuery[gin439][gin311](dest, type, events[type][i]);
          }
        }
      }
    }
    if (dataUser[gin376](src)) {
      udataOld = dataUser[gin377](src);
      udataCur = jQuery[gin30]({}, udataOld);
      dataUser[gin368](dest, udataCur);
    }
  }
  function fixInput(src, dest) {
    var nodeName = dest[gin137][gin56]();
    if (nodeName === gin154 && rcheckableType[gin136](src[gin39])) {
      dest[gin270] = src[gin270];
    } else if (nodeName === gin154 || nodeName === gin500) {
      dest[gin501] = src[gin501];
    }
  }
  function domManip(collection, args, callback, ignored) {
    args = concat[gin26]([], args);
    var fragment, first, scripts, hasScripts, node, doc, i = 0, l = collection.length, iNoClone = l - 1, value = args[0], isFunction = jQuery[gin32](value);
    if (isFunction || (l > 1 && typeof value === gin43 && !support.checkClone && rchecked[gin136](value))) {
      return collection[gin23](function(index) {
  var self = collection[gin27](index);
  if (isFunction) {
    args[0] = value[gin7](this, index, self[gin502]());
  }
  domManip(self, args, callback, ignored);
});
    }
    if (l) {
      fragment = buildFragment(args, collection[0][gin129], false, collection, ignored);
      first = fragment[gin227];
      if (fragment[gin128][gin19] === 1) {
        fragment = first;
      }
      if (first || ignored) {
        scripts = jQuery[gin25](getAll(fragment, gin9), disableScript);
        hasScripts = scripts[gin19];
        for (; i < l; i++) {
          node = fragment;
          if (i !== iNoClone) {
            node = jQuery[gin503](node, true, true);
            if (hasScripts) {
              jQuery[gin20](scripts, getAll(node, gin9));
            }
          }
          callback[gin7](collection[i], node, i);
        }
        if (hasScripts) {
          doc = scripts[scripts[gin19] - 1][gin129];
          jQuery[gin25](scripts, restoreScript);
          for (i = 0; i < hasScripts; i++) {
            node = scripts[i];
            if (rscriptType[gin136](node[gin39] || gin38) && !dataPriv[gin377](node, gin425) && jQuery[gin216](doc, node)) {
              if (node.src) {
                if (jQuery._evalUrl) {
                  jQuery[gin504](node[gin505]);
                }
              } else {
                DOMEval(node[gin256][gin37](rcleanScript, gin38), doc);
              }
            }
          }
        }
      }
    }
    return collection;
  }
  function remove(elem, selector, keepData) {
    var node, nodes = selector ? jQuery[gin173](selector, elem) : elem, i = 0;
    for (; (node = nodes[i]) != null; i++) {
      if (!keepData && node[gin151] === 1) {
        jQuery[gin506](getAll(node));
      }
      if (node.parentNode) {
        if (keepData && jQuery[gin216](node[gin129], node)) {
          setGlobalEval(getAll(node, gin9));
        }
        node[gin13][gin14](node);
      }
    }
    return elem;
  }
  jQuery[gin30]({
  htmlPrefilter: function(html) {
  return html[gin37](rxhtmlTag, gin507);
}, 
  clone: function(elem, dataAndEvents, deepDataAndEvents) {
  var i, l, srcElements, destElements, clone = elem[gin435](true), inPage = jQuery[gin216](elem[gin129], elem);
  if (!support.noCloneChecked && (elem[gin151] === 1 || elem[gin151] === 11) && !jQuery[gin296](elem)) {
    destElements = getAll(clone);
    srcElements = getAll(elem);
    for (i = 0 , l = srcElements[gin19]; i < l; i++) {
      fixInput(srcElements[i], destElements[i]);
    }
  }
  if (dataAndEvents) {
    if (deepDataAndEvents) {
      srcElements = srcElements || getAll(elem);
      destElements = destElements || getAll(clone);
      for (i = 0 , l = srcElements[gin19]; i < l; i++) {
        cloneCopyEvent(srcElements[i], destElements[i]);
      }
    } else {
      cloneCopyEvent(elem, clone);
    }
  }
  destElements = getAll(clone, gin9);
  if (destElements[gin19] > 0) {
    setGlobalEval(destElements, !inPage && getAll(elem, gin9));
  }
  return clone;
}, 
  cleanData: function(elems) {
  var data, elem, type, special = jQuery[gin439].special, i = 0;
  for (; (elem = elems[i]) !== undefined; i++) {
    if (acceptData(elem)) {
      if ((data = elem[dataPriv[gin363]])) {
        if (data.events) {
          for (type in data.events) {
            if (special[type]) {
              jQuery[gin439][gin378](elem, type);
            } else {
              jQuery[gin457](elem, type, data[gin442]);
            }
          }
        }
        elem[dataPriv[gin363]] = undefined;
      }
      if (elem[dataUser[gin363]]) {
        elem[dataUser[gin363]] = undefined;
      }
    }
  }
}});
  jQuery[gin16][gin30]({
  detach: function(selector) {
  return remove(this, selector, true);
}, 
  remove: function(selector) {
  return remove(this, selector);
}, 
  text: function(value) {
  return access(this, function(value) {
  return value === undefined ? jQuery[gin10](this) : this[gin273]()[gin23](function() {
  if (this[gin151] === 1 || this[gin151] === 11 || this[gin151] === 9) {
    this[gin256] = value;
  }
});
}, null, value, arguments[gin19]);
}, 
  append: function() {
  return domManip(this, arguments, function(elem) {
  if (this[gin151] === 1 || this[gin151] === 11 || this[gin151] === 9) {
    var target = manipulationTarget(this, elem);
    target[gin12](elem);
  }
});
}, 
  prepend: function() {
  return domManip(this, arguments, function(elem) {
  if (this[gin151] === 1 || this[gin151] === 11 || this[gin151] === 9) {
    var target = manipulationTarget(this, elem);
    target[gin508](elem, target[gin227]);
  }
});
}, 
  before: function() {
  return domManip(this, arguments, function(elem) {
  if (this.parentNode) {
    this[gin13][gin508](elem, this);
  }
});
}, 
  after: function() {
  return domManip(this, arguments, function(elem) {
  if (this.parentNode) {
    this[gin13][gin508](elem, this[gin153]);
  }
});
}, 
  empty: function() {
  var elem, i = 0;
  for (; (elem = this[i]) != null; i++) {
    if (elem[gin151] === 1) {
      jQuery[gin506](getAll(elem, false));
      elem[gin256] = gin38;
    }
  }
  return this;
}, 
  clone: function(dataAndEvents, deepDataAndEvents) {
  dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
  deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;
  return this[gin25](function() {
  return jQuery[gin503](this, dataAndEvents, deepDataAndEvents);
});
}, 
  html: function(value) {
  return access(this, function(value) {
  var elem = this[0] || {}, i = 0, l = this.length;
  if (value === undefined && elem[gin151] === 1) {
    return elem.innerHTML;
  }
  if (typeof value === gin43 && !rnoInnerhtml[gin136](value) && !wrapMap[(rtagName[gin130](value) || ["", ""])[1][gin56]()]) {
    value = jQuery[gin430](value);
    try {
      for (; i < l; i++) {
        elem = this[i] || {};
        if (elem[gin151] === 1) {
          jQuery[gin506](getAll(elem, false));
          elem[gin177] = value;
        }
      }
      elem = 0;
    }    catch (e) {
}
  }
  if (elem) {
    this[gin273]()[gin509](value);
  }
}, null, value, arguments[gin19]);
}, 
  replaceWith: function() {
  var ignored = [];
  return domManip(this, arguments, function(elem) {
  var parent = this.parentNode;
  if (jQuery[gin320](this, ignored) < 0) {
    jQuery[gin506](getAll(this));
    if (parent) {
      parent[gin510](elem, this);
    }
  }
}, ignored);
}});
  jQuery[gin23]({
  appendTo: gin509, 
  prependTo: gin511, 
  insertBefore: gin512, 
  insertAfter: gin513, 
  replaceAll: gin514}, function(name, original) {
  jQuery[gin16][name] = function(selector) {
  var elems, ret = [], insert = jQuery(selector), last = insert[gin19] - 1, i = 0;
  for (; i <= last; i++) {
    elems = i === last ? this : this[gin503](true);
    jQuery(insert[i])[original](elems);
    push[gin26](ret, elems[gin310]());
  }
  return this[gin24](ret);
};
});
  var rmargin = (/^margin/);
  var rnumnonpx = new RegExp(gin94 + pnum + gin515, "i");
  var getStyles = function(elem) {
  var view = elem[gin129].defaultView;
  if (!view || !view.opener) {
    view = gin5;
  }
  return view[gin516](elem);
};
  (function() {
  function computeStyleTests() {
    if (!div) {
      return;
    }
    div[gin392][gin517] = gin518 + gin519 + gin520 + gin521;
    div[gin177] = gin38;
    documentElement[gin12](container);
    var divStyle = gin5[gin516](div);
    pixelPositionVal = divStyle[gin163] !== gin522;
    reliableMarginLeftVal = divStyle[gin523] === gin524;
    boxSizingReliableVal = divStyle[gin525] === gin526;
    div[gin392][gin527] = gin528;
    pixelMarginRightVal = divStyle[gin527] === gin526;
    documentElement[gin14](container);
    div = null;
  }
  var pixelPositionVal, boxSizingReliableVal, pixelMarginRightVal, reliableMarginLeftVal, container = document[gin8](gin428), div = document[gin8](gin428);
  if (!div.style) {
    return;
  }
  div[gin392][gin529] = gin530;
  div[gin435](true)[gin392][gin529] = gin38;
  support[gin531] = div[gin392][gin529] === gin530;
  container[gin392][gin517] = gin532 + gin533;
  container[gin12](div);
  jQuery[gin30](support, {
  pixelPosition: function() {
  computeStyleTests();
  return pixelPositionVal;
}, 
  boxSizingReliable: function() {
  computeStyleTests();
  return boxSizingReliableVal;
}, 
  pixelMarginRight: function() {
  computeStyleTests();
  return pixelMarginRightVal;
}, 
  reliableMarginLeft: function() {
  computeStyleTests();
  return reliableMarginLeftVal;
}});
})();
  function curCSS(elem, name, computed) {
    var width, minWidth, maxWidth, ret, style = elem.style;
    computed = computed || getStyles(elem);
    if (computed) {
      ret = computed[gin534](name) || computed[name];
      if (ret === gin38 && !jQuery[gin216](elem[gin129], elem)) {
        ret = jQuery[gin392](elem, name);
      }
      if (!support[gin535]() && rnumnonpx[gin136](ret) && rmargin[gin136](name)) {
        width = style[gin525];
        minWidth = style[gin536];
        maxWidth = style[gin537];
        style[gin536] = style[gin537] = style[gin525] = ret;
        ret = computed[gin525];
        style[gin525] = width;
        style[gin536] = minWidth;
        style[gin537] = maxWidth;
      }
    }
    return ret !== undefined ? ret + gin38 : ret;
  }
  function addGetHookIf(conditionFn, hookFn) {
    return {
  get: function() {
  if (conditionFn()) {
    delete this.get;
    return;
  }
  return (this[gin310] = hookFn)[gin26](this, arguments);
}};
  }
  var rdisplayswap = /^(none|table(?!-c[ea]).+)/, rcustomProp = /^--/, cssShow = {
  position: gin538, 
  visibility: gin197, 
  display: gin404}, cssNormalTransform = {
  letterSpacing: gin283, 
  fontWeight: gin539}, cssPrefixes = ["Webkit", "Moz", "ms"], emptyStyle = document[gin8](gin428).style;
  function vendorPropName(name) {
    if (name in emptyStyle) {
      return name;
    }
    var capName = name[0][gin17]() + name[gin120](1), i = cssPrefixes.length;
    while (i--) {
      name = cssPrefixes[i] + capName;
      if (name in emptyStyle) {
        return name;
      }
    }
  }
  function finalPropName(name) {
    var ret = jQuery[gin543][name];
    if (!ret) {
      ret = jQuery[gin543][name] = vendorPropName(name) || name;
    }
    return ret;
  }
  function setPositiveNumber(elem, value, subtract) {
    var matches = rcssNum[gin130](value);
    return matches ? Math[gin544](0, matches[2] - (subtract || 0)) + (matches[3] || gin398) : value;
  }
  function augmentWidthOrHeight(elem, name, extra, isBorderBox, styles) {
    var i, val = 0;
    if (extra === (isBorderBox ? "border" : "content")) {
      i = 4;
    } else {
      i = name === gin525 ? 1 : 0;
    }
    for (; i < 4; i += 2) {
      if (extra === gin546) {
        val += jQuery[gin395](elem, extra + cssExpand[i], true, styles);
      }
      if (isBorderBox) {
        if (extra === gin314) {
          val -= jQuery[gin395](elem, gin547 + cssExpand[i], true, styles);
        }
        if (extra !== gin546) {
          val -= jQuery[gin395](elem, gin545 + cssExpand[i] + gin548, true, styles);
        }
      } else {
        val += jQuery[gin395](elem, gin547 + cssExpand[i], true, styles);
        if (extra !== gin547) {
          val += jQuery[gin395](elem, gin545 + cssExpand[i] + gin548, true, styles);
        }
      }
    }
    return val;
  }
  function getWidthOrHeight(elem, name, extra) {
    var valueIsBorderBox, styles = getStyles(elem), val = curCSS(elem, name, styles), isBorderBox = jQuery[gin395](elem, gin549, false, styles) === gin550;
    if (rnumnonpx[gin136](val)) {
      return val;
    }
    valueIsBorderBox = isBorderBox && (support[gin551]() || val === elem[gin392][name]);
    if (val === gin552) {
      val = elem[gin553 + name[0][gin17]() + name[gin120](1)];
    }
    val = parseFloat(val) || 0;
    return (val + augmentWidthOrHeight(elem, name, extra || (isBorderBox ? "border" : "content"), valueIsBorderBox, styles)) + gin398;
  }
  jQuery[gin30]({
  cssHooks: {
  opacity: {
  get: function(elem, computed) {
  if (computed) {
    var ret = curCSS(elem, gin554);
    return ret === gin38 ? "1" : ret;
  }
}}}, 
  cssNumber: {
  gin556: true, 
  gin557: true, 
  gin558: true, 
  gin559: true, 
  gin560: true, 
  gin561: true, 
  gin562: true, 
  gin554: true, 
  gin563: true, 
  gin564: true, 
  gin565: true, 
  gin566: true, 
  gin567: true}, 
  cssProps: {
  gin568: gin569}, 
  style: function(elem, name, value, extra) {
  if (!elem || elem[gin151] === 3 || elem[gin151] === 8 || !elem.style) {
    return;
  }
  var ret, type, hooks, origName = jQuery[gin367](name), isCustomProp = rcustomProp[gin136](name), style = elem.style;
  if (!isCustomProp) {
    name = finalPropName(origName);
  }
  hooks = jQuery[gin570][name] || jQuery[gin570][origName];
  if (value !== undefined) {
    type = typeof value;
    if (type === gin43 && (ret = rcssNum[gin130](value)) && ret[1]) {
      value = adjustCSS(elem, name, ret);
      type = gin42;
    }
    if (value == null || value !== value) {
      return;
    }
    if (type === gin42) {
      value += ret && ret[3] || (jQuery[gin397][origName] ? "" : "px");
    }
    if (!support.clearCloneStyle && value === gin38 && name[gin235](gin571) === 0) {
      style[name] = gin572;
    }
    if (!hooks || !(gin368 in hooks) || (value = hooks[gin368](elem, value, extra)) !== undefined) {
      if (isCustomProp) {
        style[gin573](name, value);
      } else {
        style[name] = value;
      }
    }
  } else {
    if (hooks && gin310 in hooks && (ret = hooks[gin310](elem, false, extra)) !== undefined) {
      return ret;
    }
    return style[name];
  }
}, 
  css: function(elem, name, extra, styles) {
  var val, num, hooks, origName = jQuery[gin367](name), isCustomProp = rcustomProp[gin136](name);
  if (!isCustomProp) {
    name = finalPropName(origName);
  }
  hooks = jQuery[gin570][name] || jQuery[gin570][origName];
  if (hooks && gin310 in hooks) {
    val = hooks[gin310](elem, true, extra);
  }
  if (val === undefined) {
    val = curCSS(elem, name, styles);
  }
  if (val === gin574 && name in cssNormalTransform) {
    val = cssNormalTransform[name];
  }
  if (extra === gin38 || extra) {
    num = parseFloat(val);
    return extra === true || isFinite(num) ? num || 0 : val;
  }
  return val;
}});
  jQuery[gin23](["height", "width"], function(i, name) {
  jQuery[gin570][name] = {
  get: function(elem, computed, extra) {
  if (computed) {
    return rdisplayswap[gin136](jQuery[gin395](elem, gin393)) && (!elem[gin576]().length || !elem[gin577]().width) ? swap(elem, cssShow, function() {
  return getWidthOrHeight(elem, name, extra);
}) : getWidthOrHeight(elem, name, extra);
  }
}, 
  set: function(elem, value, extra) {
  var matches, styles = extra && getStyles(elem), subtract = extra && augmentWidthOrHeight(elem, name, extra, jQuery[gin395](elem, gin549, false, styles) === gin550, styles);
  if (subtract && (matches = rcssNum[gin130](value)) && (matches[3] || gin398) !== gin398) {
    elem[gin392][name] = value;
    value = jQuery[gin395](elem, name);
  }
  return setPositiveNumber(elem, value, subtract);
}};
});
  jQuery[gin570][gin523] = addGetHookIf(support[gin578], function(elem, computed) {
  if (computed) {
    return (parseFloat(curCSS(elem, gin523)) || elem[gin577]()[gin579] - swap(elem, {
  marginLeft: 0}, function() {
  return elem[gin577]().left;
})) + gin398;
  }
});
  jQuery[gin23]({
  margin: gin38, 
  padding: gin38, 
  border: gin548}, function(prefix, suffix) {
  jQuery[gin570][prefix + suffix] = {
  expand: function(value) {
  var i = 0, expanded = {}, parts = typeof value === gin43 ? value[gin52](gin53) : [value];
  for (; i < 4; i++) {
    expanded[prefix + cssExpand[i] + suffix] = parts[i] || parts[i - 2] || parts[0];
  }
  return expanded;
}};
  if (!rmargin[gin136](prefix)) {
    jQuery[gin570][prefix + suffix][gin368] = setPositiveNumber;
  }
});
  jQuery[gin16][gin30]({
  css: function(name, value) {
  return access(this, function(elem, name, value) {
  var styles, len, map = {}, i = 0;
  if (Array[gin34](name)) {
    styles = getStyles(elem);
    len = name[gin19];
    for (; i < len; i++) {
      map[name[i]] = jQuery[gin395](elem, name[i], false, styles);
    }
    return map;
  }
  return value !== undefined ? jQuery[gin392](elem, name, value) : jQuery[gin395](elem, name);
}, name, value, arguments[gin19] > 1);
}});
  function Tween(elem, options, prop, end, easing) {
    return new Tween[gin18].init(elem, options, prop, end, easing);
  }
  jQuery[gin580] = Tween;
  Tween[gin18] = {
  constructor: Tween, 
  init: function(elem, options, prop, end, easing, unit) {
  this[gin465] = elem;
  this[gin581] = prop;
  this[gin582] = easing || jQuery[gin582][gin429];
  this[gin583] = options;
  this[gin401] = this[gin49] = this[gin396]();
  this[gin402] = end;
  this[gin400] = unit || (jQuery[gin397][prop] ? "" : "px");
}, 
  cur: function() {
  var hooks = Tween[gin584][this[gin581]];
  return hooks && hooks[gin310] ? hooks[gin310](this) : Tween[gin584][gin429][gin310](this);
}, 
  run: function(percent) {
  var eased, hooks = Tween[gin584][this[gin581]];
  if (this[gin583].duration) {
    this[gin585] = eased = jQuery[gin582][this[gin582]](percent, this[gin583][gin586] * percent, 0, 1, this[gin583][gin586]);
  } else {
    this[gin585] = eased = percent;
  }
  this[gin49] = (this[gin402] - this[gin401]) * eased + this[gin401];
  if (this[gin583].step) {
    this[gin583][gin587][gin7](this[gin465], this[gin49], this);
  }
  if (hooks && hooks[gin368]) {
    hooks[gin368](this);
  } else {
    Tween[gin584][gin429][gin368](this);
  }
  return this;
}};
  Tween[gin18][gin302][gin18] = Tween[gin18];
  Tween[gin584] = {
  _default: {
  get: function(tween) {
  var result;
  if (tween[gin465][gin151] !== 1 || tween[gin465][tween[gin581]] != null && tween[gin465][gin392][tween[gin581]] == null) {
    return tween[gin465][tween[gin581]];
  }
  result = jQuery[gin395](tween[gin465], tween[gin581], gin38);
  return !result || result === gin552 ? 0 : result;
}, 
  set: function(tween) {
  if (jQuery[gin380][gin587][tween[gin581]]) {
    jQuery[gin380][gin587][tween[gin581]](tween);
  } else if (tween[gin465][gin151] === 1 && (tween[gin465][gin392][jQuery[gin543][tween[gin581]]] != null || jQuery[gin570][tween[gin581]])) {
    jQuery[gin392](tween[gin465], tween[gin581], tween[gin49] + tween[gin400]);
  } else {
    tween[gin465][tween[gin581]] = tween[gin49];
  }
}}};
  Tween[gin584][gin588] = Tween[gin584][gin589] = {
  set: function(tween) {
  if (tween[gin465][gin151] && tween[gin465][gin13]) {
    tween[gin465][tween[gin581]] = tween[gin49];
  }
}};
  jQuery[gin582] = {
  linear: function(p) {
  return p;
}, 
  swing: function(p) {
  return 0.5 - Math[gin590](p * Math[gin591]) / 2;
}, 
  _default: gin592};
  jQuery[gin380] = Tween[gin18][gin302];
  jQuery[gin380][gin587] = {};
  var fxNow, inProgress, rfxtypes = /^(?:toggle|show|hide)$/, rrun = /queueHooks$/;
  function schedule() {
    if (inProgress) {
      if (document[gin197] === false && gin5[gin593]) {
        gin5[gin593](schedule);
      } else {
        gin5[gin345](schedule, jQuery[gin380][gin594]);
      }
      jQuery[gin380][gin595]();
    }
  }
  function createFxNow() {
    gin5[gin345](function() {
  fxNow = undefined;
});
    return (fxNow = jQuery[gin49]());
  }
  function genFx(type, includeWidth) {
    var which, i = 0, attrs = {
  height: type};
    includeWidth = includeWidth ? 1 : 0;
    for (; i < 4; i += 2 - includeWidth) {
      which = cssExpand[i];
      attrs[gin546 + which] = attrs[gin547 + which] = type;
    }
    if (includeWidth) {
      attrs[gin554] = attrs[gin525] = type;
    }
    return attrs;
  }
  function createTween(value, prop, animation) {
    var tween, collection = (Animation[gin596][prop] || [])[gin47](Animation[gin596][gin83]), index = 0, length = collection.length;
    for (; index < length; index++) {
      if ((tween = collection[index][gin7](animation, prop, value))) {
        return tween;
      }
    }
  }
  function defaultPrefilter(elem, props, opts) {
    var prop, value, toggle, hooks, oldfire, propTween, restoreDisplay, display, isBox = gin525 in props || gin575 in props, anim = this, orig = {}, style = elem.style, hidden = elem[gin151] && isHiddenWithinTree(elem), dataShow = dataPriv[gin310](elem, gin597);
    if (!opts.queue) {
      hooks = jQuery[gin382](elem, gin380);
      if (hooks[gin598] == null) {
        hooks[gin598] = 0;
        oldfire = hooks[gin273][gin348];
        hooks[gin273][gin348] = function() {
  if (!hooks.unqueued) {
    oldfire();
  }
};
      }
      hooks.unqueued++;
      anim[gin599](function() {
  anim[gin599](function() {
  hooks.unqueued--;
  if (!jQuery[gin381](elem, gin380).length) {
    hooks[gin273][gin348]();
  }
});
});
    }
    for (prop in props) {
      value = props[prop];
      if (rfxtypes[gin136](value)) {
        delete props[prop];
        toggle = toggle || value === gin600;
        if (value === (hidden ? "hide" : "show")) {
          if (value === gin405 && dataShow && dataShow[prop] !== undefined) {
            hidden = true;
          } else {
            continue;
          }
        }
        orig[prop] = dataShow && dataShow[prop] || jQuery[gin392](elem, prop);
      }
    }
    propTween = !jQuery[gin369](props);
    if (!propTween && jQuery[gin369](orig)) {
      return;
    }
    if (isBox && elem[gin151] === 1) {
      opts[gin601] = [style.overflow, style.overflowX, style.overflowY];
      restoreDisplay = dataShow && dataShow[gin393];
      if (restoreDisplay == null) {
        restoreDisplay = dataPriv[gin310](elem, gin393);
      }
      display = jQuery[gin395](elem, gin393);
      if (display === gin394) {
        if (restoreDisplay) {
          display = restoreDisplay;
        } else {
          showHide([elem], true);
          restoreDisplay = elem[gin392][gin393] || restoreDisplay;
          display = jQuery[gin395](elem, gin393);
          showHide([elem]);
        }
      }
      if (display === gin602 || display === gin603 && restoreDisplay != null) {
        if (jQuery[gin395](elem, gin568) === gin394) {
          if (!propTween) {
            anim[gin323](function() {
  style[gin393] = restoreDisplay;
});
            if (restoreDisplay == null) {
              display = style[gin393];
              restoreDisplay = display === gin394 ? "" : display;
            }
          }
          style[gin393] = gin603;
        }
      }
    }
    if (opts.overflow) {
      style[gin601] = gin197;
      anim[gin599](function() {
  style[gin601] = opts[gin601][0];
  style[gin604] = opts[gin601][1];
  style[gin605] = opts[gin601][2];
});
    }
    propTween = false;
    for (prop in orig) {
      if (!propTween) {
        if (dataShow) {
          if (gin197 in dataShow) {
            hidden = dataShow[gin197];
          }
        } else {
          dataShow = dataPriv[gin377](elem, gin597, {
  display: restoreDisplay});
        }
        if (toggle) {
          dataShow[gin197] = !hidden;
        }
        if (hidden) {
          showHide([elem], true);
        }
        anim[gin323](function() {
  if (!hidden) {
    showHide([elem]);
  }
  dataPriv[gin378](elem, gin597);
  for (prop in orig) {
    jQuery[gin392](elem, prop, orig[prop]);
  }
});
      }
      propTween = createTween(hidden ? dataShow[prop] : 0, prop, anim);
      if (!(prop in dataShow)) {
        dataShow[prop] = propTween[gin401];
        if (hidden) {
          propTween[gin402] = propTween[gin401];
          propTween[gin401] = 0;
        }
      }
    }
  }
  function propFilter(props, specialEasing) {
    var index, name, easing, value, hooks;
    for (index in props) {
      name = jQuery[gin367](index);
      easing = specialEasing[name];
      value = props[index];
      if (Array[gin34](value)) {
        easing = value[1];
        value = props[index] = value[0];
      }
      if (index !== name) {
        props[name] = value;
        delete props[index];
      }
      hooks = jQuery[gin570][name];
      if (hooks && gin606 in hooks) {
        value = hooks[gin606](value);
        delete props[name];
        for (index in value) {
          if (!(index in props)) {
            props[index] = value[index];
            specialEasing[index] = easing;
          }
        }
      } else {
        specialEasing[name] = easing;
      }
    }
  }
  function Animation(elem, properties, options) {
    var result, stopped, index = 0, length = Animation[gin607].length, deferred = jQuery[gin336]()[gin599](function() {
  delete tick.elem;
}), tick = function() {
  if (stopped) {
    return false;
  }
  var currentTime = fxNow || createFxNow(), remaining = Math[gin544](0, animation[gin608] + animation[gin586] - currentTime), temp = remaining / animation[gin586] || 0, percent = 1 - temp, index = 0, length = animation[gin609].length;
  for (; index < length; index++) {
    animation[gin609][index][gin610](percent);
  }
  deferred[gin339](elem, [animation, percent, remaining]);
  if (percent < 1 && length) {
    return remaining;
  }
  if (!length) {
    deferred[gin339](elem, [animation, 1, 0]);
  }
  deferred[gin340](elem, [animation]);
  return false;
}, animation = deferred[gin322]({
  elem: elem, 
  props: jQuery[gin30]({}, properties), 
  opts: jQuery[gin30](true, {
  specialEasing: {}, 
  easing: jQuery[gin582][gin429]}, options), 
  originalProperties: properties, 
  originalOptions: options, 
  startTime: fxNow || createFxNow(), 
  duration: options[gin586], 
  tweens: [], 
  createTween: function(prop, end) {
  var tween = jQuery[gin580](elem, animation[gin611], prop, end, animation[gin611][gin612][prop] || animation[gin611][gin582]);
  animation[gin609][gin46](tween);
  return tween;
}, 
  stop: function(gotoEnd) {
  var index = 0, length = gotoEnd ? animation[gin609].length : 0;
  if (stopped) {
    return this;
  }
  stopped = true;
  for (; index < length; index++) {
    animation[gin609][index][gin610](1);
  }
  if (gotoEnd) {
    deferred[gin339](elem, [animation, 1, 0]);
    deferred[gin340](elem, [animation, gotoEnd]);
  } else {
    deferred[gin343](elem, [animation, gotoEnd]);
  }
  return this;
}}), props = animation.props;
    propFilter(props, animation[gin611][gin612]);
    for (; index < length; index++) {
      result = Animation[gin607][index][gin7](animation, elem, props, animation[gin611]);
      if (result) {
        if (jQuery[gin32](result[gin613])) {
          jQuery[gin382](animation[gin465], animation[gin611][gin381])[gin613] = jQuery[gin614](result[gin613], result);
        }
        return result;
      }
    }
    jQuery[gin25](props, createTween, animation);
    if (jQuery[gin32](animation[gin611][gin401])) {
      animation[gin611][gin401][gin7](elem, animation);
    }
    animation[gin327](animation[gin611][gin327])[gin323](animation[gin611][gin323], animation[gin611][gin361])[gin324](animation[gin611][gin324])[gin599](animation[gin611][gin599]);
    jQuery[gin380][gin615](jQuery[gin30](tick, {
  elem: elem, 
  anim: animation, 
  queue: animation[gin611][gin381]}));
    return animation;
  }
  jQuery[gin616] = jQuery[gin30](Animation, {
  tweeners: {
  gin83: [function(prop, value) {
  var tween = this[gin617](prop, value);
  adjustCSS(tween[gin465], prop, rcssNum[gin130](value), tween);
  return tween;
}]}, 
  tweener: function(props, callback) {
  if (jQuery[gin32](props)) {
    callback = props;
    props = ["*"];
  } else {
    props = props[gin299](rnothtmlwhite);
  }
  var prop, index = 0, length = props.length;
  for (; index < length; index++) {
    prop = props[index];
    Animation[gin596][prop] = Animation[gin596][prop] || [];
    Animation[gin596][prop][gin217](callback);
  }
}, 
  prefilters: [defaultPrefilter], 
  prefilter: function(callback, prepend) {
  if (prepend) {
    Animation[gin607][gin217](callback);
  } else {
    Animation[gin607][gin46](callback);
  }
}});
  jQuery[gin618] = function(speed, easing, fn) {
  var opt = speed && typeof speed === gin2 ? jQuery[gin30]({}, speed) : {
  complete: fn || !fn && easing || jQuery[gin32](speed) && speed, 
  duration: speed, 
  easing: fn && easing || easing && !jQuery[gin32](easing) && easing};
  if (jQuery[gin380].off) {
    opt[gin586] = 0;
  } else {
    if (typeof opt.duration !== gin42) {
      if (opt[gin586] in jQuery[gin380][gin619]) {
        opt[gin586] = jQuery[gin380][gin619][opt[gin586]];
      } else {
        opt[gin586] = jQuery[gin380][gin619][gin429];
      }
    }
  }
  if (opt[gin381] == null || opt[gin381] === true) {
    opt[gin381] = gin380;
  }
  opt[gin620] = opt[gin361];
  opt[gin361] = function() {
  if (jQuery[gin32](opt[gin620])) {
    opt[gin620][gin7](this);
  }
  if (opt.queue) {
    jQuery[gin383](this, opt[gin381]);
  }
};
  return opt;
};
  jQuery[gin16][gin30]({
  fadeTo: function(speed, to, easing, callback) {
  return this[gin173](isHiddenWithinTree)[gin395](gin554, 0)[gin405]()[gin402]()[gin621]({
  opacity: to}, speed, easing, callback);
}, 
  animate: function(prop, speed, easing, callback) {
  var empty = jQuery[gin369](prop), optall = jQuery[gin618](speed, easing, callback), doAnimation = function() {
  var anim = Animation(this, jQuery[gin30]({}, prop), optall);
  if (empty || dataPriv[gin310](this, gin622)) {
    anim[gin613](true);
  }
};
  doAnimation[gin622] = doAnimation;
  return empty || optall[gin381] === false ? this[gin23](doAnimation) : this[gin381](optall[gin381], doAnimation);
}, 
  stop: function(type, clearQueue, gotoEnd) {
  var stopQueue = function(hooks) {
  var stop = hooks.stop;
  delete hooks.stop;
  stop(gotoEnd);
};
  if (typeof type !== gin43) {
    gotoEnd = clearQueue;
    clearQueue = type;
    type = undefined;
  }
  if (clearQueue && type !== false) {
    this[gin381](type || gin380, []);
  }
  return this[gin23](function() {
  var dequeue = true, index = type != null && type + gin385, timers = jQuery.timers, data = dataPriv[gin310](this);
  if (index) {
    if (data[index] && data[index][gin613]) {
      stopQueue(data[index]);
    }
  } else {
    for (index in data) {
      if (data[index] && data[index][gin613] && rrun[gin136](index)) {
        stopQueue(data[index]);
      }
    }
  }
  for (index = timers[gin19]; index--; ) {
    if (timers[index][gin465] === this && (type == null || timers[index][gin381] === type)) {
      timers[index][gin623][gin613](gotoEnd);
      dequeue = false;
      timers[gin29](index, 1);
    }
  }
  if (dequeue || !gotoEnd) {
    jQuery[gin383](this, type);
  }
});
}, 
  finish: function(type) {
  if (type !== false) {
    type = type || gin380;
  }
  return this[gin23](function() {
  var index, data = dataPriv[gin310](this), queue = data[type + gin381], hooks = data[type + gin385], timers = jQuery.timers, length = queue ? queue.length : 0;
  data[gin622] = true;
  jQuery[gin381](this, type, []);
  if (hooks && hooks[gin613]) {
    hooks[gin613][gin7](this, true);
  }
  for (index = timers[gin19]; index--; ) {
    if (timers[index][gin465] === this && timers[index][gin381] === type) {
      timers[index][gin623][gin613](true);
      timers[gin29](index, 1);
    }
  }
  for (index = 0; index < length; index++) {
    if (queue[index] && queue[index][gin622]) {
      queue[index][gin622][gin7](this);
    }
  }
  delete data.finish;
});
}});
  jQuery[gin23](["toggle", "show", "hide"], function(i, name) {
  var cssFn = jQuery[gin16][name];
  jQuery[gin16][name] = function(speed, easing, callback) {
  return speed == null || typeof speed === gin31 ? cssFn[gin26](this, arguments) : this[gin621](genFx(name, true), speed, easing, callback);
};
});
  jQuery[gin23]({
  slideDown: genFx(gin405), 
  slideUp: genFx(gin406), 
  slideToggle: genFx(gin600), 
  fadeIn: {
  opacity: gin405}, 
  fadeOut: {
  opacity: gin406}, 
  fadeToggle: {
  opacity: gin600}}, function(name, props) {
  jQuery[gin16][name] = function(speed, easing, callback) {
  return this[gin621](props, speed, easing, callback);
};
});
  jQuery[gin624] = [];
  jQuery[gin380][gin595] = function() {
  var timer, i = 0, timers = jQuery.timers;
  fxNow = jQuery[gin49]();
  for (; i < timers[gin19]; i++) {
    timer = timers[i];
    if (!timer() && timers[i] === timer) {
      timers[gin29](i--, 1);
    }
  }
  if (!timers.length) {
    jQuery[gin380][gin613]();
  }
  fxNow = undefined;
};
  jQuery[gin380][gin615] = function(timer) {
  jQuery[gin624][gin46](timer);
  jQuery[gin380][gin401]();
};
  jQuery[gin380][gin594] = 13;
  jQuery[gin380][gin401] = function() {
  if (inProgress) {
    return;
  }
  inProgress = true;
  schedule();
};
  jQuery[gin380][gin613] = function() {
  inProgress = null;
};
  jQuery[gin380][gin619] = {
  slow: 600, 
  fast: 200, 
  _default: 400};
  jQuery[gin16][gin625] = function(time, type) {
  time = jQuery.fx ? jQuery[gin380][gin619][time] || time : time;
  type = type || gin380;
  return this[gin381](type, function(next, hooks) {
  var timeout = gin5[gin345](next, time);
  hooks[gin613] = function() {
  gin5[gin626](timeout);
};
});
};
  (function() {
  var input = document[gin8](gin154), select = document[gin8](gin286), opt = select[gin12](document[gin8](gin271));
  input[gin39] = gin480;
  support[gin627] = input[gin176] !== gin38;
  support[gin628] = opt[gin272];
  input = document[gin8](gin154);
  input[gin176] = gin433;
  input[gin39] = gin432;
  support[gin629] = input[gin176] === gin433;
})();
  var boolHook, attrHandle = jQuery[gin293].attrHandle;
  jQuery[gin16][gin30]({
  attr: function(name, value) {
  return access(this, jQuery[gin220], name, value, arguments[gin19] > 1);
}, 
  removeAttr: function(name) {
  return this[gin23](function() {
  jQuery[gin630](this, name);
});
}});
  jQuery[gin30]({
  attr: function(elem, name, value) {
  var ret, hooks, nType = elem.nodeType;
  if (nType === 3 || nType === 8 || nType === 2) {
    return;
  }
  if (typeof elem.getAttribute === gin6) {
    return jQuery[gin581](elem, name, value);
  }
  if (nType !== 1 || !jQuery[gin296](elem)) {
    hooks = jQuery[gin631][name[gin56]()] || (jQuery[gin293][gin299][gin105][gin136](name) ? boolHook : undefined);
  }
  if (value !== undefined) {
    if (value === null) {
      jQuery[gin630](elem, name);
      return;
    }
    if (hooks && gin368 in hooks && (ret = hooks[gin368](elem, value, name)) !== undefined) {
      return ret;
    }
    elem[gin139](name, value + gin38);
    return value;
  }
  if (hooks && gin310 in hooks && (ret = hooks[gin310](elem, name)) !== null) {
    return ret;
  }
  ret = jQuery[gin174][gin220](elem, name);
  return ret == null ? undefined : ret;
}, 
  attrHooks: {
  type: {
  set: function(elem, value) {
  if (!support.radioValue && value === gin432 && nodeName(elem, gin154)) {
    var val = elem.value;
    elem[gin139](gin39, value);
    if (val) {
      elem[gin176] = val;
    }
    return value;
  }
}}}, 
  removeAttr: function(elem, value) {
  var name, i = 0, attrNames = value && value[gin299](rnothtmlwhite);
  if (attrNames && elem[gin151] === 1) {
    while ((name = attrNames[i++])) {
      elem[gin144](name);
    }
  }
}});
  boolHook = {
  set: function(elem, value, name) {
  if (value === false) {
    jQuery[gin630](elem, name);
  } else {
    elem[gin139](name, name);
  }
  return name;
}};
  jQuery[gin23](jQuery[gin293][gin299][gin105][gin632][gin299](/\w+/g), function(i, name) {
  var getter = attrHandle[name] || jQuery[gin174][gin220];
  attrHandle[name] = function(elem, name, isXML) {
  var ret, handle, lowercaseName = name[gin56]();
  if (!isXML) {
    handle = attrHandle[lowercaseName];
    attrHandle[lowercaseName] = ret;
    ret = getter(elem, name, isXML) != null ? lowercaseName : null;
    attrHandle[lowercaseName] = handle;
  }
  return ret;
};
});
  var rfocusable = /^(?:input|select|textarea|button)$/i, rclickable = /^(?:a|area)$/i;
  jQuery[gin16][gin30]({
  prop: function(name, value) {
  return access(this, jQuery[gin581], name, value, arguments[gin19] > 1);
}, 
  removeProp: function(name) {
  return this[gin23](function() {
  delete this[jQuery[gin633][name] || name];
});
}});
  jQuery[gin30]({
  prop: function(elem, name, value) {
  var ret, hooks, nType = elem.nodeType;
  if (nType === 3 || nType === 8 || nType === 2) {
    return;
  }
  if (nType !== 1 || !jQuery[gin296](elem)) {
    name = jQuery[gin633][name] || name;
    hooks = jQuery[gin584][name];
  }
  if (value !== undefined) {
    if (hooks && gin368 in hooks && (ret = hooks[gin368](elem, value, name)) !== undefined) {
      return ret;
    }
    return (elem[name] = value);
  }
  if (hooks && gin310 in hooks && (ret = hooks[gin310](elem, name)) !== null) {
    return ret;
  }
  return elem[name];
}, 
  propHooks: {
  tabIndex: {
  get: function(elem) {
  var tabindex = jQuery[gin174][gin220](elem, gin634);
  if (tabindex) {
    return parseInt(tabindex, 10);
  }
  if (rfocusable[gin136](elem[gin137]) || rclickable[gin136](elem[gin137]) && elem[gin268]) {
    return 0;
  }
  return -1;
}}}, 
  propFix: {
  gin635: gin636, 
  gin239: gin169}});
  if (!support.optSelected) {
    jQuery[gin584][gin272] = {
  get: function(elem) {
  var parent = elem.parentNode;
  if (parent && parent[gin13]) {
    parent[gin13].selectedIndex;
  }
  return null;
}, 
  set: function(elem) {
  var parent = elem.parentNode;
  if (parent) {
    parent.selectedIndex;
    if (parent.parentNode) {
      parent[gin13].selectedIndex;
    }
  }
}};
  }
  jQuery[gin23](["tabIndex", "readOnly", "maxLength", "cellSpacing", "cellPadding", "rowSpan", "colSpan", "useMap", "frameBorder", "contentEditable"], function() {
  jQuery[gin633][this[gin56]()] = this;
});
  function stripAndCollapse(value) {
    var tokens = value[gin299](rnothtmlwhite) || [];
    return tokens[gin141](gin53);
  }
  function getClass(elem) {
    return elem[gin138] && elem[gin138](gin239) || gin38;
  }
  jQuery[gin16][gin30]({
  addClass: function(value) {
  var classes, elem, cur, curValue, clazz, j, finalValue, i = 0;
  if (jQuery[gin32](value)) {
    return this[gin23](function(j) {
  jQuery(this)[gin647](value[gin7](this, j, getClass(this)));
});
  }
  if (typeof value === gin43 && value) {
    classes = value[gin299](rnothtmlwhite) || [];
    while ((elem = this[i++])) {
      curValue = getClass(elem);
      cur = elem[gin151] === 1 && (gin53 + stripAndCollapse(curValue) + gin53);
      if (cur) {
        j = 0;
        while ((clazz = classes[j++])) {
          if (cur[gin235](gin53 + clazz + gin53) < 0) {
            cur += clazz + gin53;
          }
        }
        finalValue = stripAndCollapse(cur);
        if (curValue !== finalValue) {
          elem[gin139](gin239, finalValue);
        }
      }
    }
  }
  return this;
}, 
  removeClass: function(value) {
  var classes, elem, cur, curValue, clazz, j, finalValue, i = 0;
  if (jQuery[gin32](value)) {
    return this[gin23](function(j) {
  jQuery(this)[gin648](value[gin7](this, j, getClass(this)));
});
  }
  if (!arguments.length) {
    return this[gin220](gin239, gin38);
  }
  if (typeof value === gin43 && value) {
    classes = value[gin299](rnothtmlwhite) || [];
    while ((elem = this[i++])) {
      curValue = getClass(elem);
      cur = elem[gin151] === 1 && (gin53 + stripAndCollapse(curValue) + gin53);
      if (cur) {
        j = 0;
        while ((clazz = classes[j++])) {
          while (cur[gin235](gin53 + clazz + gin53) > -1) {
            cur = cur[gin37](gin53 + clazz + gin53, gin53);
          }
        }
        finalValue = stripAndCollapse(cur);
        if (curValue !== finalValue) {
          elem[gin139](gin239, finalValue);
        }
      }
    }
  }
  return this;
}, 
  toggleClass: function(value, stateVal) {
  var type = typeof value;
  if (typeof stateVal === gin31 && type === gin43) {
    return stateVal ? this[gin647](value) : this[gin648](value);
  }
  if (jQuery[gin32](value)) {
    return this[gin23](function(i) {
  jQuery(this)[gin649](value[gin7](this, i, getClass(this), stateVal), stateVal);
});
  }
  return this[gin23](function() {
  var className, i, self, classNames;
  if (type === gin43) {
    i = 0;
    self = jQuery(this);
    classNames = value[gin299](rnothtmlwhite) || [];
    while ((className = classNames[i++])) {
      if (self[gin650](className)) {
        self[gin648](className);
      } else {
        self[gin647](className);
      }
    }
  } else if (value === undefined || type === gin31) {
    className = getClass(this);
    if (className) {
      dataPriv[gin368](this, gin651, className);
    }
    if (this.setAttribute) {
      this[gin139](gin239, className || value === false ? "" : dataPriv[gin310](this, gin651) || gin38);
    }
  }
});
}, 
  hasClass: function(selector) {
  var className, elem, i = 0;
  className = gin53 + selector + gin53;
  while ((elem = this[i++])) {
    if (elem[gin151] === 1 && (gin53 + stripAndCollapse(getClass(elem)) + gin53)[gin235](className) > -1) {
      return true;
    }
  }
  return false;
}});
  var rreturn = /\r/g;
  jQuery[gin16][gin30]({
  val: function(value) {
  var hooks, ret, isFunction, elem = this[0];
  if (!arguments.length) {
    if (elem) {
      hooks = jQuery[gin652][elem[gin39]] || jQuery[gin652][elem[gin137][gin56]()];
      if (hooks && gin310 in hooks && (ret = hooks[gin310](elem, gin176)) !== undefined) {
        return ret;
      }
      ret = elem[gin176];
      if (typeof ret === gin43) {
        return ret[gin37](rreturn, gin38);
      }
      return ret == null ? "" : ret;
    }
    return;
  }
  isFunction = jQuery[gin32](value);
  return this[gin23](function(i) {
  var val;
  if (this[gin151] !== 1) {
    return;
  }
  if (isFunction) {
    val = value[gin7](this, i, jQuery(this)[gin653]());
  } else {
    val = value;
  }
  if (val == null) {
    val = gin38;
  } else if (typeof val === gin42) {
    val += gin38;
  } else if (Array[gin34](val)) {
    val = jQuery[gin25](val, function(value) {
  return value == null ? "" : value + gin38;
});
  }
  hooks = jQuery[gin652][this[gin39]] || jQuery[gin652][this[gin137][gin56]()];
  if (!hooks || !(gin368 in hooks) || hooks[gin368](this, val, gin176) === undefined) {
    this[gin176] = val;
  }
});
}});
  jQuery[gin30]({
  valHooks: {
  option: {
  get: function(elem) {
  var val = jQuery[gin174][gin220](elem, gin176);
  return val != null ? val : stripAndCollapse(jQuery[gin10](elem));
}}, 
  select: {
  get: function(elem) {
  var value, option, i, options = elem.options, index = elem.selectedIndex, one = elem[gin39] === gin654, values = one ? null : [], max = one ? index + 1 : options.length;
  if (index < 0) {
    i = max;
  } else {
    i = one ? index : 0;
  }
  for (; i < max; i++) {
    option = options[i];
    if ((option[gin272] || i === index) && !option.disabled && (!option[gin13].disabled || !nodeName(option[gin13], gin417))) {
      value = jQuery(option)[gin653]();
      if (one) {
        return value;
      }
      values[gin46](value);
    }
  }
  return values;
}, 
  set: function(elem, value) {
  var optionSet, option, options = elem.options, values = jQuery[gin307](value), i = options.length;
  while (i--) {
    option = options[i];
    if (option[gin272] = jQuery[gin320](jQuery[gin652][gin271][gin310](option), values) > -1) {
      optionSet = true;
    }
  }
  if (!optionSet) {
    elem[gin655] = -1;
  }
  return values;
}}}});
  jQuery[gin23](["radio", "checkbox"], function() {
  jQuery[gin652][this] = {
  set: function(elem, value) {
  if (Array[gin34](value)) {
    return (elem[gin270] = jQuery[gin320](jQuery(elem)[gin653](), value) > -1);
  }
}};
  if (!support.checkOn) {
    jQuery[gin652][this][gin310] = function(elem) {
  return elem[gin138](gin176) === null ? "on" : elem.value;
};
  }
});
  var rfocusMorph = /^(?:focusinfocus|focusoutblur)$/;
  jQuery[gin30](jQuery[gin439], {
  trigger: function(event, data, elem, onlyHandlers) {
  var i, cur, tmp, bubbleType, ontype, handle, special, eventPath = [elem || document], type = hasOwn[gin7](event, gin39) ? event.type : event, namespaces = hasOwn[gin7](event, gin454) ? event[gin454][gin52](gin445) : [];
  cur = tmp = elem = elem || document;
  if (elem[gin151] === 3 || elem[gin151] === 8) {
    return;
  }
  if (rfocusMorph[gin136](type + jQuery[gin439][gin443])) {
    return;
  }
  if (type[gin235](gin445) > -1) {
    namespaces = type[gin52](gin445);
    type = namespaces[gin147]();
    namespaces[gin28]();
  }
  ontype = type[gin235](gin294) < 0 && gin656 + type;
  event = event[jQuery[gin363]] ? event : new jQuery.Event(type, typeof event === gin2 && event);
  event[gin657] = onlyHandlers ? 2 : 3;
  event[gin454] = namespaces[gin141](gin445);
  event[gin467] = event.namespace ? new RegExp(gin450 + namespaces[gin141](gin451) + gin452) : null;
  event[gin470] = undefined;
  if (!event.target) {
    event[gin261] = elem;
  }
  data = data == null ? [event] : jQuery[gin307](data, [event]);
  special = jQuery[gin439][gin446][type] || {};
  if (!onlyHandlers && special[gin658] && special[gin658][gin26](elem, data) === false) {
    return;
  }
  if (!onlyHandlers && !special.noBubble && !jQuery[gin57](elem)) {
    bubbleType = special[gin659] || type;
    if (!rfocusMorph[gin136](bubbleType + type)) {
      cur = cur[gin13];
    }
    for (; cur; cur = cur[gin13]) {
      eventPath[gin46](cur);
      tmp = cur;
    }
    if (tmp === (elem[gin129] || document)) {
      eventPath[gin46](tmp[gin162] || tmp[gin660] || gin5);
    }
  }
  i = 0;
  while ((cur = eventPath[i++]) && !event[gin463]()) {
    event[gin39] = i > 1 ? bubbleType : special[gin661] || type;
    handle = (dataPriv[gin310](cur, gin441) || {})[event[gin39]] && dataPriv[gin310](cur, gin442);
    if (handle) {
      handle[gin26](cur, data);
    }
    handle = ontype && cur[ontype];
    if (handle && handle[gin26] && acceptData(cur)) {
      event[gin470] = handle[gin26](cur, data);
      if (event[gin470] === false) {
        event[gin471]();
      }
    }
  }
  event[gin39] = type;
  if (!onlyHandlers && !event[gin483]()) {
    if ((!special._default || special[gin429][gin26](eventPath[gin249](), data) === false) && acceptData(elem)) {
      if (ontype && jQuery[gin32](elem[type]) && !jQuery[gin57](elem)) {
        tmp = elem[ontype];
        if (tmp) {
          elem[ontype] = null;
        }
        jQuery[gin439][gin443] = type;
        elem[type]();
        jQuery[gin439][gin443] = undefined;
        if (tmp) {
          elem[ontype] = tmp;
        }
      }
    }
  }
  return event.result;
}, 
  simulate: function(type, elem, event) {
  var e = jQuery[gin30](new jQuery.Event(), event, {
  type: type, 
  isSimulated: true});
  jQuery[gin439][gin658](e, null, elem);
}});
  jQuery[gin16][gin30]({
  trigger: function(type, data) {
  return this[gin23](function() {
  jQuery[gin439][gin658](type, data, this);
});
}, 
  triggerHandler: function(type, data) {
  var elem = this[0];
  if (elem) {
    return jQuery[gin439][gin658](type, data, elem, true);
  }
}});
  jQuery[gin23]((gin662 + gin663 + gin664)[gin52](gin53), function(i, name) {
  jQuery[gin16][name] = function(data, fn) {
  return arguments[gin19] > 0 ? this[gin656](name, null, data, fn) : this[gin658](name);
};
});
  jQuery[gin16][gin30]({
  hover: function(fnOver, fnOut) {
  return this[gin665](fnOver)[gin666](fnOut || fnOver);
}});
  support[gin477] = gin667 in gin5;
  if (!support.focusin) {
    jQuery[gin23]({
  focus: gin477, 
  blur: gin479}, function(orig, fix) {
  var handler = function(event) {
  jQuery[gin439][gin668](fix, event[gin261], jQuery[gin439][gin459](event));
};
  jQuery[gin439][gin446][fix] = {
  setup: function() {
  var doc = this[gin129] || this, attaches = dataPriv[gin377](doc, fix);
  if (!attaches) {
    doc[gin164](orig, handler, true);
  }
  dataPriv[gin377](doc, fix, (attaches || 0) + 1);
}, 
  teardown: function() {
  var doc = this[gin129] || this, attaches = dataPriv[gin377](doc, fix) - 1;
  if (!attaches) {
    doc[gin357](orig, handler, true);
    dataPriv[gin378](doc, fix);
  } else {
    dataPriv[gin377](doc, fix, attaches);
  }
}};
});
  }
  var location = gin5.location;
  var nonce = jQuery[gin49]();
  var rquery = (/\?/);
  jQuery[gin669] = function(data) {
  var xml;
  if (!data || typeof data !== gin43) {
    return null;
  }
  try {
    xml = (new gin5.DOMParser())[gin670](data, gin671);
  }  catch (e) {
  xml = undefined;
}
  if (!xml || xml[gin133](gin672)[gin19]) {
    jQuery[gin223](gin673 + data);
  }
  return xml;
};
  var rbracket = /\[\]$/, rCRLF = /\r?\n/g, rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i, rsubmittable = /^(?:input|select|textarea|keygen)/i;
  function buildParams(prefix, obj, traditional, add) {
    var name;
    if (Array[gin34](obj)) {
      jQuery[gin23](obj, function(i, v) {
  if (traditional || rbracket[gin136](prefix)) {
    add(prefix, v);
  } else {
    buildParams(prefix + gin674 + (typeof v === gin2 && v != null ? i : "") + gin55, v, traditional, add);
  }
});
    } else if (!traditional && jQuery[gin39](obj) === gin2) {
      for (name in obj) {
        buildParams(prefix + gin674 + name + gin55, obj[name], traditional, add);
      }
    } else {
      add(prefix, obj);
    }
  }
  jQuery[gin675] = function(a, traditional) {
  var prefix, s = [], add = function(key, valueOrFunction) {
  var value = jQuery[gin32](valueOrFunction) ? valueOrFunction() : valueOrFunction;
  s[s[gin19]] = encodeURIComponent(key) + gin86 + encodeURIComponent(value == null ? "" : value);
};
  if (Array[gin34](a) || (a[gin305] && !jQuery[gin33](a))) {
    jQuery[gin23](a, function() {
  add(this[gin198], this[gin176]);
});
  } else {
    for (prefix in a) {
      buildParams(prefix, a[prefix], traditional, add);
    }
  }
  return s[gin141](gin676);
};
  jQuery[gin16][gin30]({
  serialize: function() {
  return jQuery[gin675](this[gin677]());
}, 
  serializeArray: function() {
  return this[gin25](function() {
  var elements = jQuery[gin581](this, gin678);
  return elements ? jQuery[gin307](elements) : this;
})[gin173](function() {
  var type = this.type;
  return this[gin198] && !jQuery(this)[gin298](gin203) && rsubmittable[gin136](this[gin137]) && !rsubmitterTypes[gin136](type) && (this[gin270] || !rcheckableType[gin136](type));
})[gin25](function(i, elem) {
  var val = jQuery(this)[gin653]();
  if (val == null) {
    return null;
  }
  if (Array[gin34](val)) {
    return jQuery[gin25](val, function(val) {
  return {
  name: elem[gin198], 
  value: val[gin37](rCRLF, gin679)};
});
  }
  return {
  name: elem[gin198], 
  value: val[gin37](rCRLF, gin679)};
})[gin310]();
}});
  var r20 = /%20/g, rhash = /#.*$/, rantiCache = /([?&])_=[^&]*/, rheaders = /^(.*?):[ \t]*([^\r\n]*)$/mg, rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/, rnoContent = /^(?:GET|HEAD)$/, rprotocol = /^\/\//, prefilters = {}, transports = {}, allTypes = gin680[gin47](gin83), originAnchor = document[gin8](gin481);
  originAnchor[gin268] = location[gin268];
  function addToPrefiltersOrTransports(structure) {
    return function(dataTypeExpression, func) {
  if (typeof dataTypeExpression !== gin43) {
    func = dataTypeExpression;
    dataTypeExpression = gin83;
  }
  var dataType, i = 0, dataTypes = dataTypeExpression[gin56]()[gin299](rnothtmlwhite) || [];
  if (jQuery[gin32](func)) {
    while ((dataType = dataTypes[i++])) {
      if (dataType[0] === gin77) {
        dataType = dataType[gin120](1) || gin83;
        (structure[dataType] = structure[dataType] || [])[gin217](func);
      } else {
        (structure[dataType] = structure[dataType] || [])[gin46](func);
      }
    }
  }
};
  }
  function inspectPrefiltersOrTransports(structure, options, originalOptions, jqXHR) {
    var inspected = {}, seekingTransport = (structure === transports);
    function inspect(dataType) {
      var selected;
      inspected[dataType] = true;
      jQuery[gin23](structure[dataType] || [], function(_, prefilterOrFactory) {
  var dataTypeOrTransport = prefilterOrFactory(options, originalOptions, jqXHR);
  if (typeof dataTypeOrTransport === gin43 && !seekingTransport && !inspected[dataTypeOrTransport]) {
    options[gin681][gin217](dataTypeOrTransport);
    inspect(dataTypeOrTransport);
    return false;
  } else if (seekingTransport) {
    return !(selected = dataTypeOrTransport);
  }
});
      return selected;
    }
    return inspect(options[gin681][0]) || !inspected[gin83] && inspect(gin83);
  }
  function ajaxExtend(target, src) {
    var key, deep, flatOptions = jQuery[gin682][gin683] || {};
    for (key in src) {
      if (src[key] !== undefined) {
        (flatOptions[key] ? target : (deep || (deep = {})))[key] = src[key];
      }
    }
    if (deep) {
      jQuery[gin30](true, target, deep);
    }
    return target;
  }
  function ajaxHandleResponses(s, jqXHR, responses) {
    var ct, type, finalDataType, firstDataType, contents = s.contents, dataTypes = s.dataTypes;
    while (dataTypes[0] === gin83) {
      dataTypes[gin147]();
      if (ct === undefined) {
        ct = s[gin684] || jqXHR[gin685](gin686);
      }
    }
    if (ct) {
      for (type in contents) {
        if (contents[type] && contents[type][gin136](ct)) {
          dataTypes[gin217](type);
          break;
        }
      }
    }
    if (dataTypes[0] in responses) {
      finalDataType = dataTypes[0];
    } else {
      for (type in responses) {
        if (!dataTypes[0] || s[gin687][type + gin53 + dataTypes[0]]) {
          finalDataType = type;
          break;
        }
        if (!firstDataType) {
          firstDataType = type;
        }
      }
      finalDataType = finalDataType || firstDataType;
    }
    if (finalDataType) {
      if (finalDataType !== dataTypes[0]) {
        dataTypes[gin217](finalDataType);
      }
      return responses[finalDataType];
    }
  }
  function ajaxConvert(s, response, jqXHR, isSuccess) {
    var conv2, current, conv, tmp, prev, converters = {}, dataTypes = s[gin681][gin120]();
    if (dataTypes[1]) {
      for (conv in s.converters) {
        converters[conv[gin56]()] = s[gin687][conv];
      }
    }
    current = dataTypes[gin147]();
    while (current) {
      if (s[gin688][current]) {
        jqXHR[s[gin688][current]] = response;
      }
      if (!prev && isSuccess && s[gin689]) {
        response = s[gin689](response, s[gin690]);
      }
      prev = current;
      current = dataTypes[gin147]();
      if (current) {
        if (current === gin83) {
          current = prev;
        } else if (prev !== gin83 && prev !== current) {
          conv = converters[prev + gin53 + current] || converters[gin691 + current];
          if (!conv) {
            for (conv2 in converters) {
              tmp = conv2[gin52](gin53);
              if (tmp[1] === current) {
                conv = converters[prev + gin53 + tmp[0]] || converters[gin691 + tmp[0]];
                if (conv) {
                  if (conv === true) {
                    conv = converters[conv2];
                  } else if (converters[conv2] !== true) {
                    current = tmp[0];
                    dataTypes[gin217](tmp[1]);
                  }
                  break;
                }
              }
            }
          }
          if (conv !== true) {
            if (conv && s[gin692]) {
              response = conv(response);
            } else {
              try {
                response = conv(response);
              }              catch (e) {
  return {
  state: gin672, 
  error: conv ? e : gin693 + prev + gin694 + current};
}
            }
          }
        }
      }
    }
    return {
  state: gin695, 
  data: response};
  }
  jQuery[gin30]({
  active: 0, 
  lastModified: {}, 
  etag: {}, 
  ajaxSettings: {
  url: location[gin268], 
  type: gin696, 
  isLocal: rlocalProtocol[gin136](location[gin697]), 
  global: true, 
  processData: true, 
  async: true, 
  contentType: gin698, 
  accepts: {
  gin83: allTypes, 
  text: gin699, 
  html: gin700, 
  xml: gin701, 
  json: gin702}, 
  contents: {
  xml: /\bxml\b/, 
  html: /\bhtml/, 
  json: /\bjson\b/}, 
  responseFields: {
  xml: gin703, 
  text: gin704, 
  json: gin705}, 
  converters: {
  gin706: String, 
  gin707: true, 
  gin708: JSON[gin373], 
  gin709: jQuery[gin669]}, 
  flatOptions: {
  url: true, 
  context: true}}, 
  ajaxSetup: function(target, settings) {
  return settings ? ajaxExtend(ajaxExtend(target, jQuery[gin682]), settings) : ajaxExtend(jQuery[gin682], target);
}, 
  ajaxPrefilter: addToPrefiltersOrTransports(prefilters), 
  ajaxTransport: addToPrefiltersOrTransports(transports), 
  ajax: function(url, options) {
  if (typeof url === gin2) {
    options = url;
    url = undefined;
  }
  options = options || {};
  var transport, cacheURL, responseHeadersString, responseHeaders, timeoutTimer, urlAnchor, completed, fireGlobals, i, uncached, s = jQuery[gin710]({}, options), callbackContext = s[gin711] || s, globalEventContext = s[gin711] && (callbackContext[gin151] || callbackContext[gin305]) ? jQuery(callbackContext) : jQuery.event, deferred = jQuery[gin336](), completeDeferred = jQuery[gin317](gin330), statusCode = s[gin712] || {}, requestHeaders = {}, requestHeadersNames = {}, strAbort = "canceled", jqXHR = {
  readyState: 0, 
  getResponseHeader: function(key) {
  var match;
  if (completed) {
    if (!responseHeaders) {
      responseHeaders = {};
      while ((match = rheaders[gin130](responseHeadersString))) {
        responseHeaders[match[1][gin56]()] = match[2];
      }
    }
    match = responseHeaders[key[gin56]()];
  }
  return match == null ? null : match;
}, 
  getAllResponseHeaders: function() {
  return completed ? responseHeadersString : null;
}, 
  setRequestHeader: function(name, value) {
  if (completed == null) {
    name = requestHeadersNames[name[gin56]()] = requestHeadersNames[name[gin56]()] || name;
    requestHeaders[name] = value;
  }
  return this;
}, 
  overrideMimeType: function(type) {
  if (completed == null) {
    s[gin684] = type;
  }
  return this;
}, 
  statusCode: function(map) {
  var code;
  if (map) {
    if (completed) {
      jqXHR[gin599](map[jqXHR[gin714]]);
    } else {
      for (code in map) {
        statusCode[code] = [statusCode[code], map[code]];
      }
    }
  }
  return this;
}, 
  abort: function(statusText) {
  var finalText = statusText || strAbort;
  if (transport) {
    transport[gin715](finalText);
  }
  done(0, finalText);
  return this;
}};
  deferred[gin322](jqXHR);
  s[gin716] = ((url || s[gin716] || location[gin268]) + gin38)[gin37](rprotocol, location[gin697] + gin717);
  s[gin39] = options[gin718] || options[gin39] || s[gin718] || s[gin39];
  s[gin681] = (s[gin690] || gin83)[gin56]()[gin299](rnothtmlwhite) || [""];
  if (s[gin719] == null) {
    urlAnchor = document[gin8](gin481);
    try {
      urlAnchor[gin268] = s[gin716];
      urlAnchor[gin268] = urlAnchor[gin268];
      s[gin719] = originAnchor[gin697] + gin717 + originAnchor[gin720] !== urlAnchor[gin697] + gin717 + urlAnchor[gin720];
    }    catch (e) {
  s[gin719] = true;
}
  }
  if (s[gin469] && s[gin721] && typeof s.data !== gin43) {
    s[gin469] = jQuery[gin675](s[gin469], s[gin722]);
  }
  inspectPrefiltersOrTransports(prefilters, s, options, jqXHR);
  if (completed) {
    return jqXHR;
  }
  fireGlobals = jQuery[gin439] && s[gin449];
  if (fireGlobals && jQuery.active++ === 0) {
    jQuery[gin439][gin658](gin723);
  }
  s[gin39] = s[gin39][gin17]();
  s[gin724] = !rnoContent[gin136](s[gin39]);
  cacheURL = s[gin716][gin37](rhash, gin38);
  if (!s.hasContent) {
    uncached = s[gin716][gin120](cacheURL[gin19]);
    if (s.data) {
      cacheURL += (rquery[gin136](cacheURL) ? "&" : "?") + s[gin469];
      delete s.data;
    }
    if (s[gin366] === false) {
      cacheURL = cacheURL[gin37](rantiCache, gin145);
      uncached = (rquery[gin136](cacheURL) ? "&" : "?") + gin726 + (nonce++) + uncached;
    }
    s[gin716] = cacheURL + uncached;
  } else if (s[gin469] && s[gin721] && (s[gin727] || gin38)[gin235](gin728) === 0) {
    s[gin469] = s[gin469][gin37](r20, gin77);
  }
  if (s.ifModified) {
    if (jQuery[gin729][cacheURL]) {
      jqXHR[gin730](gin731, jQuery[gin729][cacheURL]);
    }
    if (jQuery[gin732][cacheURL]) {
      jqXHR[gin730](gin733, jQuery[gin732][cacheURL]);
    }
  }
  if (s[gin469] && s[gin724] && s[gin727] !== false || options[gin727]) {
    jqXHR[gin730](gin686, s[gin727]);
  }
  jqXHR[gin730](gin734, s[gin681][0] && s[gin735][s[gin681][0]] ? s[gin735][s[gin681][0]] + (s[gin681][0] !== gin83 ? gin736 + allTypes + gin737 : "") : s[gin735][gin83]);
  for (i in s.headers) {
    jqXHR[gin730](i, s[gin738][i]);
  }
  if (s[gin739] && (s[gin739][gin7](callbackContext, jqXHR, s) === false || completed)) {
    return jqXHR[gin715]();
  }
  strAbort = gin715;
  completeDeferred[gin311](s[gin361]);
  jqXHR[gin323](s[gin695]);
  jqXHR[gin324](s[gin223]);
  transport = inspectPrefiltersOrTransports(transports, s, options, jqXHR);
  if (!transport) {
    done(-1, gin740);
  } else {
    jqXHR[gin360] = 1;
    if (fireGlobals) {
      globalEventContext[gin658](gin741, [jqXHR, s]);
    }
    if (completed) {
      return jqXHR;
    }
    if (s[gin742] && s[gin743] > 0) {
      timeoutTimer = gin5[gin345](function() {
  jqXHR[gin715](gin743);
}, s[gin743]);
    }
    try {
      completed = false;
      transport[gin744](requestHeaders, done);
    }    catch (e) {
  if (completed) {
    throw e;
  }
  done(-1, e);
}
  }
  function done(status, nativeStatusText, responses, headers) {
    var isSuccess, success, error, response, modified, statusText = nativeStatusText;
    if (completed) {
      return;
    }
    completed = true;
    if (timeoutTimer) {
      gin5[gin626](timeoutTimer);
    }
    transport = undefined;
    responseHeadersString = headers || gin38;
    jqXHR[gin360] = status > 0 ? 4 : 0;
    isSuccess = status >= 200 && status < 300 || status === 304;
    if (responses) {
      response = ajaxHandleResponses(s, jqXHR, responses);
    }
    response = ajaxConvert(s, response, jqXHR, isSuccess);
    if (isSuccess) {
      if (s.ifModified) {
        modified = jqXHR[gin685](gin745);
        if (modified) {
          jQuery[gin729][cacheURL] = modified;
        }
        modified = jqXHR[gin685](gin732);
        if (modified) {
          jQuery[gin732][cacheURL] = modified;
        }
      }
      if (status === 204 || s[gin39] === gin746) {
        statusText = gin747;
      } else if (status === 304) {
        statusText = gin748;
      } else {
        statusText = response[gin349];
        success = response[gin469];
        error = response[gin223];
        isSuccess = !error;
      }
    } else {
      error = statusText;
      if (status || !statusText) {
        statusText = gin223;
        if (status < 0) {
          status = 0;
        }
      }
    }
    jqXHR[gin714] = status;
    jqXHR[gin749] = (nativeStatusText || statusText) + gin38;
    if (isSuccess) {
      deferred[gin340](callbackContext, [success, statusText, jqXHR]);
    } else {
      deferred[gin343](callbackContext, [jqXHR, statusText, error]);
    }
    jqXHR[gin712](statusCode);
    statusCode = undefined;
    if (fireGlobals) {
      globalEventContext[gin658](isSuccess ? "ajaxSuccess" : "ajaxError", [jqXHR, s, isSuccess ? success : error]);
    }
    completeDeferred[gin321](callbackContext, [jqXHR, statusText]);
    if (fireGlobals) {
      globalEventContext[gin658](gin752, [jqXHR, s]);
      if (!(--jQuery.active)) {
        jQuery[gin439][gin658](gin753);
      }
    }
  }
  return jqXHR;
}, 
  getJSON: function(url, data, callback) {
  return jQuery[gin310](url, data, callback, gin754);
}, 
  getScript: function(url, callback) {
  return jQuery[gin310](url, undefined, callback, gin9);
}});
  jQuery[gin23](["get", "post"], function(i, method) {
  jQuery[method] = function(url, data, callback, type) {
  if (jQuery[gin32](data)) {
    type = type || callback;
    callback = data;
    data = undefined;
  }
  return jQuery[gin756](jQuery[gin30]({
  url: url, 
  type: method, 
  dataType: type, 
  data: data, 
  success: callback}, jQuery[gin33](url) && url));
};
});
  jQuery[gin504] = function(url) {
  return jQuery[gin756]({
  url: url, 
  type: gin696, 
  dataType: gin9, 
  cache: true, 
  async: false, 
  global: false, 
  gin692: true});
};
  jQuery[gin16][gin30]({
  wrapAll: function(html) {
  var wrap;
  if (this[0]) {
    if (jQuery[gin32](html)) {
      html = html[gin7](this[0]);
    }
    wrap = jQuery(html, this[0][gin129])[gin27](0)[gin503](true);
    if (this[0].parentNode) {
      wrap[gin508](this[0]);
    }
    wrap[gin25](function() {
  var elem = this;
  while (elem.firstElementChild) {
    elem = elem[gin757];
  }
  return elem;
})[gin509](this);
  }
  return this;
}, 
  wrapInner: function(html) {
  if (jQuery[gin32](html)) {
    return this[gin23](function(i) {
  jQuery(this)[gin758](html[gin7](this, i));
});
  }
  return this[gin23](function() {
  var self = jQuery(this), contents = self[gin759]();
  if (contents.length) {
    contents[gin760](html);
  } else {
    self[gin509](html);
  }
});
}, 
  wrap: function(html) {
  var isFunction = jQuery[gin32](html);
  return this[gin23](function(i) {
  jQuery(this)[gin760](isFunction ? html[gin7](this, i) : html);
});
}, 
  unwrap: function(selector) {
  this[gin274](selector)[gin254](gin403)[gin23](function() {
  jQuery(this)[gin514](this[gin128]);
});
  return this;
}});
  jQuery[gin293][gin250][gin197] = function(elem) {
  return !jQuery[gin293][gin250][gin761](elem);
};
  jQuery[gin293][gin250][gin761] = function(elem) {
  return !!(elem[gin762] || elem[gin763] || elem[gin576]()[gin19]);
};
  jQuery[gin682][gin764] = function() {
  try {
    return new gin5.XMLHttpRequest();
  }  catch (e) {
}
};
  var xhrSuccessStatus = {
  0: 200, 
  1223: 204}, xhrSupported = jQuery[gin682][gin764]();
  support[gin765] = !!xhrSupported && (gin766 in xhrSupported);
  support[gin756] = xhrSupported = !!xhrSupported;
  jQuery[gin767](function(options) {
  var callback, errorCallback;
  if (support[gin765] || xhrSupported && !options.crossDomain) {
    return {
  send: function(headers, complete) {
  var i, xhr = options[gin764]();
  xhr[gin768](options[gin39], options[gin716], options[gin742], options[gin769], options[gin770]);
  if (options.xhrFields) {
    for (i in options.xhrFields) {
      xhr[i] = options[gin771][i];
    }
  }
  if (options[gin684] && xhr[gin772]) {
    xhr[gin772](options[gin684]);
  }
  if (!options.crossDomain && !headers[gin773]) {
    headers[gin773] = gin774;
  }
  for (i in headers) {
    xhr[gin730](i, headers[i]);
  }
  callback = function(type) {
  return function() {
  if (callback) {
    callback = errorCallback = xhr[gin775] = xhr[gin776] = xhr[gin777] = xhr[gin778] = null;
    if (type === gin715) {
      xhr[gin715]();
    } else if (type === gin223) {
      if (typeof xhr.status !== gin42) {
        complete(0, gin223);
      } else {
        complete(xhr[gin714], xhr[gin749]);
      }
    } else {
      complete(xhrSuccessStatus[xhr[gin714]] || xhr[gin714], xhr[gin749], (xhr[gin779] || gin10) !== gin10 || typeof xhr.responseText !== gin43 ? {
  binary: xhr[gin780]} : {
  text: xhr[gin704]}, xhr[gin781]());
    }
  }
};
};
  xhr[gin775] = callback();
  errorCallback = xhr[gin776] = callback(gin223);
  if (xhr[gin777] !== undefined) {
    xhr[gin777] = errorCallback;
  } else {
    xhr[gin778] = function() {
  if (xhr[gin360] === 4) {
    gin5[gin345](function() {
  if (callback) {
    errorCallback();
  }
});
  }
};
  }
  callback = callback(gin715);
  try {
    xhr[gin744](options[gin724] && options[gin469] || null);
  }  catch (e) {
  if (callback) {
    throw e;
  }
}
}, 
  abort: function() {
  if (callback) {
    callback();
  }
}};
  }
});
  jQuery[gin782](function(s) {
  if (s.crossDomain) {
    s[gin759][gin9] = false;
  }
});
  jQuery[gin710]({
  accepts: {
  script: gin783 + gin784}, 
  contents: {
  script: /\b(?:java|ecma)script\b/}, 
  converters: {
  gin785: function(text) {
  jQuery[gin425](text);
  return text;
}}});
  jQuery[gin782](gin9, function(s) {
  if (s[gin366] === undefined) {
    s[gin366] = false;
  }
  if (s.crossDomain) {
    s[gin39] = gin696;
  }
});
  jQuery[gin767](gin9, function(s) {
  if (s.crossDomain) {
    var script, callback;
    return {
  send: function(_, complete) {
  script = jQuery(gin786)[gin581]({
  charset: s[gin787], 
  src: s[gin716]})[gin656](gin788, callback = function(evt) {
  script[gin378]();
  callback = null;
  if (evt) {
    complete(evt[gin39] === gin223 ? 404 : 200, evt[gin39]);
  }
});
  document[gin11][gin12](script[0]);
}, 
  abort: function() {
  if (callback) {
    callback();
  }
}};
  }
});
  var oldCallbacks = [], rjsonp = /(=)\?(?=&|$)|\?\?/;
  jQuery[gin710]({
  jsonp: gin789, 
  jsonpCallback: function() {
  var callback = oldCallbacks[gin249]() || (jQuery[gin363] + gin790 + (nonce++));
  this[callback] = true;
  return callback;
}});
  jQuery[gin782](gin791, function(s, originalSettings, jqXHR) {
  var callbackName, overwritten, responseContainer, jsonProp = s[gin792] !== false && (rjsonp[gin136](s[gin716]) ? "url" : typeof s.data === gin43 && (s[gin727] || gin38)[gin235](gin728) === 0 && rjsonp[gin136](s[gin469]) && gin469);
  if (jsonProp || s[gin681][0] === gin792) {
    callbackName = s[gin793] = jQuery[gin32](s[gin793]) ? s[gin793]() : s.jsonpCallback;
    if (jsonProp) {
      s[jsonProp] = s[jsonProp][gin37](rjsonp, gin145 + callbackName);
    } else if (s[gin792] !== false) {
      s[gin716] += (rquery[gin136](s[gin716]) ? "&" : "?") + s[gin792] + gin86 + callbackName;
    }
    s[gin687][gin794] = function() {
  if (!responseContainer) {
    jQuery[gin223](callbackName + gin795);
  }
  return responseContainer[0];
};
    s[gin681][0] = gin754;
    overwritten = gin5[callbackName];
    gin5[callbackName] = function() {
  responseContainer = arguments;
};
    jqXHR[gin599](function() {
  if (overwritten === undefined) {
    jQuery(gin5)[gin796](callbackName);
  } else {
    gin5[callbackName] = overwritten;
  }
  if (s[callbackName]) {
    s[gin793] = originalSettings[gin793];
    oldCallbacks[gin46](callbackName);
  }
  if (responseContainer && jQuery[gin32](overwritten)) {
    overwritten(responseContainer[0]);
  }
  responseContainer = overwritten = undefined;
});
    return "script";
  }
});
  support[gin797] = (function() {
  var body = document[gin798][gin797](gin38).body;
  body[gin177] = gin799;
  return body[gin128][gin19] === 2;
})();
  jQuery[gin304] = function(data, context, keepScripts) {
  if (typeof data !== gin43) {
    return [];
  }
  if (typeof context === gin31) {
    keepScripts = context;
    context = false;
  }
  var base, parsed, scripts;
  if (!context) {
    if (support.createHTMLDocument) {
      context = document[gin798][gin797](gin38);
      base = context[gin8](gin800);
      base[gin268] = document[gin262][gin268];
      context[gin11][gin12](base);
    } else {
      context = document;
    }
  }
  parsed = rsingleTag[gin130](data);
  scripts = !keepScripts && [];
  if (parsed) {
    return [context[gin8](parsed[1])];
  }
  parsed = buildFragment([data], context, scripts);
  if (scripts && scripts[gin19]) {
    jQuery(scripts)[gin378]();
  }
  return jQuery[gin20]([], parsed[gin128]);
};
  jQuery[gin16][gin359] = function(url, params, callback) {
  var selector, type, response, self = this, off = url[gin235](gin53);
  if (off > -1) {
    selector = stripAndCollapse(url[gin120](off));
    url = url[gin120](0, off);
  }
  if (jQuery[gin32](params)) {
    callback = params;
    params = undefined;
  } else if (params && typeof params === gin2) {
    type = gin801;
  }
  if (self[gin19] > 0) {
    jQuery[gin756]({
  url: url, 
  type: type || gin696, 
  dataType: gin502, 
  data: params})[gin323](function(responseText) {
  response = arguments;
  self[gin502](selector ? jQuery(gin802)[gin509](jQuery[gin304](responseText))[gin174](selector) : responseText);
})[gin599](callback && function(jqXHR, status) {
  self[gin23](function() {
  callback[gin26](this, response || [jqXHR.responseText, status, jqXHR]);
});
});
  }
  return this;
};
  jQuery[gin23](["ajaxStart", "ajaxStop", "ajaxComplete", "ajaxError", "ajaxSuccess", "ajaxSend"], function(i, type) {
  jQuery[gin16][type] = function(fn) {
  return this[gin656](type, fn);
};
});
  jQuery[gin293][gin250][gin803] = function(elem) {
  return jQuery[gin300](jQuery[gin624], function(fn) {
  return elem === fn[gin465];
}).length;
};
  jQuery[gin553] = {
  setOffset: function(elem, options, i) {
  var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition, position = jQuery[gin395](elem, gin804), curElem = jQuery(elem), props = {};
  if (position === gin805) {
    elem[gin392][gin804] = gin282;
  }
  curOffset = curElem[gin553]();
  curCSSTop = jQuery[gin395](elem, gin163);
  curCSSLeft = jQuery[gin395](elem, gin579);
  calculatePosition = (position === gin538 || position === gin806) && (curCSSTop + curCSSLeft)[gin235](gin552) > -1;
  if (calculatePosition) {
    curPosition = curElem[gin804]();
    curTop = curPosition[gin163];
    curLeft = curPosition[gin579];
  } else {
    curTop = parseFloat(curCSSTop) || 0;
    curLeft = parseFloat(curCSSLeft) || 0;
  }
  if (jQuery[gin32](options)) {
    options = options[gin7](elem, i, jQuery[gin30]({}, curOffset));
  }
  if (options[gin163] != null) {
    props[gin163] = (options[gin163] - curOffset[gin163]) + curTop;
  }
  if (options[gin579] != null) {
    props[gin579] = (options[gin579] - curOffset[gin579]) + curLeft;
  }
  if (gin807 in options) {
    options[gin807][gin7](elem, props);
  } else {
    curElem[gin395](props);
  }
}};
  jQuery[gin16][gin30]({
  offset: function(options) {
  if (arguments.length) {
    return options === undefined ? this : this[gin23](function(i) {
  jQuery[gin553][gin808](this, options, i);
});
  }
  var doc, docElem, rect, win, elem = this[0];
  if (!elem) {
    return;
  }
  if (!elem[gin576]().length) {
    return {
  top: 0, 
  left: 0};
  }
  rect = elem[gin577]();
  doc = elem[gin129];
  docElem = doc[gin159];
  win = doc[gin162];
  return {
  top: rect[gin163] + win[gin809] - docElem[gin810], 
  left: rect[gin579] + win[gin811] - docElem[gin812]};
}, 
  position: function() {
  if (!this[0]) {
    return;
  }
  var offsetParent, offset, elem = this[0], parentOffset = {
  top: 0, 
  left: 0};
  if (jQuery[gin395](elem, gin804) === gin806) {
    offset = elem[gin577]();
  } else {
    offsetParent = this[gin813]();
    offset = this[gin553]();
    if (!nodeName(offsetParent[0], gin502)) {
      parentOffset = offsetParent[gin553]();
    }
    parentOffset = {
  top: parentOffset[gin163] + jQuery[gin395](offsetParent[0], gin814, true), 
  left: parentOffset[gin579] + jQuery[gin395](offsetParent[0], gin815, true)};
  }
  return {
  top: offset[gin163] - parentOffset[gin163] - jQuery[gin395](elem, gin816, true), 
  left: offset[gin579] - parentOffset[gin579] - jQuery[gin395](elem, gin523, true)};
}, 
  offsetParent: function() {
  return this[gin25](function() {
  var offsetParent = this.offsetParent;
  while (offsetParent && jQuery[gin395](offsetParent, gin804) === gin805) {
    offsetParent = offsetParent[gin813];
  }
  return offsetParent || documentElement;
});
}});
  jQuery[gin23]({
  scrollLeft: gin811, 
  scrollTop: gin809}, function(method, prop) {
  var top = gin809 === prop;
  jQuery[gin16][method] = function(val) {
  return access(this, function(elem, method, val) {
  var win;
  if (jQuery[gin57](elem)) {
    win = elem;
  } else if (elem[gin151] === 9) {
    win = elem[gin162];
  }
  if (val === undefined) {
    return win ? win[prop] : elem[method];
  }
  if (win) {
    win[gin817](!top ? val : win.pageXOffset, top ? val : win.pageYOffset);
  } else {
    elem[method] = val;
  }
}, method, val, arguments[gin19]);
};
});
  jQuery[gin23](["top", "left"], function(i, prop) {
  jQuery[gin570][prop] = addGetHookIf(support[gin818], function(elem, computed) {
  if (computed) {
    computed = curCSS(elem, prop);
    return rnumnonpx[gin136](computed) ? jQuery(elem)[gin804]()[prop] + gin398 : computed;
  }
});
});
  jQuery[gin23]({
  Height: gin575, 
  Width: gin525}, function(name, type) {
  jQuery[gin23]({
  padding: gin819 + name, 
  content: type, 
  gin38: gin820 + name}, function(defaultExtra, funcName) {
  jQuery[gin16][funcName] = function(margin, value) {
  var chainable = arguments[gin19] && (defaultExtra || typeof margin !== gin31), extra = defaultExtra || (margin === true || value === true ? "margin" : "border");
  return access(this, function(elem, type, value) {
  var doc;
  if (jQuery[gin57](elem)) {
    return funcName[gin235](gin820) === 0 ? elem[gin819 + name] : elem[gin219][gin159][gin821 + name];
  }
  if (elem[gin151] === 9) {
    doc = elem[gin159];
    return Math[gin544](elem[gin403][gin822 + name], doc[gin822 + name], elem[gin403][gin553 + name], doc[gin553 + name], doc[gin821 + name]);
  }
  return value === undefined ? jQuery[gin395](elem, type, extra) : jQuery[gin392](elem, type, value, extra);
}, type, chainable ? margin : undefined, chainable);
};
});
});
  jQuery[gin16][gin30]({
  bind: function(types, data, fn) {
  return this[gin656](types, null, data, fn);
}, 
  unbind: function(types, fn) {
  return this[gin438](types, null, fn);
}, 
  delegate: function(selector, types, data, fn) {
  return this[gin656](types, selector, data, fn);
}, 
  undelegate: function(selector, types, fn) {
  return arguments[gin19] === 1 ? this[gin438](selector, gin455) : this[gin438](types, selector || gin455, fn);
}});
  jQuery[gin823] = function(hold) {
  if (hold) {
    jQuery.readyWait++;
  } else {
    jQuery[gin306](true);
  }
};
  jQuery[gin34] = Array[gin34];
  jQuery[gin824] = JSON[gin373];
  jQuery[gin137] = nodeName;
  if (typeof define === gin40 && define[gin825]) {
    define(gin305, [], function() {
  return jQuery;
});
  }
  var _jQuery = gin5.jQuery, _$ = gin5.$;
  jQuery[gin826] = function(deep) {
  if (gin5[gin88] === jQuery) {
    gin5[gin88] = _$;
  }
  if (deep && gin5[gin35] === jQuery) {
    gin5[gin35] = _jQuery;
  }
  return jQuery;
};
  if (!noGlobal) {
    gin5[gin35] = gin5[gin88] = jQuery;
  }
  return jQuery;
});
}("use strict", "object", "exports", "jQuery requires a window with a document", window, "undefined", "call", "createElement", "script", "text", "head", "appendChild", "parentNode", "removeChild", "3.2.1", "fn", "toUpperCase", "prototype", "length", "merge", "constructor", "prevObject", "each", "pushStack", "map", "apply", "eq", "sort", "splice", "extend", "boolean", "isFunction", "isPlainObject", "isArray", "jQuery", "random", "replace", "", "type", "function", "window", "number", "string", "[object Object]", "ms-", "push", "concat", "guid", "now", "iterator", "Boolean Number String Function Array Date RegExp Object Error Symbol", "split", " ", "[object ", "]", "toLowerCase", "isWindow", "array", "sizzle", "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped", "[\\x20\\t\\r\\n\\f]", "(?:\\\\.|[\\w-]|[^\x00-\\xa0])+", "\\[", "*(", ")(?:", "*([*^$|!~]?=)", "*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(", "))|)", "*\\]", ":(", ")(?:\\((", "('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|", "((?:\\\\.|[^\\\\()[\\]]|", ")*)|", ".*", ")\\)|)", "+", "g", "^", "+|((?:^|[^\\\\])(?:\\\\.)*)", "+$", "*,", "*", "*([>+~]|", ")", "=", "*([^\\]'\"]*?)", "$", "ID", "^#(", "CLASS", "^\\.(", "TAG", "^(", "|[*])", "ATTR", "PSEUDO", "CHILD", "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(", "*(even|odd|(([+-]|)(\\d*)n|)", "*(?:([+-]|)", "*(\\d+)|))", "*\\)|)", "i", "bool", "^(?:", ")$", "needsContext", "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(", "*((?:-\\d)?\\d*)", "*\\)|)(?=[^-]|$)", "\\\\([\\da-f]{1,6}", "?|(", ")|.)", "ig", "0x", "fromCharCode", "\x00", "\ufffd", "slice", "\\", "charCodeAt", "toString", "disabled", "form", "label", "legend", "childNodes", "ownerDocument", "exec", "getElementById", "id", "getElementsByTagName", "getElementsByClassName", "qsa", "test", "nodeName", "getAttribute", "setAttribute", "#", "join", ",", "querySelectorAll", "removeAttribute", "$1", "cacheLength", "shift", "fieldset", "|", "attrHandle", "nodeType", "sourceIndex", "nextSibling", "input", "button", "isDisabled", "support", "isXML", "documentElement", "HTML", "setDocument", "defaultView", "top", "addEventListener", "unload", "attachEvent", "onunload", "attributes", "className", "createComment", "getById", "getElementsByName", "filter", "find", "getAttributeNode", "value", "innerHTML", "<a id='", "'></a>", "<select id='", "-\r\\' msallowcapture=''>", "<option selected=''></option></select>", "[msallowcapture^='']", "[*^$]=", "*(?:''|\"\")", "[selected]", "*(?:value|", "[id~=", "-]", "~=", ":checked", "a#", "+*", ".#.+[+~]", "<a href='' disabled='disabled'></a>", "<select disabled='disabled'><option/></select>", "hidden", "name", "D", "[name=d]", "*[*^$|!~]?=", ":enabled", ":disabled", "*,:x", ",.*:", "matchesSelector", "matches", "webkitMatchesSelector", "mozMatchesSelector", "oMatchesSelector", "msMatchesSelector", "disconnectedMatch", "[s!='']:x", "!=", "compareDocumentPosition", "contains", "unshift", "='$1']", "document", "attr", "specified", "escape", "error", "Syntax error, unrecognized expression: ", "uniqueSort", "getText", "firstChild", "selectors", ">", "previousSibling", "~", "nth", "even", "odd", "indexOf", "(^|", "(", "|$)", "class", "^=", "*=", "$=", "|=", "-", "last", "of-type", "only", "uniqueID", "pop", "pseudos", "setFilters", "unsupported pseudo: ", "hasOwnProperty", "not", "has", "textContent", "innerText", "lang", "unsupported lang: ", "xml:lang", "target", "location", "hash", "root", "focus", "activeElement", "hasFocus", "href", "enabled", "checked", "option", "selected", "empty", "parent", "header", "first", "lt", "gt", "filters", "tokenize", "preFilter", "relative", "0", "compile", "selector", "select", "sortStable", "detectDuplicates", "sortDetached", "<a href='#'></a>", "type|href|height|width", "<input/>", "expr", ":", "unique", "isXMLDoc", "escapeSelector", "is", "match", "grep", ":not(", "init", "<", "parseHTML", "jquery", "ready", "makeArray", "index", "prevAll", "get", "add", "iframe", "template", "content", "Until", "reverse", "Callbacks", "once", "stopOnFalse", "inArray", "fireWith", "promise", "done", "fail", "then", "notify", "progress", "memory", "resolve", "once memory", "resolved", "reject", "rejected", "pending", "catch", "Deferred", "With", "Thenable self-resolution", "notifyWith", "resolveWith", "exceptionHook", "stackTrace", "rejectWith", "getStackHook", "setTimeout", "disable", "lock", "fire", "state", "console", "warn", "jQuery.Deferred exception: ", "message", "stack", "readyException", "isReady", "removeEventListener", "DOMContentLoaded", "load", "readyState", "complete", "loading", "expando", "uid", "defineProperty", "cache", "camelCase", "set", "isEmptyObject", "true", "false", "null", "parse", "data-", "-$&", "hasData", "access", "remove", "hasDataAttrs", "fx", "queue", "_queueHooks", "dequeue", "inprogress", "queueHooks", "^(?:([+-])=|)(", ")([a-z%]*)$", "Top", "Right", "Bottom", "Left", "style", "display", "none", "css", "cur", "cssNumber", "px", ".5", "unit", "start", "end", "body", "block", "show", "hide", "<select multiple='multiple'>", "</select>", "<table>", "</table>", "<table><colgroup>", "</colgroup></table>", "<table><tbody>", "</tbody></table>", "<table><tbody><tr>", "</tr></tbody></table>", "optgroup", "tbody", "tfoot", "colgroup", "caption", "thead", "th", "td", "globalEval", "createDocumentFragment", "createTextNode", "div", "_default", "htmlPrefilter", "lastChild", "radio", "t", "checkClone", "cloneNode", "<textarea>x</textarea>", "noCloneChecked", "off", "event", "handler", "events", "handle", "triggered", "dispatch", ".", "special", "delegateCount", "setup", "global", "(^|\\.)", "\\.(?:.*\\.|)", "(\\.|$)", "origType", "namespace", "**", "teardown", "removeEvent", "handle events", "fix", "delegateTarget", "preDispatch", "handlers", "isPropagationStopped", "currentTarget", "elem", "isImmediatePropagationStopped", "rnamespace", "handleObj", "data", "result", "preventDefault", "stopPropagation", "postDispatch", "click", "Event", "originalEvent", "focusin", "blur", "focusout", "checkbox", "a", "returnValue", "isDefaultPrevented", "defaultPrevented", "relatedTarget", "timeStamp", "stopImmediatePropagation", "char", "which", "charCode", "addProp", "mouseover", "mouseout", "pointerover", "pointerout", "table", "tr", ">tbody", "/", "textarea", "defaultValue", "html", "clone", "_evalUrl", "src", "cleanData", "<$1></$2>", "insertBefore", "append", "replaceChild", "prepend", "before", "after", "replaceWith", ")(?!px)[a-z%]+$", "getComputedStyle", "cssText", "box-sizing:border-box;", "position:relative;display:block;", "margin:auto;border:1px;padding:1px;", "top:1%;width:50%", "1%", "marginLeft", "2px", "width", "4px", "marginRight", "50%", "backgroundClip", "content-box", "clearCloneStyle", "border:0;width:8px;height:0;top:0;left:-9999px;", "padding:0;margin-top:1px;position:absolute", "getPropertyValue", "pixelMarginRight", "minWidth", "maxWidth", "absolute", "400", "Webkit", "Moz", "ms", "cssProps", "max", "border", "margin", "padding", "Width", "boxSizing", "border-box", "boxSizingReliable", "auto", "offset", "opacity", "1", "animationIterationCount", "columnCount", "fillOpacity", "flexGrow", "flexShrink", "fontWeight", "lineHeight", "order", "orphans", "widows", "zIndex", "zoom", "float", "cssFloat", "cssHooks", "background", "inherit", "setProperty", "normal", "height", "getClientRects", "getBoundingClientRect", "reliableMarginLeft", "left", "Tween", "prop", "easing", "options", "propHooks", "pos", "duration", "step", "scrollTop", "scrollLeft", "cos", "PI", "swing", "requestAnimationFrame", "interval", "tick", "tweeners", "fxshow", "unqueued", "always", "toggle", "overflow", "inline", "inline-block", "overflowX", "overflowY", "expand", "prefilters", "startTime", "tweens", "run", "opts", "specialEasing", "stop", "proxy", "timer", "Animation", "createTween", "speed", "speeds", "old", "animate", "finish", "anim", "timers", "delay", "clearTimeout", "checkOn", "optSelected", "radioValue", "removeAttr", "attrHooks", "source", "propFix", "tabindex", "for", "htmlFor", "tabIndex", "readOnly", "maxLength", "cellSpacing", "cellPadding", "rowSpan", "colSpan", "useMap", "frameBorder", "contentEditable", "addClass", "removeClass", "toggleClass", "hasClass", "__className__", "valHooks", "val", "select-one", "selectedIndex", "on", "isTrigger", "trigger", "delegateType", "parentWindow", "bindType", "blur focus focusin focusout resize scroll click dblclick ", "mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave ", "change select submit keydown keypress keyup contextmenu", "mouseenter", "mouseleave", "onfocusin", "simulate", "parseXML", "parseFromString", "text/xml", "parsererror", "Invalid XML: ", "[", "param", "&", "serializeArray", "elements", "\r\n", "*/", "dataTypes", "ajaxSettings", "flatOptions", "mimeType", "getResponseHeader", "Content-Type", "converters", "responseFields", "dataFilter", "dataType", "* ", "throws", "No conversion from ", " to ", "success", "GET", "protocol", "application/x-www-form-urlencoded; charset=UTF-8", "text/plain", "text/html", "application/xml, text/xml", "application/json, text/javascript", "responseXML", "responseText", "responseJSON", "* text", "text html", "text json", "text xml", "ajaxSetup", "context", "statusCode", "canceled", "status", "abort", "url", "//", "method", "crossDomain", "host", "processData", "traditional", "ajaxStart", "hasContent", "?", "_=", "contentType", "application/x-www-form-urlencoded", "lastModified", "setRequestHeader", "If-Modified-Since", "etag", "If-None-Match", "Accept", "accepts", ", ", "; q=0.01", "headers", "beforeSend", "No Transport", "ajaxSend", "async", "timeout", "send", "Last-Modified", "HEAD", "nocontent", "notmodified", "statusText", "ajaxSuccess", "ajaxError", "ajaxComplete", "ajaxStop", "json", "post", "ajax", "firstElementChild", "wrapInner", "contents", "wrapAll", "visible", "offsetWidth", "offsetHeight", "xhr", "cors", "withCredentials", "ajaxTransport", "open", "username", "password", "xhrFields", "overrideMimeType", "X-Requested-With", "XMLHttpRequest", "onload", "onerror", "onabort", "onreadystatechange", "responseType", "response", "getAllResponseHeaders", "ajaxPrefilter", "text/javascript, application/javascript, ", "application/ecmascript, application/x-ecmascript", "text script", "<script>", "scriptCharset", "load error", "callback", "_", "json jsonp", "jsonp", "jsonpCallback", "script json", " was not called", "removeProp", "createHTMLDocument", "implementation", "<form></form><form></form>", "base", "POST", "<div>", "animated", "position", "static", "fixed", "using", "setOffset", "pageYOffset", "clientTop", "pageXOffset", "clientLeft", "offsetParent", "borderTopWidth", "borderLeftWidth", "marginTop", "scrollTo", "pixelPosition", "inner", "outer", "client", "scroll", "holdReady", "parseJSON", "amd", "noConflict");
